# NewGraphhopper/benchmark/

 ## benchmark
+ `benchmark.sh` : benchmark.sh 스크립트는 GraphHopper의 라우팅 알고리즘 성능을 측정하기 위해 다양한 설정과 구성을 사용하여 라우팅 요청을 실행하고, 그 결과를 분석할 수 있도록 출력하는 역할을 합니다.

+ `download_map.sh` : download_map.sh 스크립트는 GraphHopper에서 사용할 지도 데이터를 다운로드하기 위해 지정된 URL에서 OSM(PBF) 파일을 다운로드하고 압축을 풀어 지정된 디렉토리에 저장하는 역할을 합니다.

+ `post_benchmark.sh` : post_benchmark.sh 스크립트는 GraphHopper 벤치마크 테스트 후, 결과 파일을 분석하고, 벤치마크 실행 결과를 지정된 형식으로 정리하여 출력하거나 저장하는 역할을 합니다.

+ `very_custom.json` : very_custom.json 파일은 GraphHopper 벤치마크 테스트에서 사용되는 사용자 정의 설정을 포함한 JSON 파일로, 테스트 환경과 파라미터를 세부적으로 지정하여 특정 조건에서의 성능을 측정하도록 구성되어 있습니다.


----------------------------

# NewGraphhopper/client-hc/

## client-hc/src/main/java/com/graphhopper/api

+ `GHMRequest.java` : 이 코드는 GHMRequest 클래스이며, GraphHopper의 거리 또는 시간 매트릭스 요청을 구성하기 위한 데이터 모델을 정의합니다. 경로 계산의 입력 데이터로 프로필, 좌표 리스트, 힌트, 큐브사이드 설정, 스냅 방지 옵션, 출력 배열 설정 및 기타 요청 관련 매개변수를 설정하고 관리합니다.

+ `GHMatrixAbstractRequester.java` : 이 코드는 GHMatrixAbstractRequester 추상 클래스로, GraphHopper의 거리/시간 매트릭스 API 요청을 처리하는 기능을 제공합니다. 요청 데이터(JSON)를 생성하고 API 요청을 전송하며 응답 데이터를 처리 및 검증하는 메서드들을 포함하고 있습니다.

+ `GHMatrixBatchRequester.java` : GHMatrixBatchRequester 클래스는 GraphHopper의 거리 및 시간 매트릭스 계산을 비동기적으로 처리하는 기능을 제공합니다. 이 클래스는 주어진 출발지와 도착지 간의 거리 및 시간을 계산하기 위해 비동기 요청을 보내고, 결과를 수신하여 처리합니다. 이를 통해 대규모 매트릭스 계산을 효율적으로 수행할 수 있습니다.

+ `GHMatrixSyncRequester.java` : GHMatrixSyncRequester 클래스는 GraphHopper의 거리 및 시간 매트릭스 계산을 동기적으로 처리하는 기능을 제공합니다. 이 클래스는 주어진 출발지와 도착지 간의 거리 및 시간을 계산하기 위해 동기 요청을 보내고, 결과를 수신하여 처리합니다. 이를 통해 매트릭스 계산을 간단하고 직관적으로 수행할 수 있습니다.

+ `GraphHopperGeocoding.java` : `GraphHopperGeocoding` 클래스는 GraphHopper의 지오코딩 API와의 상호작용을 위한 클라이언트로, 주소를 좌표로 변환하거나(전방 지오코딩), 좌표를 주소로 변환하는(역방향 지오코딩) 기능을 제공합니다. 이 클래스는 `GHGeocodingRequest` 객체를 사용하여 요청을 구성하고, `GHGeocodingResponse` 객체를 통해 응답을 처리합니다. 이를 통해 애플리케이션에서 지오코딩 기능을 손쉽게 구현할 수 있습니다.

+ `GraphHopperMatrixWeb.java` : GraphHopperMatrixWeb 클래스는 GraphHopper의 거리 및 시간 매트릭스 API와 상호작용하는 클라이언트로, 주어진 출발지와 도착지 간의 거리 및 시간을 계산하는 기능을 제공합니다. 이 클래스는 GHMatrixAbstractRequester를 사용하여 요청을 처리하며, API 키 설정 및 서비스 URL 지정 등의 기능을 포함하고 있습니다.

+ `GraphHopperWeb.java` : GraphHopperWeb 클래스는 GraphHopper의 웹 API와 상호작용하는 Java 클라이언트로, 경로 계산, 거리 및 시간 측정, 턴바이턴 안내 등의 기능을 제공합니다. 이 클래스는 HTTP 요청을 통해 GraphHopper 서버와 통신하며, 다양한 매개변수를 설정하여 경로 계산을 수행할 수 있습니다.

+ `GzipRequestInterceptor.java` : `GzipRequestInterceptor` 클래스는 OkHttp 클라이언트의 요청 본문을 Gzip으로 압축하여 전송하는 인터셉터입니다. 이를 통해 네트워크 트래픽을 줄이고 전송 속도를 향상시킬 수 있습니다.  

+ `MatrixResponse.java` : `MatrixResponse` 클래스는 GraphHopper Directions API의 매트릭스 요청에 대한 응답을 나타내는 Java 모델입니다. 이 클래스는 여러 지점 간의 거리, 시간, 가중치 등의 정보를 포함하며, 경로 최적화나 거리 행렬 계산과 같은 기능을 지원합니다.  

+ `Version.java` : Version 클래스는 GraphHopper 라이브러리의 버전 정보를 관리하는 Java 클래스입니다. 이 클래스는 라이브러리의 현재 버전 번호를 제공하며, 주로 디버깅이나 로깅 목적으로 사용됩니다. 이를 통해 개발자는 사용 중인 GraphHopper의 정확한 버전을 확인할 수 있습니다.

## client-hc/src/main/java/com/graphhopper/api/model

* `GHGeocodingEntry` : 이 코드는 GHGeocodingEntry라는 클래스와 지리 정보(위도, 경도, 주소 등)를 관리하는 데 사용되는 데이터 모델을 정의합니다.

* `GHGeocodingRequest` : 이 코드는 GHGeocodingRequest 클래스이며, GraphHopper API의 지오코딩 요청을 구성하기 위한 데이터 구조를 정의합니다. Forward 또는 Reverse 지오코딩 요청에 필요한 매개변수(좌표, 쿼리, 언어, 제한 수, 제공자, 타임아웃)를 설정할 수 있습니다.

* `GHGeocodingResponse.java` : 이 코드는 GHGeocodingResponse 클래스이며, GraphHopper API에서 지오코딩 요청의 결과를 처리하기 위해 설계되었습니다. 요청 결과로 반환된 여러 지오코딩 항목(GHGeocodingEntry)을 저장하며, 로케일과 저작권 정보를 관리합니다.

## client-hc/src/test/java/com/graphhopper/api

* `AbstractGHMatrixWebTester.java` : AbstractGHMatrixWebTester 클래스는 GraphHopper의 매트릭스 API 클라이언트를 테스트하기 위한 추상 클래스입니다. 이 클래스는 다양한 테스트 케이스를 정의하여 매트릭스 API의 응답 처리, 오류 처리, 프로필 설정 등을 검증합니다. 이를 통해 매트릭스 API 클라이언트의 기능과 안정성을 확인할 수 있습니다.

* `Examples.java : Examples.java` 파일은 GraphHopper의 매트릭스 API 클라이언트 사용법을 보여주는 예제 코드 모음입니다. 이 파일에는 다양한 시나리오에 대한 코드 샘플이 포함되어 있어, 개발자가 매트릭스 API를 효과적으로 활용하는 데 도움이 됩니다. 

* `GHMatrixBatchTest.java` : GHMatrixBatchTest 클래스는 GraphHopper의 매트릭스 API 클라이언트의 배치 요청 기능을 테스트하는 JUnit 테스트 클래스입니다. 이 클래스는 AbstractGHMatrixWebTester를 상속하며, GraphHopperMatrixWeb 객체를 생성하여 배치 요청의 응답 처리와 오류 처리를 검증합니다. 이를 통해 매트릭스 API 클라이언트의 배치 처리 기능이 올바르게 작동하는지 확인할 수 있습니다.

* `GHMatrixSyncTest.java` : GHMatrixSyncTest 클래스는 GraphHopper의 매트릭스 API 클라이언트의 동기 요청 기능을 테스트하는 JUnit 테스트 클래스입니다. 이 클래스는 AbstractGHMatrixWebTester를 상속하며, GraphHopperMatrixWeb 객체를 생성하여 동기 요청의 응답 처리와 오류 처리를 검증합니다. 이를 통해 매트릭스 API 클라이언트의 동기 처리 기능이 올바르게 작동하는지 확인할 수 있습니다.

* `GraphHopperWebTest.java` : GraphHopperWebTest 클래스는 GraphHopper의 웹 API 클라이언트인 GraphHopperWeb의 기능을 검증하는 JUnit 테스트 클래스입니다. 이 클래스는 다양한 경로 요청 시나리오를 테스트하여, 클라이언트가 예상대로 작동하는지 확인합니다. 이를 통해 GraphHopperWeb의 경로 계산 및 응답 처리 기능이 올바르게 구현되었는지 검증할 수 있습니다. 

---------------------------

# NewGraphhopper/core/

## core/src/main/java/com/graphhopper

* `GraphHopper.java` : GraphHopperWebTest 클래스는 GraphHopper의 웹 API 클라이언트인 GraphHopperWeb의 기능을 검증하는 JUnit 테스트 클래스입니다. 이 클래스는 다양한 경로 요청 시나리오를 테스트하여, 클라이언트가 예상대로 작동하는지 확인합니다. 이를 통해 GraphHopperWeb의 경로 계산 및 응답 처리 기능이 올바르게 구현되었는지 검증할 수 있습니다.

* `GraphHopperConfig.java` : GraphHopperConfig.java는 GraphHopper의 설정을 관리하는 클래스입니다. 이 클래스는 config-example.yml 파일의 설정을 Java 객체로 변환하여, GraphHopper 인스턴스의 동작 방식을 제어합니다.

## core/src/main/java/com/graphhopper/apache/commons/collections

* `IntFloatBinaryHeap.java` : `IntFloatBinaryHeap` 클래스는 정수 키와 부동 소수점 값을 효율적으로 관리하는 최소 힙(min-heap) 자료 구조를 구현합니다. 이 클래스는 우선순위 큐와 같은 기능을 제공하며, 최솟값을 빠르게 추출하거나 새로운 요소를 삽입할 수 있습니다. 이를 통해 그래프 탐색 알고리즘 등에서 효율적인 경로 계산이 가능합니다. 

* `StringUtils.java` : StringUtils 클래스는 문자열 조작을 위한 다양한 유틸리티 메서드를 제공하여, 문자열 처리 작업을 간편하게 수행할 수 있도록 돕습니다. 

## core/src/main/java/com/graphhopper/coll

* `GHBitSet.java` : `GHBitSet` 인터페이스는 정수 집합을 효율적으로 관리하기 위한 메서드를 정의합니다. 이 인터페이스는 `GHBitSetImpl` 클래스에서 구현되며, `BitSet` 클래스를 확장하여 `GHBitSet` 인터페이스를 구현합니다.  

* `GHBitSetImpl.java` : `GHBitSetImpl` 클래스는 `BitSet`을 확장하여 `GHBitSet` 인터페이스를 구현하는 클래스입니다. 이 클래스는 비트 집합의 추가, 제거, 포함 여부 확인 등의 기능을 제공합니다.  

* `GHIntHashSet.java` : `GHIntHashSet`는 GraphHopper 프로젝트에서 사용되는 정수 전용 해시 집합 클래스입니다. 이 클래스는 `IntHashSet`을 상속하며, 다양한 생성자를 통해 초기 용량, 로드 팩터, 해시 순서 혼합 전략 등을 설정할 수 있습니다. 또한, 다른 `IntContainer`로부터 요소를 받아들이는 생성자도 제공하여 유연한 초기화를 지원합니다.  

* `GHIntLongHashMap.java` : GHIntLongHashMap는 IntLongHashMap을 상속하여, GraphHopper에서 정수 키와 긴 정수 값을 효율적으로 저장하고 관리하는 해시 맵을 제공합니다.

* `GHIntObjectHashMap.java` : GHIntObjectHashMap 클래스는 GraphHopper 프로젝트에서 사용하는 정수 키와 객체 값을 효율적으로 매핑하는 해시 맵입니다.

* `GHLongHashSet.java` : `GHLongHashSet` 클래스는 GraphHopper 프로젝트에서 사용되는 정수형 해시 집합으로, `LongHashSet`을 상속하여 초기 용량, 로드 팩터, 해시 순서 혼합 전략 등을 설정할 수 있는 다양한 생성자를 제공합니다.  

* `GHLongLongBTree.java` : `GHLongLongBTree`는 GraphHopper 프로젝트의 `com.graphhopper.coll` 패키지에 포함된 클래스입니다. 이 클래스는 메모리 내에서 B-트리를 구현하여, 키와 값을 모두 `long` 타입으로 저장하는 기능을 제공합니다. 특히, 값의 크기를 1에서 8바이트까지 설정할 수 있으며, 삭제 연산은 지원하지 않습니다.  

* `GHLongLongHashMap.java` : GHLongLongHashMap는 GraphHopper 프로젝트에서 사용되는 해시맵 구현체로, long 타입의 키와 값을 저장합니다. 이 클래스는 com.carrotsearch.hppc.LongLongHashMap을 상속하여, GraphHopper의 특정 요구사항에 맞게 커스터마이즈되었습니다. 이를 통해 대규모의 long 타입 데이터를 효율적으로 관리할 수 있습니다.

* `GHLongObjectHashMap.java` : `GHLongObjectHashMap`는 GraphHopper 프로젝트의 `com.graphhopper.coll` 패키지에 포함된 클래스입니다. 이 클래스는 `LongObjectHashMap`을 상속하며, 다양한 생성자를 통해 초기 용량, 로드 팩터, 해시 순서 혼합 전략 등을 설정할 수 있습니다.  

* `GHObjectIntHashMap.java` : `GHObjectIntHashMap`는 `ObjectIntHashMap`을 상속하여, 객체를 키로 하고 정수를 값으로 저장하는 해시맵입니다. 이 클래스는 `DETERMINISTIC` 해시 순서 혼합 전략을 사용하여 해시 순서를 결정합니다.  

* `GHSortedCollection.java` : `GHTBitSet`는 `GHBitSet` 인터페이스를 구현하며, 내부적으로 `GHIntHashSet`을 사용하여 비트셋 기능을 제공합니다. 이는 적은 수의 항목을 처리할 때 효율적입니다.  

* `GHTBitSet.java` : `GHTBitSet`는 `GHBitSet` 인터페이스를 구현하는 클래스입니다. 내부적으로 `GHIntHashSet`을 사용하여 비트셋 기능을 제공합니다. 이는 적은 수의 항목을 처리할 때 효율적입니다.  

* `LongLongMap.java` : LongLongMap는 GraphHopper 프로젝트의 com.graphhopper.coll 패키지에 정의된 인터페이스로, long 타입의 키와 값을 저장하는 맵 구조를 제공합니다.

* `MapEntry.java` : MapEntry는 GraphHopper 프로젝트에서 사용되는 불변의 키-값 쌍을 나타내는 클래스입니다. 이 클래스는 Map.Entry<K, V> 인터페이스를 구현하며, 키와 값을 저장하는 동시에 equals, hashCode, toString 메서드를 재정의하여 객체 비교와 출력 기능을 제공합니다.

* `MinHeapWithUpdate.java` : `MinHeapWithUpdate`는 GraphHopper 프로젝트의 `com.graphhopper.coll` 패키지에 포함된 클래스입니다. 이 클래스는 최소 힙(min-heap) 자료구조를 구현하며, 기존 요소의 값을 효율적으로 업데이트할 수 있는 기능을 제공합니다. 이를 통해 그래프 탐색 알고리즘 등에서 우선순위 큐로 활용할 수 있습니다.  

## core/src/main/java/com/graphhopper/config

* `CHProfile.java` : `CHProfile` 클래스는 GraphHopper의 `com.graphhopper.config` 패키지에 위치하며, `config.yml` 파일의 `profiles_ch` 섹션에 해당하는 엔트리를 나타냅니다. 이 클래스는 특정 라우팅 프로파일을 정의하며, 해당 프로파일을 사용하여 Contraction Hierarchies(CH)를 준비합니다.  

* `LMProfile.java` : LMProfile 클래스는 GraphHopper의 com.graphhopper.config 패키지에 위치하며, config.yml 파일의 profiles_lm 섹션에 해당하는 엔트리를 나타냅니다. 이 클래스는 랜드마크(Landmarks, LM)를 사용하여 준비된 라우팅 프로파일을 정의합니다.

* `Profile.java` : Profile 클래스는 GraphHopper의 com.graphhopper.config 패키지에 위치하며, config.yml 파일의 profiles 섹션에 해당하는 엔트리를 나타냅니다. 이 클래스는 라우팅 프로파일의 이름, 차량 유형, 가중치 방식, 커스텀 모델, 턴 비용 설정 등을 정의하여, 다양한 라우팅 요구사항에 맞게 경로 계산을 구성할 수 있도록 합니다.

## core/src/main/java/com/graphhopper/debatty/java/stringsimilarity

* `JaroWinkler.java` : JaroWinkler 클래스는 문자열 유사도를 측정하기 위한 Jaro-Winkler 알고리즘의 Java 구현체입니다. 이 알고리즘은 주로 사람의 이름과 같은 짧은 문자열에서 철자 오류나 전치 오류를 감지하는 데 효과적입니다.

## core/src/main/java/com/graphhopper/geohash

* `SpatialKeyAlgo.java` : SpatialKeyAlgo 클래스는 GraphHopper 프로젝트의 com.graphhopper.geohash 패키지에 위치하며, 공간 데이터를 효율적으로 인코딩하고 디코딩하는 기능을 제공합니다. 이 클래스는 지정된 비트 수와 경계 상자를 기반으로 지리적 좌표를 이진 키로 변환하거나, 반대로 이진 키를 지리적 좌표로 변환하는 알고리즘을 구현합니다. 이를 통해 공간 데이터의 저장 및 검색을 최적화할 수 있습니다.

## core/src/main/java/com/graphhopper/isochrone/algorithm

* `ContourBuilder.java` : `ContourBuilder`는 GraphHopper의 `com.graphhopper.isochrone.algorithm` 패키지에 위치한 클래스입니다. 이 클래스는 주어진 등고선 값(`z0`)에 대해 등고선을 계산하고, 결과를 `MultiPolygon` 형식으로 반환합니다. 이를 통해 등고선 생성 및 분석을 효율적으로 수행할 수 있습니다.  

* `JTSTriangulator.java` : JTSTriangulator는 GraphHopper 프로젝트의 com.graphhopper.isochrone.algorithm 패키지에 위치한 클래스입니다. 이 클래스는 JTS Topology Suite를 활용하여 이소크론(등시간선)을 생성하는 기능을 제공합니다. 이를 통해 특정 지점에서 일정 시간 내에 도달 가능한 영역을 효율적으로 계산할 수 있습니다.

* `QuadEdgeAsReadableQuadEdge.java` : QuadEdgeAsReadableQuadEdge 클래스는 GraphHopper의 com.graphhopper.isochrone.algorithm 패키지에 위치하며, JTS(Java Topology Suite)의 QuadEdge를 ReadableQuadEdge 인터페이스로 변환하는 어댑터 역할을 합니다. 이를 통해 QuadEdge 객체를 ReadableQuadEdge로 취급하여, 다양한 알고리즘에서 활용할 수 있도록 지원합니다.

* `QuadEdgeSubdivisionAsReadableTriangulation.java` : QuadEdgeSubdivisionAsReadableTriangulation 클래스는 GraphHopper의 com.graphhopper.isochrone.algorithm 패키지에 위치하며, JTS Topology Suite의 QuadEdgeSubdivision을 ReadableTriangulation 인터페이스로 변환하는 어댑터 역할을 합니다. 이를 통해 삼각형 메쉬 데이터를 효율적으로 처리하고, 등시간선(isoline) 분석과 같은 공간 분석 작업을 수행할 수 있습니다.

* `ReadableQuadEdge.java` : ReadableQuadEdge는 GraphHopper 프로젝트의 com.graphhopper.isochrone.algorithm 패키지에 위치한 인터페이스로, 삼각형 메쉬 구조에서 읽기 전용의 QuadEdge를 나타냅니다. 이 인터페이스는 삼각형 메쉬의 위상 정보를 효율적으로 탐색하고 처리하기 위한 메서드를 정의하고 있습니다. 이를 통해 삼각형 메쉬의 구조를 이해하고 활용하는 데 도움을 줍니다.

* `ReadableTriangulation.java` : ReadableTriangulation은 GraphHopper의 com.graphhopper.isochrone.algorithm 패키지에 정의된 인터페이스로, 삼각형 메쉬 구조를 읽기 위한 메서드를 제공합니다. 이 인터페이스는 삼각형 메쉬의 정점(Vertex)과 간선(Edge)에 접근할 수 있는 기능을 정의하여, 등고선 생성이나 등시간선 분석과 같은 공간 분석 작업에 활용됩니다.

* `ShortestPathTree.java` : ShortestPathTree는 GraphHopper 프로젝트의 com.graphhopper.isochrone.algorithm 패키지에 위치한 클래스입니다. 이 클래스는 특정 출발 지점에서 그래프의 모든 노드까지의 최단 경로를 계산하여, 각 노드에 대한 최단 경로 트리를 구성합니다. 이를 통해 이소크론 분석 등 다양한 경로 탐색 및 분석 작업을 수행할 수 있습니다.

* `Triangulation.java` : Triangulation.java는 GraphHopper 프로젝트의 com.graphhopper.isochrone.algorithm 패키지에 위치한 클래스입니다. 이 클래스는 등시간선(isoline) 생성을 위한 삼각형 분할 알고리즘을 구현합니다. 특히, 주어진 점 집합을 기반으로 삼각형을 생성하여 공간을 분할하고, 이를 통해 등시간선을 효율적으로 계산할 수 있도록 지원합니다.

* `TriangulationAsReadableTriangulation.java` : TriangulationAsReadableTriangulation 클래스는 GraphHopper의 com.graphhopper.isochrone.algorithm 패키지에 위치하며, Triangulation 인터페이스를 구현하여 삼각분할 데이터를 읽기 전용으로 제공하는 역할을 합니다. 이 클래스는 내부적으로 삼각형 목록을 관리하며, 각 삼각형은 세 개의 정점으로 구성됩니다. 이를 통해 등시간선(isoline) 분석 등에서 효율적인 삼각형 데이터 접근을 지원합니다.

* `Triangulator.java` : Triangulator 클래스는 GraphHopper의 com.graphhopper.isochrone.algorithm 패키지에 위치하며, 등시간선(isoline) 생성을 위한 삼각분할(triangulation) 알고리즘을 구현합니다. 이 클래스는 주어진 점 집합을 삼각형으로 분할하여, 등시간선을 효율적으로 계산하는 데 사용됩니다.

## core/src/main/java/com/graphhopper/reader

* `ReaderElement.java` : ReaderElement 클래스는 GraphHopper에서 OSM 데이터를 읽고 처리하기 위한 XML 요소의 타입과 속성을 정의하는 역할을 합니다.

* `ReaderNode.java` : ReaderNode 클래스는 GraphHopper에서 OSM 노드 데이터를 나타내며, 노드의 ID, 좌표(위도, 경도), 및 태그 정보를 관리하는 역할을 합니다.

* `ReaderRelation.java` : ReaderRelation 클래스는 GraphHopper에서 OSM 관계(Relation) 데이터를 나타내며, 관계의 ID, 역할이 지정된 멤버, 및 태그 정보를 관리하는 역할을 합니다.

* `ReaderWay.java` : ReaderWay 클래스는 GraphHopper에서 OSM 도로(Way) 데이터를 나타내며, 도로의 ID, 노드 리스트, 태그 정보를 관리하여 라우팅과 관련된 데이터를 처리하는 역할을 합니다.

## core/src/main/java/com/graphhopper/reader/dem

* `AbstractSRTMElevationProvider.java` : AbstractSRTMElevationProvider 클래스는 GraphHopper에서 SRTM(Satellite Radar Topography Mission) 데이터를 기반으로 고도 정보를 제공하기 위한 추상 클래스이며, 파일 로드 및 고도 계산과 같은 기본 동작을 정의하는 역할을 합니다.

* `AbstractTiffElevationProvider.java` : AbstractTiffElevationProvider 클래스는 GraphHopper에서 GeoTIFF 형식의 고도 데이터를 처리하기 위한 추상 클래스이며, GeoTIFF 파일을 읽고 고도 정보를 추출하는 기본 기능을 제공하는 역할을 합니다.

* `CGIARProvider.java` : CGIARProvider 클래스는 GraphHopper에서 CGIAR(Consultative Group for International Agricultural Research) SRTM 데이터를 사용하여 고도 정보를 제공하며, SRTM 데이터를 다운로드, 캐싱, 및 처리하는 구체적인 구현을 담당합니다.

* `EdgeElevationInterpolator.java` : EdgeElevationInterpolator 클래스는 GraphHopper에서 그래프의 엣지(도로)에 대해 고도 데이터를 보간(interpolate)하여 매끄러운 고도 프로파일을 생성하는 역할을 합니다.

* `EdgeElevationSmoothingMovingAverage.java` : EdgeElevationSmoothingMovingAverage 클래스는 GraphHopper에서 도로(엣지) 고도 데이터를 이동 평균 알고리즘을 사용해 평활화(smoothing)하여 더 자연스러운 고도 프로파일을 생성하는 역할을 합니다.

* `EdgeElevationSmoothingRamer.java` : EdgeElevationSmoothingRamer 클래스는 GraphHopper에서 래머-더글러스-퍼커 알고리즘(Ramer-Douglas-Peucker algorithm)을 사용하여 도로(엣지)의 고도 데이터를 단순화(simplification)하고 평활화(smoothing)하는 역할을 합니다.

* `EdgeSampling.java` : EdgeSampling 클래스는 GraphHopper에서 도로(엣지) 고도 데이터를 샘플링하여 지정된 간격으로 고도 정보를 생성 및 관리하는 역할을 합니다.

* `ElevationInterpolator.java` : ElevationInterpolator 클래스는 GraphHopper에서 고도 데이터를 보간(interpolation)하여 도로(엣지) 또는 노드 간의 고도 정보를 계산하고 추가하는 역할을 합니다.

* `ElevationProvider.java` : ElevationProvider 인터페이스는 GraphHopper에서 고도 데이터를 제공하기 위한 계약을 정의하며, 고도 값을 가져오는 메서드와 고도 데이터 초기화 및 관리를 위한 기능을 규정하는 역할을 합니다.

* `GMTEDProvider.java` : GMTEDProvider 클래스는 GraphHopper에서 GMTED2010 (Global Multi-resolution Terrain Elevation Data) 데이터를 사용하여 고도 정보를 제공하며, 고도 데이터의 다운로드, 로드, 및 조회를 처리하는 역할을 합니다.

* `HGTProvider.java` : HGTProvider 클래스는 GraphHopper에서 HGT 형식의 SRTM 데이터를 사용하여 고도 정보를 제공하며, HGT 파일의 로드, 캐싱, 및 고도 값 조회를 처리하는 역할을 합니다.

* `HeightTile.java` : HeightTile 클래스는 GraphHopper에서 고도 데이터를 타일 단위로 관리하기 위한 객체로, 특정 영역의 고도 값을 저장하고 이를 조회하거나 계산하는 역할을 합니다.

* `MultiSourceElevationProvider.java` : MultiSourceElevationProvider 클래스는 GraphHopper에서 여러 고도 데이터 소스(SRTM, GMTED 등)를 통합하여 고도 정보를 제공하며, 데이터 소스 간의 우선순위와 결합 로직을 관리하는 역할을 합니다.

* `NodeElevationInterpolator.java` : NodeElevationInterpolator 클래스는 GraphHopper에서 노드(node) 간의 고도 데이터를 보간(interpolation)하여 매끄러운 고도 프로파일을 생성하고 고도 값을 보완하는 역할을 합니다.

* `SRTMGL1Provider.java` : SRTMGL1Provider 클래스는 GraphHopper에서 SRTMGL1 (Shuttle Radar Topography Mission Global 1 Arc-Second) 데이터를 사용하여 고도 정보를 제공하며, 고해상도 SRTM 데이터를 다운로드, 캐싱, 및 조회하는 역할을 합니다.

* `SRTMProvider.java` : SRTMProvider 클래스는 GraphHopper에서 SRTM (Shuttle Radar Topography Mission) 데이터를 사용하여 고도 정보를 제공하며, SRTM 데이터를 다운로드, 읽기, 캐싱, 및 조회하는 기본 기능을 구현하는 역할을 합니다.

* `SkadiProvider.java` : SkadiProvider 클래스는 GraphHopper에서 Skadi 형식의 고도 데이터를 처리하여 고도 정보를 제공하며, Skadi 데이터 파일의 다운로드, 캐싱, 및 고도 값을 계산하고 조회하는 역할을 합니다.

* `TileBasedElevationProvider.java` : TileBasedElevationProvider 클래스는 GraphHopper에서 타일 기반의 고도 데이터를 관리하며, 특정 지역의 고도 값을 타일 단위로 로드, 캐싱, 및 조회하는 역할을 합니다.

## core/src/main/java/com/graphhopper/reader/osm

* `OSMFileHeader.java` : OSMFileHeader.java 클래스는 GraphHopper에서 OSM 파일의 헤더 정보를 처리하며, 파일의 메타데이터(예: OSM 파일의 버전, 작성자, 파일 크기 등)를 파싱하고 관리하는 역할을 합니다.

* `OSMInput.java` : OSMInput.java 클래스는 GraphHopper에서 OSM 데이터를 읽어들이는 기능을 담당하며, OSM 파일을 파싱하고, 파일 내의 노드, 도로, 관계 데이터를 처리하여 그래프 구조로 변환하는 역할을 합니다.

* `OSMInputFile.java` : OSMInputFile.java 클래스는 GraphHopper에서 OSM 파일을 읽고 처리하는 기능을 제공하며, 파일을 스트림으로 열고, 해당 파일 내의 OSM 데이터를 파싱하여 처리할 수 있도록 하는 역할을 합니다.

* `OSMNodeData.java` : OSMNodeData.java 클래스는 GraphHopper에서 OSM 노드 데이터를 나타내며, 노드의 ID, 좌표(위도, 경도), 및 관련 태그 정보를 관리하고 저장하는 역할을 합니다.

* `OSMReader.java` : OSMReader.java 클래스는 GraphHopper에서 OSM 데이터를 읽고 파싱하는 주요 클래스이며, OSM 파일을 처리하고 노드, 도로, 관계 등의 데이터를 추출하여 그래프를 생성하는 역할을 합니다.

* `OSMReaderUtility.java` : OSMReaderUtility.java 클래스는 GraphHopper에서 OSM 데이터를 읽는 데 필요한 유틸리티 기능을 제공하며, OSM 파일을 효율적으로 파싱하고, 데이터의 오류를 처리하거나 성능을 최적화하는 데 사용되는 보조적인 메서드들을 포함하는 역할을 합니다.

* `OSMRestrictionConverter.java` : OSMRestrictionConverter.java 클래스는 GraphHopper에서 OSM의 제한(예: 특정 도로에서의 회전 금지, 진입 금지 등) 정보를 처리하고, 이를 GraphHopper의 그래프 모델에 맞게 변환하는 역할을 합니다. 이를 통해 라우팅 시 도로 제한 사항을 반영할 수 있게 됩니다.

* `OSMRestrictionException.java` : OSMRestrictionException.java 클래스는 GraphHopper에서 OSM 제한 처리 중 발생할 수 있는 예외를 정의하는 클래스입니다. 이 예외는 OSM 파일에서 제한 정보를 처리할 때 발생하는 오류를 처리하기 위해 사용됩니다.

* `OSMXMLHelper.java` : OSMXMLHelper.java 클래스는 GraphHopper에서 OSM XML 파일을 파싱하고 처리하는 데 도움을 주는 유틸리티 클래스입니다. 이 클래스는 XML 데이터를 효율적으로 읽고, OSM 데이터를 추출하거나 변환하는 데 필요한 보조적인 메서드를 제공합니다.

* `Pair.java` : Pair.java 클래스는 GraphHopper에서 두 개의 관련된 값을 쌍으로 묶어 처리하기 위한 간단한 데이터 구조 클래스입니다. 일반적으로 OSM 데이터 처리나 그래프 생성 시 두 값을 함께 저장하고 전달하는 데 사용됩니다.

* `PillarInfo.java` : PillarInfo.java 클래스는 GraphHopper에서 OSM 데이터에서 특정한 기둥(Pillar)과 관련된 정보를 관리하는 클래스입니다. 이 클래스는 도로 및 노드와 관련된 기둥 정보를 저장하며, 주로 지도에서 고도나 위치와 관련된 특수한 요소를 처리하는 데 사용됩니다.

* `RestrictionMembers.java` : RestrictionMembers.java 클래스는 GraphHopper에서 OSM의 제한(Restriction) 정보를 처리하는 데 사용되는 클래스입니다. 이 클래스는 특정 제한이 적용되는 도로의 구성 요소들(예: 특정 방향으로만 진행 가능한 도로, 회전 금지 등)을 나타내며, 제한 사항을 구성하는 멤버들의 목록을 관리합니다.

* `RestrictionTagParser.java` : RestrictionTagParser.java 클래스는 GraphHopper에서 OSM의 제한 태그 정보를 파싱하는 역할을 합니다. 이 클래스는 OSM 데이터에서 제한 사항을 나타내는 태그(예: 회전 금지, 진입 금지 등)를 해석하고, 이를 GraphHopper의 라우팅 시스템에서 적용할 수 있는 형식으로 변환하는 기능을 제공합니다.

* `RestrictionTopology.java` : RestrictionTopology.java 클래스는 GraphHopper에서 OSM 데이터에 정의된 제한 사항(예: 회전 금지, 진입 금지 등)과 관련된 토폴로지(구조적 관계)를 관리하는 클래스입니다. 이 클래스는 제한이 적용되는 도로의 연결 관계를 파악하고, 라우팅 시 제한 사항을 올바르게 반영할 수 있도록 합니다.

* `RestrictionType.java` : RestrictionType.java 클래스는 GraphHopper에서 OSM의 도로 제한 유형을 정의하는 열거형(Enum) 클래스입니다. 이 클래스는 제한 사항의 종류를 정의하며, 예를 들어 "회전 금지", "진입 금지", "일방통행" 등의 다양한 제한 유형을 구분하여 라우팅 시 적절한 제한을 적용할 수 있게 합니다.

* `SegmentNode.java` : SegmentNode.java 클래스는 GraphHopper에서 OSM 데이터를 기반으로 생성된 그래프에서, 도로의 세그먼트(segment)와 관련된 노드 정보를 관리하는 역할을 합니다. 이 클래스는 도로 세그먼트의 시작과 끝을 나타내는 노드들을 연결하고, 그래프 구조 내에서 세그먼트가 올바르게 연결되도록 처리하는 데 사용됩니다.

* `SkipOptions.java` : SkipOptions.java 클래스는 GraphHopper에서 OSM 데이터 로딩 과정에서 일부 데이터를 건너뛰기 위한 설정을 정의하는 클래스입니다. 이 클래스는 특정 조건에 맞는 OSM 요소(예: 노드, 도로 등)를 건너뛰거나 무시할 수 있도록 하는 옵션을 제공하여, 데이터 로딩 성능을 최적화하거나 불필요한 데이터를 제외할 수 있게 합니다.

* `Triple.java` : Triple.java 클래스는 GraphHopper에서 OSM 데이터를 처리하는 데 사용되는 간단한 데이터 구조로, 세 개의 관련된 값을 묶어서 저장하는 역할을 합니다. 이 클래스는 주로 OSM에서 처리해야 할 세 가지 요소(예: 노드 ID, 좌표, 태그 등)를 함께 관리하기 위해 사용됩니다.

* `WaySegmentParser.java` : WaySegmentParser.java 클래스는 GraphHopper에서 OSM의 도로(Way) 데이터를 파싱하고 처리하는 클래스입니다. 이 클래스는 도로의 세그먼트 정보를 분석하여, 도로를 그래프의 엣지로 변환하는 작업을 수행합니다. 이를 통해 도로 데이터가 라우팅에 적합한 형태로 변환됩니다.

* `WayToEdgeConverter.java` : WayToEdgeConverter.java 클래스는 GraphHopper에서 OSM의 도로(Way) 데이터를 그래프의 엣지(Edge)로 변환하는 역할을 합니다. 이 클래스는 OSM 도로의 세그먼트를 분석하여, 해당 도로 정보를 라우팅 그래프의 엣지 형식에 맞게 변환하고, 경로 계산에 필요한 정보를 제공할 수 있도록 합니다.

* `WayToEdgesMap.java` : WayToEdgesMap.java 클래스는 GraphHopper에서 OSM의 도로(Way) 데이터를 여러 개의 엣지(Edge)로 매핑(mapping)하는 역할을 합니다. 이 클래스는 도로의 각 세그먼트를 그래프의 엣지에 적절히 연결하여, 도로 데이터가 라우팅 시스템에 맞게 구성되도록 합니다. 즉, 하나의 도로가 여러 개의 엣지로 분해되어 라우팅 계산에 활용될 수 있게 합니다.

## core/src/main/java/com/graphhopper/reader/osm/conditional

* `ConditionalValueParser.java` : ConditionalValueParser.java 클래스는 GraphHopper에서 OSM의 조건부 태그 데이터를 파싱하는 역할을 합니다. 이 클래스는 도로의 조건부 제한(예: 특정 시간대에만 유효한 제한, 특정 차량 유형에만 적용되는 제한 등)을 처리하고, 이를 라우팅 시스템에서 적용할 수 있는 형식으로 변환하는 기능을 제공합니다.

* `DateRange.java` : DateRange.java 클래스는 GraphHopper에서 OSM의 조건부 제한 중 특정 날짜 범위에 적용되는 제한을 나타내는 클래스입니다. 이 클래스는 제한이 유효한 시작 날짜와 종료 날짜를 관리하며, 특정 기간에만 적용되는 도로 제한을 처리하는 데 사용됩니다. 예를 들어, 특정 도로의 제한이 특정 날짜에만 적용되는 경우 이 클래스를 통해 그 기간을 정의할 수 있습니다.

* `DateRangeParser.java` : DateRangeParser.java 클래스는 GraphHopper에서 OSM의 조건부 제한 중 날짜 범위에 적용되는 데이터를 파싱하는 역할을 합니다. 이 클래스는 도로 제한이 특정 기간에만 적용되는 경우, 해당 날짜 범위를 해석하고 라우팅 시스템에 필요한 형식으로 변환하여 조건부 제한을 적절하게 처리할 수 있도록 합니다.

* `ParsedCalendar.java` : ParsedCalendar.java 클래스는 GraphHopper에서 OSM의 조건부 제한을 처리할 때 사용되는 클래스입니다. 이 클래스는 특정 날짜 및 시간 범위에 대한 제한을 계산하고, 조건부 제한이 유효한 시간대를 정확하게 파악할 수 있도록 돕습니다. 즉, 특정 도로의 제한이 특정 날짜나 시간에만 적용되는 경우 이를 해석하고 처리하는 데 필요한 정보를 관리합니다.


## core/src/main/java/com/graphhopper/reader/osm/pbf

* `PbfBlobDecoder.java` : PbfBlobDecoder 클래스는 PBF 형식의 블롭 데이터를 읽고 해석하여 OSM 데이터의 노드, 웨이, 관계 등의 엔티티로 변환하며, 이를 비동기적으로 처리해 지정된 리스너에 전달하는 역할을 합니다.

* `PbfBlobDecoderListener.java` : PbfBlobDecoderListener는 PBF 디코딩 작업의 성공 또는 실패 결과를 처리하는 인터페이스입니다.

* `PbfBlobResult.java` : PbfBlobResult는 PBF 블롭 디코딩의 성공 또는 실패 상태와 결과 데이터를 저장 및 관리하는 클래스입니다.

* `PbfDecoder.java` : PbfDecoder는 PBF 형식의 OSM 데이터를 다중 스레드를 활용해 디코딩하고 처리하는 역할을 담당하는 클래스입니다.

* `PbfFieldDecoder.java` : PbfFieldDecoder는 PBF 데이터에서 압축된 필드 값을 해독하여 읽기 가능한 형식으로 변환하는 역할을 하는 클래스입니다.

* `PbfRawBlob.java` : PbfRawBlob은 PBF 블롭 데이터의 원시 바이트를 저장하고 관리하는 클래스입니다.

* `PbfReader.java` : PbfReader는 PBF 형식의 OSM 데이터를 읽어들여 디코딩하고, 결과를 OSM 데이터 소비자에게 전달하는 역할을 수행하는 클래스입니다.

* `PbfStreamSplitter.java` : PbfStreamSplitter는 PBF 파일 스트림을 개별 블롭 단위로 분리하여 처리하는 클래스입니다.

* `Sink.java` : Sink는 디코딩된 OSM 데이터를 소비하고 처리하기 위한 인터페이스입니다.

## core/src/main/java/com/graphhopper/routing

* `AStar.java` : AStar는 그래프 데이터를 기반으로 A* 알고리즘을 사용해 최단 경로를 계산하는 클래스입니다.

* `AStarBidirection.java` : AStarBidirection은 A* 알고리즘의 양방향 버전을 구현하여 경로 탐색 효율을 향상시키는 클래스입니다.

* `AStarBidirectionCH.java` : AStarBidirectionCH는 A* 양방향 알고리즘에 수축 계층(CH)을 결합하여 경로 탐색을 더욱 효율적으로 수행하는 클래스입니다.

* `AStarBidirectionEdgeCHNoSOD.java` : AStarBidirectionEdgeCHNoSOD는 A* 양방향 알고리즘과 수축 계층(CH)을 사용하되, 순서 독립적 탐색(SOD)을 비활성화한 경로 탐색을 수행하는 클래스입니다.

* `AbstractBidirAlgo.java` : AbstractBidirAlgo는 양방향 경로 탐색 알고리즘의 공통 로직을 정의하는 추상 클래스입니다.

* `AbstractBidirCHAlgo.java` : AbstractBidirCHAlgo는 수축 계층(CH)을 사용하는 양방향 경로 탐색 알고리즘의 공통 로직을 정의하는 추상 클래스입니다.

* `AbstractBidirectionEdgeCHNoSOD.java` : AbstractBidirectionEdgeCHNoSOD는 순서 독립적 탐색(SOD)을 비활성화한 수축 계층(CH) 기반 양방향 경로 탐색 알고리즘의 공통 로직을 정의하는 추상 클래스입니다.

* `AbstractNonCHBidirAlgo.java` : AbstractNonCHBidirAlgo는 수축 계층(CH)을 사용하지 않는 양방향 경로 탐색 알고리즘의 공통 로직을 정의하는 추상 클래스입니다.

* `AbstractRoutingAlgorithm.java` : AbstractRoutingAlgorithm는 경로 탐색 알고리즘의 공통 로직과 설정을 정의하는 추상 클래스입니다.

* `AlgorithmOptions.java` : AlgorithmOptions는 경로 탐색 알고리즘 실행을 위한 설정과 옵션(예: 알고리즘 유형, 힌트, 플래그 인코더 등)을 관리하는 클래스입니다.

* `AlternativeRoute.java` : AlternativeRoute는 단일 최단 경로 대신 여러 대안 경로를 탐색하고 계산하는 기능을 제공하는 클래스입니다.

* `AlternativeRouteCH.java` : AlternativeRouteCH는 수축 계층(CH)을 사용하여 효율적으로 대안 경로를 탐색하고 계산하는 클래스입니다.

* AlternativeRouteEdgeCH.java : AlternativeRouteEdgeCH는 수축 계층(CH)을 사용하여 간선 기반의 대안 경로를 탐색하는 클래스입니다.

* `BidirPathExtractor.java` : BidirPathExtractor는 양방향 탐색 알고리즘에서 소스와 대상 노드 간의 경로를 추출하는 클래스입니다.

* `CHPathCalculator.java` : CHPathCalculator는 수축 계층(CH)을 기반으로 최적의 경로를 계산하는 클래스입니다.

* `DefaultBidirPathExtractor.java` : DefaultBidirPathExtractor는 기본 양방향 탐색 경로 추출 로직을 구현하는 클래스입니다.

* `DefaultWeightingFactory.java` : DefaultWeightingFactory는 요청된 설정에 따라 적합한 Weighting(가중치) 인스턴스를 생성하는 기본 구현 클래스입니다.

* `Dijkstra.java` : Dijkstra는 그래프에서 최단 경로를 탐색하기 위해 다익스트라 알고리즘을 구현한 클래스입니다.

* `DijkstraBidirectionCH.java` : DijkstraBidirectionCH는 수축 계층(CH)을 사용하여 다익스트라 알고리즘의 양방향 버전을 구현한 클래스입니다.

* DijkstraBidirectionCHNoSOD.java : DijkstraBidirectionCHNoSOD는 순서 독립적 탐색(SOD)을 비활성화한 수축 계층(CH) 기반 다익스트라 양방향 알고리즘을 구현한 클래스입니다.

* `DijkstraBidirectionEdgeCHNoSOD.java` : DijkstraBidirectionEdgeCHNoSOD는 순서 독립적 탐색(SOD)을 비활성화하고 수축 계층(CH)을 사용하는 간선 기반 다익스트라 양방향 알고리즘을 구현한 클래스입니다.

* `DijkstraBidirectionRef.java` : DijkstraBidirectionRef는 다익스트라 알고리즘의 참조 구현으로, 양방향 탐색을 수행하는 클래스입니다.

* `DijkstraOneToMany.java` : DijkstraOneToMany는 다익스트라 알고리즘을 사용해 단일 출발지에서 여러 목적지까지의 경로를 탐색하는 클래스입니다.

* `DirectionResolver.java` : DirectionResolver는 그래프 탐색 중 특정 방향 제약 조건을 확인하고 해결하는 클래스입니다.

* `DirectionResolverResult.java` : DirectionResolverResult는 방향 제약 조건을 처리한 결과를 저장하는 클래스입니다.

* `EdgeRestrictions.java` : EdgeRestrictions는 경로 탐색 중 특정 간선에서 허용된 방향이나 제한 사항을 관리하는 클래스입니다.

* EdgeToEdgeRoutingAlgorithm.java : EdgeToEdgeRoutingAlgorithm은 그래프에서 간선 간의 최단 경로를 탐색하는 알고리즘을 구현한 클래스입니다.

* `FlexiblePathCalculator.java` : FlexiblePathCalculator는 수축 계층(CH)을 사용하지 않고 유연하게 경로를 계산하는 클래스입니다.

* `HeadingResolver.java` : HeadingResolver는 경로 탐색에서 특정 방향(헤딩) 제약 조건을 처리하고 해결하는 클래스입니다.

* `InstructionsFromEdges.java` : InstructionsFromEdges는 그래프의 간선을 기반으로 경로 지침(방향 안내)을 생성하는 클래스입니다.

* `InstructionsHelper.java` : InstructionsHelper는 경로 지침(방향 안내)을 생성하고 조작하는 데 도움을 주는 유틸리티 클래스입니다.

* `InstructionsOutgoingEdges.java` : InstructionsOutgoingEdges는 경로 안내를 위해 그래프의 출력 간선 정보를 처리하는 클래스입니다.

* `MultiplePointsNotFoundException.java` : MultiplePointsNotFoundException은 경로 탐색 중 여러 지점이 발견되지 않을 때 발생하는 예외를 정의하는 클래스입니다.

* `OSMReaderConfig.java` : OSMReaderConfig는 OSM 데이터를 읽을 때 사용하는 구성 옵션과 설정을 관리하는 클래스입니다.

* `Path.java` : Path는 그래프 탐색에서 계산된 경로의 노드, 간선, 거리, 시간 등의 세부 정보를 저장하고 관리하는 클래스입니다.

* `PathCalculator.java` : PathCalculator는 주어진 그래프에서 경로를 계산하기 위한 인터페이스를 정의하는 클래스입니다.

* `PathExtractor.java` : PathExtractor는 그래프 탐색 결과에서 경로를 추출하고 구성하는 역할을 하는 클래스입니다.

* `RoundTripRouting.java` : RoundTripRouting은 주어진 거리 내에서 시작점으로 돌아오는 순환 경로를 계산하는 클래스입니다.

* `Router.java` : Router는 그래프 데이터를 기반으로 경로 요청을 처리하고 최적 경로를 계산하는 핵심 클래스입니다.

* `RouterConfig.java` : RouterConfig는 경로 계산을 위한 라우터의 설정과 동작 방식을 관리하는 클래스입니다.

* `RoutingAlgorithm.java` : RoutingAlgorithm은 그래프에서 경로를 계산하기 위한 알고리즘의 기본 인터페이스를 정의합니다.

* `RoutingAlgorithmFactory.java` : RoutingAlgorithmFactory는 경로 계산을 위한 다양한 알고리즘 인스턴스를 생성하는 팩토리 인터페이스입니다.

* `RoutingAlgorithmFactorySimple.java` : RoutingAlgorithmFactorySimple은 간단한 설정으로 경로 계산 알고리즘의 인스턴스를 생성하는 기본 구현 클래스입니다.

* `SPTEntry.java` : SPTEntry는 최단 경로 트리(Shortest Path Tree)에서 노드의 상태(거리, 부모 노드 등)를 저장하는 클래스입니다.

* `TestProfiles.java` : TestProfiles는 라우팅 프로필과 관련된 설정 및 동작을 테스트하는 데 사용되는 클래스입니다.

* `ViaRouting.java` : ViaRouting은 경로 계산 중 여러 중간 지점(경유지)을 처리하고 최적 경로를 계산하는 클래스입니다.

* `WeightingFactory.java` : WeightingFactory는 그래프 탐색에서 사용할 가중치(Weighting) 객체를 생성하는 인터페이스입니다.

## core/src/main/java/com/graphhopper/routing/ch

* `AStarCHEntry.java` : AStarCHEntry는 수축 계층(CH)을 사용하는 A* 알고리즘에서 노드의 탐색 상태(비용, 휴리스틱 등)를 저장하는 클래스입니다.

* `BridgePathFinder.java` : BridgePathFinder는 수축 계층(CH)에서 경로 간의 연결을 효율적으로 찾는 역할을 하는 클래스입니다.

* `CHEntry.java` : CHEntry는 수축 계층(CH) 알고리즘에서 노드의 상태(비용, 거리 등)를 저장하고 관리하는 클래스입니다.

* `CHParameters.java` : CHParameters는 수축 계층(CH) 경로 탐색에 필요한 설정과 매개변수를 정의하는 클래스입니다.

* `CHPreparationGraph.java` : CHPreparationGraph는 수축 계층(CH)을 준비하는 동안 사용되는 그래프 데이터 구조를 관리하는 클래스입니다.

* `CHPreparationHandler.java` : CHPreparationHandler는 수축 계층(CH) 준비 과정의 설정과 실행을 관리하는 클래스입니다.

* `CHRoutingAlgorithmFactory.java` : CHRoutingAlgorithmFactory는 수축 계층(CH)을 사용한 경로 탐색 알고리즘 인스턴스를 생성하는 팩토리 클래스입니다.

* `EdgeBasedCHBidirPathExtractor.java` : EdgeBasedCHBidirPathExtractor는 수축 계층(CH)을 사용하여 간선 기반의 양방향 경로를 추출하는 클래스입니다.

* `EdgeBasedNodeContractor.java` : EdgeBasedNodeContractor는 수축 계층(CH) 알고리즘에서 간선 기반의 노드 수축 작업을 수행하는 클래스입니다.

* `EdgeBasedWitnessPathSearcher.java` : EdgeBasedWitnessPathSearcher는 수축 계층(CH) 알고리즘에서 간선 기반의 증인 경로를 검색하여 경로 유효성을 확인하는 클래스입니다.

* `NodeBasedCHBidirPathExtractor.java` : NodeBasedCHBidirPathExtractor는 수축 계층(CH)을 사용하여 노드 기반의 양방향 경로를 추출하는 클래스입니다.

* `NodeBasedNodeContractor.java` : NodeBasedNodeContractor는 수축 계층(CH) 알고리즘에서 노드 기반의 노드 수축 작업을 수행하는 클래스입니다.

* `NodeBasedWitnessPathSearcher.java` : NodeBasedWitnessPathSearcher 클래스는 도로 네트워크에서 특정 노드에 대한 최단 경로를 효율적으로 검색하는 역할을 합니다.

* `NodeContractor.java` : NodeContractor 인터페이스는 노드를 축소하는 과정에서 우선순위를 계산하고, 노드 축소를 통해 필요한 단축 경로를 추가하며, 축소 작업을 완료하는 기능을 제공합니다.

* `NodeOrderingProvider.java` : NodeOrderingProvider 인터페이스는 그래프에서 노드를 특정 순서대로 관리하고, 각 레벨에 해당하는 노드 ID를 반환하는 기능을 제공합니다.

* `OnFlyStatisticsCalculator.java` : 
OnFlyStatisticsCalculator 클래스는 데이터를 실시간으로 수집하여 평균과 분산을 계산하는 데 사용됩니다.

* `PrepareCHEntry.java` : PrepareCHEntry 클래스는 그래프 최단 경로를 준비하는 과정에서 사용되며, 각 경로에 대한 정보를 저장하고 경로의 가중치에 따라 정렬하는 기능을 제공합니다.

* `PrepareContractionHierarchies.java` : PrepareContractionHierarchies 클래스는 도로 네트워크에서 경로 축소를 준비하는 역할을 하며, 경로 최적화를 위한 계층 구조를 생성하는 기능을 제공합니다.

* `PrepareEncoder.java` : PrepareEncoder 클래스는 도로 네트워크에서 단축 경로의 방향 정보를 비트마스크 방식으로 처리하는 기능을 제공합니다.

* `PrepareGraphEdgeExplorer.java` : PrepareGraphEdgeExplorer 인터페이스는 주어진 노드를 기준으로 그래프 엣지 탐색을 설정하는 기능을 제공합니다.

* `PrepareGraphEdgeIterator.java` : PrepareGraphEdgeIterator 인터페이스는 그래프 엣지를 탐색하면서 노드 연결 정보, 경로 가중치, 원본 엣지 키 등을 처리하는 메서드를 제공합니다.

* `PrepareGraphOrigEdgeExplorer.java` : PrepareGraphOrigEdgeExplorer 클래스는 그래프 원본 엣지의 탐색을 제공하며, 경로 준비 과정에서 엣지 정보를 원본 상태로 추적하고 처리하는 기능을 수행합니다.

* `PrepareGraphOrigEdgeIterator.java` : PrepareGraphOrigEdgeIterator 클래스는 그래프에서 원본 엣지를 순차적으로 탐색하여 노드 연결 및 경로 관련 정보를 처리하는 기능을 제공합니다.

* `ShortcutUnpacker.java` : ShortcutUnpacker 클래스는 경로 최적화를 위한 축소된 경로(단축 경로)를 복원하고, 원래의 경로를 순차적으로 탐색하여 각 경로를 처리하는 기능을 제공합니다.

## core/src/main/java/com/graphhopper/routing/ev

* `ArrayEdgeIntAccess.java` : ArrayEdgeIntAccess 클래스는 도로 네트워크의 엣지에 대한 정수 값을 배열 방식으로 관리하고, 각 엣지에 대해 정수 값을 설정하거나 가져오는 기능을 제공합니다.

* `AverageSlope.java` : AverageSlope 클래스는 경로의 평균 경사도를 나타내며, 반대 방향으로 이동할 때는 값을 반전시켜 처리합니다.

* `BikeNetwork.java` : BikeNetwork 클래스는 자전거 네트워크의 도로 정보를 관리하며, 자전거 최적 경로 계산에 필요한 데이터와 메서드를 제공합니다.

* `BikeTemporalAccess.java` : BikeTemporalAccess 클래스는 자전거 경로에 영향을 미치는 조건부 제한을 관리하며, 특정 태그에서 자전거에 대한 시간적 접근 정보를 처리하는 기능을 제공합니다.

* `BooleanEncodedValue.java` : BooleanEncodedValue 인터페이스는 도로 네트워크의 엣지 속성으로 boolean 값을 처리하며, 각 엣지에 대한 값을 설정하고 가져오는 메서드를 제공합니다.

* `BusAccess.java` : BusAccess 클래스는 버스 접근 가능성을 나타내는 키를 정의하고, 해당 값을 처리하는 BooleanEncodedValue를 생성하는 기능을 제공합니다.

* `CarTemporalAccess.java` : CarTemporalAccess 클래스는 GraphHopper 라이브러리에서 TemporalAccess를 상속받아 차량 접근 데이터를 시간에 따라 처리하는 기능을 제공합니다. 이 클래스는 AccessValue.ACCESS와 "car"를 조합하여 차량 접근성을 식별하는 KEY를 정의하며, 이를 통해 차량의 접근 가능 여부를 시간 기반으로 관리하거나 조회할 수 있도록 설계되었습니다.

* `Country.java` : Country 클래스는 GraphHopper에서 도로 네트워크의 각 엣지에 해당하는 국가 정보를 저장하고 관리하기 위해 사용됩니다. 이를 통해 국가별 교통 규칙이나 제한 사항을 라우팅 로직에 반영할 수 있습니다. 

* `Crossing.java` : Crossing 클래스는 GraphHopper에서 도로 네트워크 상의 교차로 유형(예: 신호등, 라운드어바웃 등)을 나타내는 데이터 속성을 정의하고 관리하기 위해 사용됩니다.

* `Curvature.java` : Curvature 클래스는 GraphHopper에서 도로의 곡률 데이터를 저장하고 관리하는 역할을 하며, 도로의 굴곡 정도를 수치화하여 라우팅 알고리즘에 활용할 수 있도록 설계되었습니다.

* `DecimalEncodedValue.java` : DecimalEncodedValue 클래스는 GraphHopper에서 도로 속성 값을 부동소수점(Decimal) 형식으로 저장하고 관리하며, 특정 도로 속성(예: 속도, 경사 등)을 세밀한 값으로 표현할 수 있도록 설계된 인터페이스입니다.

* `DecimalEncodedValueImpl.java` : DecimalEncodedValueImpl 클래스는 `DecimalEncodedValue` 인터페이스의 구현체로, GraphHopper에서 도로 속성 데이터를 부동소수점 형식으로 저장, 읽기, 수정하는 기능을 제공하며, 도로 속성 값의 범위와 정밀도를 설정할 수 있도록 설계되었습니다.

* `DefaultImportRegistry.java` : DefaultImportRegistry 클래스는 GraphHopper에서 기본적으로 사용되는 데이터 등록소로, 다양한 도로 속성이나 확장 데이터를 초기화하고 관리하며, 데이터 인코딩 및 디코딩을 위한 설정을 지원합니다.

* `EdgeIntAccess.java` : EdgeIntAccess 클래스는 GraphHopper에서 도로 네트워크의 엣지(Edge)에 대한 정수형 데이터를 효율적으로 읽고 쓰기 위한 인터페이스로, 도로 속성 값들을 저장하거나 조회할 때 사용됩니다.

* `EncodedValue.java` : EncodedValue 클래스는 GraphHopper에서 도로 속성을 표현하기 위한 인터페이스로, 속성의 이름, 데이터 형식, 범위, 디코딩 방식 등을 정의하며, 라우팅 알고리즘에서 사용되는 다양한 도로 정보를 추상화하여 관리합니다.

* `EncodedValueLookup.java` : EncodedValueLookup 클래스는 GraphHopper에서 다양한 도로 속성(EncodedValue)을 검색, 등록, 및 관리하는 기능을 제공하며, 라우팅 로직에서 속성 값을 효율적으로 사용할 수 있도록 지원합니다.

* `EncodedValueSerializer.java` : EncodedValueSerializer 클래스는 GraphHopper에서 도로 속성(EncodedValue)을 직렬화 및 역직렬화하여 저장하거나 로드할 수 있도록 지원하며, 데이터 전송이나 파일 저장 시 속성 값을 변환하는 역할을 합니다.

* `EnumEncodedValue.java` : EnumEncodedValue 클래스는 GraphHopper에서 도로 속성을 열거형(Enum) 값으로 표현하고 관리하기 위한 인터페이스로, 특정 도로 속성에 대해 미리 정의된 값들 중 하나를 저장하고 처리할 수 있도록 설계되었습니다.

* `ExternalBooleanEncodedValue.java` : ExternalBooleanEncodedValue 클래스는 GraphHopper에서 도로 속성을 외부 소스에서 가져오는 불리언 형식으로 저장하고 관리하며, 특정 조건에 따라 도로 사용 가능 여부를 처리할 수 있도록 설계된 구현체입니다.

* `FerrySpeed.java` :  FerrySpeed.java 클래스는 GraphHopper에서 페리(배)의 속도를 관리하기 위한 도로 속성으로, 페리 경로의 평균 속도를 설정하거나 조회하는 기능을 제공합니다. 이는 라우팅 알고리즘에서 페리 경로의 소요 시간을 계산하는 데 사용됩니다.

* `FootNetwork.java` :  FootNetwork.java 클래스는 GraphHopper에서 보행자 경로 네트워크를 관리하기 위한 속성을 정의하며, 보행자 전용 경로나 도로와 관련된 네트워크 정보를 일관성 있게 처리하는 데 사용됩니다.

* `FootTemporalAccess.java` :  FootTemporalAccess.java 클래스는 GraphHopper에서 보행자의 시간 기반 접근성을 처리하기 위해 설계된 클래스입니다. 이는 특정 시간 조건에 따라 보행자가 특정 경로를 사용할 수 있는지를 결정하는 데 사용됩니다.

* `Footway.java` :  Footway.java 클래스는 GraphHopper에서 도로의 'footway' 속성을 관리하기 위해 사용되며, 이는 보행자 전용 도로를 나타내는 데 활용됩니다. 이 클래스는 EnumEncodedValue를 통해 'footway'의 다양한 유형을 정의하고 처리합니다.  

* `GetOffBike.java` :  GetOffBike.java 클래스는 GraphHopper에서 자전거 이용자가 특정 도로 구간에서 자전거에서 내려 걸어야 하는 상황을 처리하기 위한 도로 속성을 정의합니다. 이 클래스는 자전거 경로 프로필에서 일방통행 도로를 역방향으로 이동하거나 자전거 통행이 금지된 구간에서 보행 모드로 전환하여 경로를 계산할 때 사용됩니다.  

* `Hazmat.java` :  Hazmat.java 클래스는 GraphHopper에서 위험물 운송에 대한 일반적인 제한 사항을 정의하는 열거형(Enum)으로, 도로 네트워크에서 위험물 운송이 허용되는지 여부를 나타냅니다. 이 클래스는 'YES'와 'NO' 두 가지 값을 가지며, 기본 키(KEY)는 "hazmat"으로 설정되어 있습니다. 또한, EnumEncodedValue<Hazmat>를 생성하는 create() 메서드와 문자열 표현을 소문자로 반환하는 toString() 메서드를 포함하고 있습니다.  

* `HazmatTunnel.java` :  HazmatTunnel.java 클래스는 GraphHopper에서 터널을 통과하는 위험물 운송에 대한 제한 등급을 정의하는 열거형(Enum)입니다. 각 등급은 위험물의 종류와 터널 통과 허용 여부를 나타내며, 이는 라우팅 시 이러한 제한을 고려하여 경로를 계산하는 데 사용됩니다.  

* `HazmatWater.java` :  HazmatWater.java 클래스는 GraphHopper에서 수질 보호 구역을 통과하는 위험물 운송 제한을 관리하며, 관련된 제한 수준을 정의합니다.

* `Hgv.java` :  Hgv.java 클래스는 GraphHopper에서 대형 화물 차량(Heavy Goods Vehicle)의 접근성을 관리하기 위한 열거형(Enum)으로, 도로 네트워크에서 HGV의 통행 가능 여부를 나타냅니다. 이 클래스는 'YES'와 'NO' 두 가지 값을 가지며, 기본 키(KEY)는 "hgv"로 설정되어 있습니다. 또한, EnumEncodedValue<Hgv>를 생성하는 create() 메서드와 문자열 표현을 소문자로 반환하는 toString() 메서드를 포함하고 있습니다.  

* `HikeRating.java` :  HikeRating.java 클래스는 GraphHopper에서 하이킹 경로의 난이도를 나타내는 속성을 정의하며, 이는 라우팅 시 하이킹 경로의 선택에 영향을 줍니다.  

* `HorseRating.java` :  HorseRating.java 클래스는 GraphHopper에서 승마 경로의 난이도를 나타내는 속성을 정의하며, 이는 라우팅 시 승마 경로의 선택에 영향을 줍니다.  

* `HovAccess.java` :  HovAccess.java 클래스는 GraphHopper에서 고속도로 전용차로(HOV, High-Occupancy Vehicle)의 접근성을 관리하기 위한 속성을 정의하며, 이는 라우팅 시 카풀이나 다인승 차량의 경로 선택에 영향을 줍니다.  

* `ImportRegistry.java` :  ImportRegistry.java 인터페이스는 GraphHopper에서 데이터 가져오기 작업을 관리하기 위한 메서드를 정의하며, 주로 createImportUnit(String name) 메서드를 통해 새로운 ImportUnit을 생성하는 데 사용됩니다.  

* `ImportUnit.java` : ImportUnit.java 클래스는 GraphHopper에서 데이터 가져오기 작업을 구성하고 관리하기 위한 단위를 정의하며, 도로 속성(EncodedValue)과 태그 파서(TagParser)를 포함하여 데이터 처리 파이프라인을 설정하는 데 사용됩니다.  

* `ImportUnitSorter.java` :  ImportUnitSorter.java 클래스는 GraphHopper에서 ImportUnit 객체들을 정렬하여 데이터 가져오기 작업의 순서를 결정하는 데 사용됩니다. 이는 데이터 처리 파이프라인의 효율성을 높이기 위해 ImportUnit 간의 의존성을 고려하여 올바른 순서로 정렬하는 기능을 제공합니다.  

* `IntEncodedValue.java` :  IntEncodedValue.java 인터페이스는 GraphHopper에서 도로 속성 값을 부호 없는 정수 형태로 저장하고 관리하기 위한 메서드를 정의하며, 특정 비트 수 내에서 값을 저장하고 복원하는 기능을 제공합니다.  

* `IntEncodedValueImpl.java` :  IntEncodedValueImpl.java 클래스는 GraphHopper에서 정수형 도로 속성 값을 관리하기 위한 IntEncodedValue 인터페이스의 구현체로, 특정 비트 수를 사용하여 도로 속성 값을 효율적으로 저장하고 검색하는 기능을 제공합니다.  

* `IntsRefEdgeIntAccess.java` :  IntsRefEdgeIntAccess.java 클래스는 GraphHopper에서 IntsRef 객체를 통해 도로 엣지의 정수 데이터를 효율적으로 읽고 쓰기 위한 기능을 제공합니다. 이를 통해 도로 네트워크의 엣지에 대한 다양한 속성 값을 관리할 수 있습니다.  

* `Lanes.java` :  Lanes.java 클래스는 GraphHopper에서 도로의 차선 수를 나타내는 속성을 정의하며, 라우팅 시 도로의 차선 정보를 고려하여 경로를 계산하는 데 사용됩니다.  

* `MaxAxleLoad.java` :  MaxAxleLoad.java 클래스는 GraphHopper에서 도로의 최대 축하중 제한을 나타내는 속성을 정의하며, 이는 특정 도로 구간에서 차량의 축하중이 허용 범위를 초과하지 않도록 라우팅 시 고려됩니다.  

* `MaxHeight.java` :  MaxHeight.java 클래스는 GraphHopper에서 도로의 최대 높이 제한을 나타내는 속성을 정의하며, 이는 차량의 높이가 도로의 제한 높이를 초과하지 않도록 라우팅 시 고려됩니다.  

* `MaxLength.java` :  MaxLength.java 클래스는 GraphHopper에서 도로의 최대 허용 길이 제한을 나타내는 속성을 정의하며, 이는 차량의 길이가 도로의 제한 길이를 초과하지 않도록 라우팅 시 고려됩니다.  

* `MaxSlope.java` :  MaxSlope.java 클래스는 GraphHopper에서 도로의 최대 경사도를 나타내는 속성을 정의하며, 이는 라우팅 시 경사도 제한을 고려하여 경로를 계산하는 데 사용됩니다.  

* `MaxSpeed.java` :  MaxSpeed.java 클래스는 GraphHopper에서 도로의 최대 속도 제한을 나타내는 속성을 정의하며, 이는 라우팅 시 도로의 제한 속도를 고려하여 경로를 계산하는 데 사용됩니다.  

* `MaxSpeedEstimated.java` :  MaxSpeedEstimated.java 클래스는 GraphHopper에서 도로의 최대 속도가 추정된 것인지 여부를 나타내는 불리언 속성을 정의하며, 이는 라우팅 시 실제 제한 속도가 명시되지 않은 도로 구간에서 사용됩니다.  

* `MaxWeight.java` :  MaxWeight.java 클래스는 GraphHopper에서 도로의 최대 허용 중량을 나타내는 속성을 정의하며, 이는 차량의 총 중량이 도로의 제한 중량을 초과하지 않도록 라우팅 시 고려됩니다.  

* `MaxWeightExcept.java` :  MaxWeightExcept.java 클래스는 GraphHopper에서 도로의 최대 중량 제한이 특정 차량 유형에 대해 예외적으로 적용되지 않는 경우를 나타내는 열거형(Enum)을 정의합니다. 예를 들어, 'DELIVERY'는 배송 차량이 최대 중량 제한을 초과하더라도 해당 도로를 이용할 수 있음을 의미합니다.  

* `MaxWidth.java` :  MaxWidth.java 클래스는 GraphHopper에서 도로의 최대 허용 너비를 나타내는 속성을 정의하며, 이는 차량의 너비가 도로의 제한 너비를 초과하지 않도록 라우팅 시 고려됩니다.  

* `MtbNetwork.java` :  MtbNetwork.java 클래스는 GraphHopper에서 산악 자전거 경로 네트워크를 나타내는 속성을 정의하며, 이는 라우팅 시 산악 자전거 경로의 선택에 영향을 줍니다.  

* `MtbRating.java` :  MtbRating.java 클래스는 GraphHopper에서 산악 자전거 경로의 난이도를 나타내는 속성을 정의하며, 이는 라우팅 시 경로 선택에 영향을 줍니다.  

* `OSMWayID.java` :  OSMWayID.java 클래스는 GraphHopper에서 OSM(OpenStreetMap) 도로의 고유 식별자(OSM Way ID)를 저장하고 관리하기 위한 속성을 정의하며, 이를 통해 OSM 데이터와의 매핑 및 참조를 용이하게 합니다.  

* `Orientation.java` :  Orientation.java 클래스는 GraphHopper에서 도로의 방향 정보를 나타내는 속성을 정의하며, 이는 라우팅 시 도로의 방향성을 고려하여 경로를 계산하는 데 사용됩니다.  

* `RoadAccess.java` :  RoadAccess.java 클래스는 GraphHopper에서 도로 접근 제한 정보를 관리하기 위한 열거형(Enum)을 정의하며, 도로의 접근 가능 여부를 나타내는 속성을 포함합니다. 이는 라우팅 시 특정 도로 구간의 접근 제한을 고려하여 경로를 계산하는 데 사용됩니다.  

* `RoadClass.java` :  RoadClass.java 클래스는 GraphHopper에서 도로의 분류를 나타내는 열거형(Enum)을 정의하며, 이는 라우팅 시 도로의 종류를 고려하여 경로를 계산하는 데 사용됩니다.  

* `RoadClassLink.java` :  RoadClassLink.java 클래스는 GraphHopper에서 도로의 연결 유형을 나타내는 속성을 정의하며, 이는 라우팅 시 도로 간의 연결 관계를 고려하여 경로를 계산하는 데 사용됩니다.  

* `RoadEnvironment.java` :  RoadEnvironment.java 클래스는 GraphHopper에서 도로의 환경적 특성을 나타내는 속성을 정의하며, 이는 라우팅 시 도로의 주변 환경을 고려하여 경로를 계산하는 데 사용됩니다.  

* `Roundabout.java` :  Roundabout.java 클래스는 GraphHopper에서 도로의 회전 교차로 여부를 나타내는 불리언 속성을 정의하며, 이는 라우팅 시 회전 교차로를 고려하여 경로를 계산하는 데 사용됩니다.  

* `RouteNetwork.java` :  RouteNetwork.java 클래스는 GraphHopper에서 도로 네트워크의 유형을 나타내는 속성을 정의하며, 이는 라우팅 시 도로 네트워크의 종류를 고려하여 경로를 계산하는 데 사용됩니다.  

* `SimpleBooleanEncodedValue.java` :  SimpleBooleanEncodedValue.java 클래스는 GraphHopper에서 도로 속성의 부울 값을 관리하기 위한 클래스로, 특정 도로 속성의 참(True) 또는 거짓(False) 상태를 저장하고 처리하는 데 사용됩니다.  

* `Smoothness.java` :  Smoothness.java 클래스는 GraphHopper에서 도로의 평탄도를 나타내는 속성을 정의하며, 이는 라우팅 시 도로의 상태를 고려하여 경로를 계산하는 데 사용됩니다.  

* `State.java` :  State.java 클래스는 GraphHopper에서 도로의 행정 구역 정보를 관리하기 위한 열거형(Enum)을 정의하며, 각 값은 특정 국가의 주나 도와 같은 행정 구역을 나타냅니다. 이는 라우팅 시 도로의 위치 정보를 고려하여 경로를 계산하는 데 사용됩니다.  

* `StringEncodedValue.java` :  StringEncodedValue 클래스는 GraphHopper에서 문자열 형태의 도로 속성 값을 관리하기 위한 클래스로, 최대 `maxValues` 개수만큼의 문자열을 저장하고, 각 문자열에 대해 인덱스 값을 부여하여 효율적으로 처리합니다.

* `Subnetwork.java` :  Subnetwork.java 클래스는 GraphHopper에서 도로 네트워크 내의 작은 연결 구성 요소를 식별하고 관리하기 위한 속성을 정의하며, 이는 라우팅 시 고립된 도로 구간을 피하는 데 사용됩니다.  

* `Surface.java` :  Surface.java 클래스는 GraphHopper에서 도로의 노면 상태를 나타내는 열거형(Enum)을 정의하며, 이는 라우팅 시 도로의 포장 상태를 고려하여 경로를 계산하는 데 사용됩니다.  

* `Toll.java` :  Toll.java 클래스는 GraphHopper에서 도로의 통행료 부과 여부를 나타내는 열거형(Enum)을 정의하며, 이는 라우팅 시 유료 도로를 피하거나 선호하는 등의 경로 계산에 사용됩니다.  

* `TrackType.java` :  TrackType.java 클래스는 GraphHopper에서 도로의 트랙 유형을 나타내는 열거형(Enum)을 정의하며, 이는 라우팅 시 도로의 품질과 상태를 고려하여 경로를 계산하는 데 사용됩니다.  

* `TurnCost.java` :  TurnCost.java 클래스는 GraphHopper에서 특정 차량 프로필에 대한 회전 비용을 관리하기 위한 기능을 제공하며, 이를 통해 교차로에서의 회전 시 발생하는 시간 또는 비용을 고려하여 경로를 계산할 수 있습니다.  

* `TurnRestriction.java` :  TurnRestriction.java 클래스는 GraphHopper에서 특정 도로 구간에서의 회전 제한 정보를 관리하기 위한 속성을 정의하며, 이는 라우팅 시 금지된 회전을 피하도록 경로를 계산하는 데 사용됩니다.  

* `UrbanDensity.java` :  UrbanDensity.java 클래스는 GraphHopper에서 도로의 도시 밀집도를 나타내는 속성을 정의하며, 이는 라우팅 시 도로가 농촌, 주거지 또는 도시 지역에 속하는지를 고려하여 경로를 계산하는 데 사용됩니다.  

* `VehicleAccess.java` :  VehicleAccess.java 클래스는 GraphHopper에서 차량의 접근 권한을 정의하는 데 사용됩니다. `key()` 메서드는 주어진 이름을 기반으로 접근 키를 생성하고, `create()` 메서드는 해당 키를 사용해 접근 권한을 나타내는 `BooleanEncodedValue` 인스턴스를 생성합니다.

* `VehiclePriority.java` :  VehiclePriority.java 클래스는 차량의 우선순위를 정의하는 메서드를 제공합니다. `key()` 메서드는 우선순위 키를 생성하고, `create()` 메서드는 속도 비트, 속도 계수 및 양방향 저장 여부를 설정하여 `DecimalEncodedValue` 인스턴스를 생성합니다. 이 클래스는 차량 우선순위 정보를 라우팅에 활용합니다.

* `VehicleSpeed.java` :  VehicleSpeed.java 클래스는 차량의 평균 속도를 정의하는 메서드를 제공합니다. `key()` 메서드는 차량 속도 정보를 나타내는 키를 생성하고, `create()` 메서드는 속도 비트, 속도 계수, 양방향 저장 여부를 설정하여 `DecimalEncodedValue` 인스턴스를 생성합니다. 이 클래스는 차량 속도 정보를 라우팅에 활용합니다.

## core/src/main/java/com/graphhopper/routing/lm

* `LMApproximator.java` : LMApproximator.java 클래스는 라우팅 성능을 최적화하기 위해 선형 모델을 사용하여 주어진 지점들 사이의 근사 경로 길이를 계산합니다. 이 클래스는 복잡한 경로 계산을 단순화하여 속도와 메모리 사용을 최적화하는 데 도움을 줍니다.

* `LMConfig.java` : LMConfig.java 클래스는 GraphHopper에서 라우팅 프로파일의 설정을 관리합니다. 이 클래스는 프로파일 이름과 가중치 정보를 저장하며, 이를 통해 라우팅 프로파일을 설정하고, 프로파일 이름을 검증하고, 객체 간 비교를 위한 `equals()`와 `hashCode()` 메서드를 제공합니다.

* `LMPreparationHandler.java` : LMPreparationHandler.java 클래스는 GraphHopper에서 라우팅 성능을 향상시키기 위한 선형 모델 준비 작업을 처리합니다. 이 클래스는 라우팅을 위한 미리 계산된 데이터를 준비하고, 필요한 인덱스와 프로파일 설정을 관리하며, 라우팅을 더 효율적으로 수행할 수 있도록 최적화된 데이터를 제공합니다.

* `LMRoutingAlgorithmFactory.java` : LMRoutingAlgorithmFactory.java 클래스는 Landmark-based 라우팅 알고리즘을 생성하는 팩토리 클래스입니다. 주어진 그래프와 가중치, 옵션에 따라 AStar 또는 AStarBidirection 알고리즘을 생성하며, 기본적으로 지정된 수의 활성 랜드마크를 사용하여 경로 계산을 최적화합니다. 이 클래스는 경로 계산 성능을 향상시키기 위해 랜드마크를 활용합니다.

* `LandmarkStorage.java` : LandmarkStorage.java 클래스는 Landmark-based 라우팅에서 사용하는 랜드마크 데이터를 저장하고 관리합니다. 이 클래스는 경로 계산 성능을 향상시키기 위해 미리 계산된 랜드마크 정보를 저장하며, 이를 효율적으로 조회하고 업데이트하는 기능을 제공합니다.

* `LandmarkSuggestion.java` : LandmarkSuggestion.java 클래스는 외부 소스에서 랜드마크를 수집하여 자동 랜드마크 찾기 과정을 대체합니다. 이 클래스는 주어진 파일에서 경유지 정보를 읽어와 해당 지점에 가장 가까운 노드를 찾아 랜드마크 후보로 추가합니다. 이 과정에서 경계 상자(BBox)도 계산하여, 랜드마크의 지리적 범위를 정의합니다.

* `PerfectApproximator.java` : PerfectApproximator.java 클래스는 WeightApproximator 인터페이스를 구현하며, 주어진 그래프와 가중치를 기반으로 경로의 길이를 정확하게 계산합니다. Dijkstra 알고리즘을 사용하여 주어진 출발지와 도착지 간의 경로를 계산하고, 그 가중치를 반환합니다. 이 클래스는 경로 근사 계산이 아닌 정확한 값을 제공하는 "완벽한" 근사기를 제공합니다.

* `PrepareLandmarks.java` : PrepareLandmarks.java 클래스는 ALT 알고리즘을 위한 전처리 작업을 수행합니다. 이 클래스는 그래프와 관련된 랜드마크 데이터를 준비하고, A* 알고리즘, 랜드마크 기반 접근법, 삼각 부등식을 사용하여 경로 계산을 최적화합니다. 랜드마크 후보를 설정하고, 랜드마크 스토리지와 데이터를 초기화하여 효율적인 라우팅을 지원합니다.

* `SplitArea.java` : SplitArea.java 클래스는 GeoJSON 형식의 다각형 데이터를 사용하여 특정 영역을 정의하는 데 사용됩니다. 이 클래스는 주어진 JSON 형식의 데이터를 `Polygon` 객체로 변환하여 영역의 경계를 정의하며, `getBorders()` 메서드를 통해 이러한 경계들을 반환합니다. 이를 통해 지역을 나누는 작업을 처리하고, 해당 지역에 대한 정보를 제공합니다.

## core/src/main/java/com/graphhopper/routing/querygraph

* `EdgeChangeBuilder.java` : EdgeChangeBuilder.java 클래스는 GraphHopper의 쿼리 그래프에서 엣지(Edge) 변경 사항을 처리하는 빌더 클래스입니다. 이 클래스는 엣지의 속성, 가중치 및 연결 상태를 업데이트하고 관리하는 기능을 제공하며, 라우팅 경로 계산 시 동적인 엣지 상태 변경을 반영할 수 있게 합니다.

* `QueryGraph.java` : QueryGraph.java 클래스는 GraphHopper에서 쿼리 그래프 구조를 관리하는 클래스입니다. 이 클래스는 경로 계산을 위해 노드와 엣지를 동적으로 구성하고 업데이트하며, 쿼리 요청 시 효율적인 경로 계산을 지원합니다. 또한, 쿼리된 그래프에 대해 최적화된 경로를 찾기 위해 여러 가지 연산을 수행합니다.

* `QueryOverlay.java` : QueryOverlay.java 클래스는 쿼리 그래프에서 그래프 노드와 엣지의 시각적 오버레이를 생성하는 기능을 제공합니다. 이 클래스는 주어진 경로에 대한 노드 및 엣지의 상태를 표시하고, 경로 계산 시 경로와 관련된 정보를 사용자에게 시각적으로 제공하여 더 나은 분석을 지원합니다.

* `QueryOverlayBuilder.java` : QueryOverlayBuilder.java 클래스는 QueryOverlay 객체를 생성하는 빌더 클래스입니다. 이 클래스는 쿼리 그래프에서 경로를 시각적으로 표현하기 위한 노드와 엣지의 오버레이를 설정하고, 사용자가 경로 계산 시 필요한 정보를 시각적으로 제공할 수 있도록 돕습니다. `QueryOverlay`와 관련된 다양한 설정을 구성하여 경로 데이터를 시각화하는 데 사용됩니다.

* `QueryRoutingCHGraph.java` : QueryRoutingCHGraph.java 클래스는 GraphHopper의 쿼리 라우팅에서 CH(Contraction Hierarchies) 그래프를 처리하는 클래스입니다. 이 클래스는 CH 알고리즘을 사용하여 더 빠른 라우팅을 지원하며, 경로 계산에 필요한 그래프의 데이터를 효율적으로 관리하고, 특정 쿼리에 대한 라우팅을 최적화합니다. CH 그래프의 속성 및 상태를 관리하고, 최단 경로를 더 빠르게 찾을 수 있도록 돕습니다.

* `VirtualEdgeIterator.java` : VirtualEdgeIterator.java 클래스는 가상 엣지들을 순차적으로 처리하는 반복기(Iterator) 클래스입니다. 이 클래스는 쿼리 그래프에서 실제 그래프와 연결되지 않은 가상 엣지를 다루며, 이러한 엣지들을 사용하여 경로 계산에 필요한 추가적인 정보를 제공합니다.

* `VirtualEdgeIteratorState.java` : VirtualEdgeIteratorState.java 클래스는 가상 엣지의 상태를 나타내는 객체로, 쿼리 그래프에서 가상 엣지를 순차적으로 처리하고 관리합니다. 이 클래스는 각 엣지의 속성, 상태 및 연결 정보를 추적하며, 라우팅에 필요한 추가적인 가상 엣지 데이터를 제공합니다.

## core/src/main/java/com/graphhopper/routing/subnetwork

* `EdgeBasedTarjanSCC.java` : EdgeBasedTarjanSCC.java 클래스는 GraphHopper에서 엣지를 기반으로 강한 연결 요소(SCC, Strongly Connected Components)를 찾는 알고리즘을 구현한 클래스입니다. Tarjan 알고리즘을 사용하여 그래프에서 강하게 연결된 부분들을 식별하고, 경로 계산 시 해당 정보로 최적화를 수행할 수 있도록 돕습니다.

* `PrepareRoutingSubnetworks.java` : PrepareRoutingSubnetworks.java 클래스는 GraphHopper에서 라우팅 서브네트워크를 준비하는 클래스입니다. 이 클래스는 네트워크를 분할하여 경로 계산을 최적화하고, 서브네트워크의 노드와 엣지 정보를 관리합니다. 주어진 그래프에서 서브네트워크를 추출하고, 이를 기반으로 더 빠르고 효율적인 라우팅을 수행할 수 있도록 돕습니다.

* `SubnetworkStorage.java` : SubnetworkStorage.java 클래스는 라우팅 서브네트워크 정보를 저장하고 관리하는 클래스입니다. 이 클래스는 서브네트워크의 데이터를 효율적으로 저장하고, 각 서브네트워크에 대한 경로 계산을 지원합니다. 서브네트워크의 노드와 엣지 정보를 관리하여 라우팅 성능을 최적화합니다.

* `TarjanSCC.java` : TarjanSCC.java 클래스는 GraphHopper에서 Tarjan 알고리즘을 사용하여 강한 연결 요소(SCC)를 찾는 클래스입니다. 이 클래스는 그래프에서 강하게 연결된 부분들을 식별하고, 이를 기반으로 라우팅 성능을 최적화하는 데 사용됩니다. 그래프 내에서 순환이나 연결된 하위 그래프를 효율적으로 분석하여 경로 계산에 도움을 줍니다.

## core/src/main/java/com/graphhopper/routing/util

* `AccessFilter.java` :  AccessFilter.java 클래스는 그래프에서 엣지의 접근 권한을 필터링하여, 특정 방향으로만 접근 가능한 엣지를 관리하는 기능을 제공합니다.

* `AllEdgesIterator.java` :  AllEdgesIterator.java 클래스는 그래프의 모든 엣지를 순차적으로 탐색할 수 있는 반복자 클래스입니다. 이 클래스는 엣지의 시작과 끝을 기준으로 모든 엣지를 순차적으로 반환하며, 주어진 그래프에서 각 엣지를 탐색하는 기능을 제공합니다.

* `AreaIndex.java` :  AreaIndex.java 클래스는 주어진 지역들을 인덱싱하고, 특정 좌표에 대해 해당 지역을 빠르게 조회하는 기능을 제공합니다. 이 클래스는 JTS(Geometry) 라이브러리를 사용하여 다각형 경계를 처리하며, `STRtree`를 사용해 효율적인 공간 검색을 수행합니다.

* `CurvatureCalculator.java` :  CurvatureCalculator.java 클래스는 도로의 곡률을 계산하는 클래스입니다. `handleWayTags` 메서드는 도로의 시작점과 끝점 간 거리를 계산하여, 도로의 곡률을 `DecimalEncodedValue`로 설정합니다. 곡률은 직선 거리와 실제 도로 거리를 비교하여 계산되며, 이를 통해 도로의 곡선 정도를 나타냅니다.

* `CustomArea.java` :  CustomArea.java 클래스는 특정 지역을 정의하고 관리하는 클래스입니다. 이 클래스는 지역 내에서 겹치는 나라를 처리하며, 더 작은 지역을 선택하는 방식으로 겹침 문제를 해결합니다. 추가적으로, 경고 메시지를 최소화하는 기능을 제공합니다.

* `DefaultSnapFilter.java` :  DefaultSnapFilter.java 클래스는 그래프 엣지의 스냅 필터를 구현합니다. 이 클래스는 엣지의 가중치를 계산하고, `inSubnetworkEnc` 값을 사용하여 서브네트워크에 포함되지 않은 엣지와 유효한 가중치를 가진 엣지만 허용하는 기능을 제공합니다.

* `DirectedEdgeFilter.java` :  DirectedEdgeFilter.java 클래스는 주어진 엣지에 대해 필터링 조건을 적용하는 인터페이스입니다. `accept` 메서드는 엣지의 상태와 방향을 확인하여 해당 엣지가 조건에 맞는지 여부를 반환합니다. 이를 통해 라우팅 중 특정 방향으로만 접근 가능한 엣지를 필터링하는 기능을 제공합니다.

* `EdgeFilter.java` :  EdgeFilter.java 클래스는 그래프의 엣지를 필터링하는 인터페이스로, 각 엣지가 처리되어야 할지 여부를 결정하는 `accept` 메서드를 제공합니다. `ALL_EDGES`는 모든 엣지를 처리 대상으로 설정하는 기본 구현을 제공합니다.

* `EncodingManager.java` :  EncodingManager.java 클래스는 그래프의 인코딩된 값을 관리하는 기능을 제공합니다. 이 클래스는 다양한 경로 계산에 필요한 인코딩 값을 등록하고, 이를 효율적으로 저장하고 조회할 수 있는 기능을 제공합니다. 또한, `EncodedValue`의 설정을 관리하며, 그래프와 관련된 여러 속성을 처리합니다.

* `FerrySpeedCalculator.java` :  FerrySpeedCalculator.java 클래스는 도로 환경과 속도 파서를 위해 페리 속도를 정의하는 클래스입니다. 이 클래스는 페리의 속도를 계산하고, 도로 네트워크의 다른 속도와 일관되게 설정할 수 있도록 지원합니다.

* `FiniteWeightFilter.java` :  FiniteWeightFilter.java 클래스는 가중치가 유한한 엣지만 허용하는 엣지 필터입니다. 이 클래스는 `Weighting`을 사용하여 엣지의 가중치를 계산하고, `calcEdgeWeight` 메서드를 통해 가중치가 유한한 경우에만 엣지를 수락합니다.

* `HeadingEdgeFilter.java` :  HeadingEdgeFilter.java 클래스는 주어진 경로에서 특정 방향(heading)에 맞는 엣지를 필터링하는 클래스입니다. 이 클래스는 `DirectedEdgeFilter`와 함께 작동하여, 엣지가 주어진 헤딩 범위 내에 있는지 확인하고, 허용된 방향으로만 엣지를 선택하는 역할을 합니다.

* `MaxSpeedCalculator.java` :  MaxSpeedCalculator.java 클래스는 도로의 최대 속도를 계산하는 클래스입니다. 이 클래스는 도로 타입에 따른 기본 속도를 설정하고, `DefaultMaxSpeedParser`를 사용하여 속도 정보를 파싱합니다. 또한, `LegalDefaultSpeeds` 라이브러리를 사용하여 도로 유형별로 속도를 계산하며, 이를 그래프의 엣지에 저장하여 라우팅 시 사용됩니다.

* `NameSimilarityEdgeFilter.java` :  NameSimilarityEdgeFilter.java 클래스는 그래프의 엣지 이름 유사도를 기반으로 필터링을 수행하는 클래스입니다. 이 클래스는 Jaro-Winkler 알고리즘을 사용하여 엣지 이름의 유사도를 계산하고, 주어진 조건에 맞는 엣지를 선택하는 역할을 합니다. 이를 통해 유사한 이름을 가진 엣지를 찾고, 라우팅에서의 정확도를 향상시킵니다.

* `OSMParsers.java` :  OSMParsers.java 클래스는 OpenStreetMap(OSM) 데이터를 파싱하는 데 사용되는 다양한 태그 파서들을 관리합니다. 이 클래스는 도로 유형, 관계 태그, 제한 태그 등을 처리하는 여러 파서(`TagParser`, `RelationTagParser`, `RestrictionTagParser`)를 포함하고 있으며, 사용자가 무시할 고속도로를 추가하거나 태그 파서를 추가하는 기능도 제공합니다. 이를 통해 OSM 데이터를 그래프에 맞게 변환하여 라우팅에 사용할 수 있습니다.

* `PriorityCode.java` :  PriorityCode.java 클래스는 엣지의 우선순위 값을 정의하는 열거형(Enum)입니다. 이 클래스는 도로의 우선순위를 `EXCLUDE`, `REACH_DESTINATION`, `VERY_BAD` 등으로 설정하고, 각 우선순위에 대한 값을 `TreeMap`에 저장하여 빠른 검색을 제공합니다. 또한, 우선순위 간의 비교를 위한 메서드(`worse()`, `better()`)와 값에 해당하는 배율을 계산하는 `getFactor()` 메서드도 포함되어 있습니다.

* `RoadDensityCalculator.java` :  RoadDensityCalculator.java 클래스는 그래프의 도로 밀도를 계산하는 클래스입니다. 주어진 그래프의 모든 엣지를 순회하고, 각 엣지에서 도로 밀도를 계산하여 이를 처리하는 핸들러 함수에 전달합니다. 또한, 멀티스레딩을 사용하여 계산 성능을 최적화하며, 도로 밀도 계산을 병렬로 수행할 수 있도록 지원합니다.

* `SlopeCalculator.java` :  SlopeCalculator.java 클래스는 도로의 경사도를 계산하는 클래스입니다. 이 클래스는 도로의 3D 고도 데이터를 기반으로 최대 경사도와 평균 경사도를 계산하여, 이를 엣지 속성으로 저장합니다. 경사도 계산은 도로의 길이가 짧을 경우 정확도가 떨어질 수 있으며, `handleWayTags` 메서드를 통해 도로의 태그에서 고도 정보를 추출하여 경사도를 계산합니다.

* `SnapPreventionEdgeFilter.java` :  SnapPreventionEdgeFilter.java 클래스는 특정 도로 유형을 피하도록 필터링하는 기능을 제공합니다. 사용자가 설정한 도로 환경(예: 고속도로, 터널, 다리, 페리 등)을 피하도록 설정할 수 있으며, 이 필터는 해당 도로 환경을 포함한 엣지를 제외하고 경로 계산을 진행합니다.

* `TransportationMode.java` :  TransportationMode.java 클래스는 다양한 교통 수단을 정의하는 열거형(Enum)입니다. 각 교통 수단은 `motorVehicle` 값에 따라 자동차, 오토바이, 버스 등 모터 차량 여부를 구별합니다. 이를 통해 OpenStreetMap 데이터에서 교통 수단에 따른 경로 인코딩 값을 생성할 수 있습니다.

* `TraversalMode.java` :  TraversalMode.java 클래스는 노드 기반 또는 엣지 기반 탐색 방식을 정의하는 열거형(Enum)입니다. `NODE_BASED`와 `EDGE_BASED` 두 가지 탐색 방식이 있으며, 각각의 방식에 따라 경로 계산 시 사용되는 ID를 다르게 생성합니다. `createTraversalId` 메서드는 현재 엣지 상태와 방향을 기반으로 적합한 탐색 ID를 생성하는 역할을 합니다.

* `UrbanDensityCalculator.java` :  UrbanDensityCalculator.java 클래스는 그래프의 엣지에 대해 도시 밀도(농촌/주거지/도시)를 계산하는 클래스입니다. 첫 번째 단계에서는 도로 밀도를 계산하여 주거지 지역을 식별하고, 두 번째 단계에서는 고밀도 주거지를 도시로 분류합니다. 이 클래스는 주어진 반경과 민감도 값에 따라 주거지 및 도시 지역을 계산하며, 멀티스레딩을 활용하여 효율적으로 처리합니다.

* `WayAccess.java` :  WayAccess.java 클래스는 도로의 접근 유형을 정의하는 열거형(Enum)입니다. `WAY`, `FERRY`, `OTHER`, `CAN_SKIP` 네 가지 접근 유형을 제공하며, 각 유형에 따라 도로를 다르게 처리할 수 있습니다. 또한, 각 접근 유형에 대한 확인 메서드(`isFerry()`, `isWay()`, `isOther()`, `canSkip()`)를 통해 해당 도로의 유형을 쉽게 판별할 수 있습니다.

## core/src/main/java/com/graphhopper/routing/util/countryrules

* `CountryRule.java` : CountryRule.java 클래스는 특정 국가에 대한 도로 규칙을 정의하는 클래스입니다. 이 클래스는 국가별 도로 제한, 속도 제한 및 기타 규제를 처리하며, 이러한 규칙을 라우팅 경로 계산에 반영하는 기능을 제공합니다. 이를 통해 각 국가의 교통 규정을 고려하여 더 정확한 경로를 계산할 수 있도록 지원합니다.

* `CountryRuleFactory.java` : CountryRuleFactory.java 클래스는 특정 국가의 도로 규칙을 생성하는 팩토리 클래스입니다. 이 클래스는 국가별 규칙을 정의하는 `CountryRule` 객체를 생성하고, 이를 관리하여 라우팅 중 각 국가의 교통 규제를 적용할 수 있도록 지원합니다.

## core/src/main/java/com/graphhopper/routing/util/countryrules/europe

* `AlbaniaCountryRule.java` : AlbaniaCountryRule.java 클래스는 알바니아의 도로 규칙을 정의하는 클래스입니다. 이 클래스는 `getToll` 메서드를 통해 도로의 통행료를 반환하며, 통행료가 이미 설정되어 있지 않으면 `Toll.NO`를 반환하여 통행료가 없는 도로로 설정합니다.

* 여기는 위와 같이 europe 국가의 contryrules를 정의하는 class들 뿐이라서 생략하도록 하겠음

## core/src/main/java/com/graphhopper/routing/util/parsers

* `AbstractAccessParser.java` :  AbstractAccessParser.java 클래스는 도로 접근 정보를 파싱하는 추상 클래스입니다. 이 클래스는 도로에 대한 접근 허용 여부를 정의하고, 각 도로 유형에 맞는 접근 규칙을 적용하는 파서를 위한 기본 구현을 제공합니다.

* `AbstractAverageSpeedParser.java` :  AbstractAverageSpeedParser.java 클래스는 평균 속도를 파싱하는 추상 클래스입니다. 이 클래스는 도로의 속도를 설정하고, 도로의 최대 속도를 추출하며, 속도 정보가 유효한지 확인하는 메서드를 제공합니다. 이를 통해 속도를 도로의 엣지에 설정할 수 있도록 지원합니다.

* `BikeAccessParser.java` :  BikeAccessParser.java 클래스는 자전거 접근을 처리하는 파서입니다. 이 클래스는 자전거에 대한 접근 정보를 `BooleanEncodedValue`로 설정하고, `blockPrivate`와 `blockFords` 속성에 따라 특정 도로 유형을 차단하는 기능을 제공합니다. 또한, `kissing_gate`, `stile`, `turnstile` 같은 장벽도 처리합니다.

* `BikeAverageSpeedParser.java` :  BikeAverageSpeedParser.java 클래스는 자전거의 평균 속도를 계산하는 파서입니다. 이 클래스는 `BikeCommonAverageSpeedParser`를 확장하여 자전거의 속도, 도로 표면 상태(Smoothness), 페리 속도 등을 처리하고, `path` 구간을 추가하여 자전거 경로에 대한 평균 속도를 설정합니다.

* `BikeCommonAccessParser.java` :  BikeCommonAccessParser.java 클래스는 자전거의 도로 접근 정보를 처리하는 파서입니다. 이 클래스는 특정 도로 유형을 허용하거나 제한하고, `FerrySpeedCalculator`를 사용해 페리를 자전거로 접근 가능한 도로로 설정합니다. 또한, 특정 도로에 대한 제한을 설정하며, 자전거가 접근할 수 없는 도로를 처리합니다.

* `BikeCommonAverageSpeedParser.java` :  BikeCommonAverageSpeedParser.java 클래스는 자전거 경로의 평균 속도를 계산하는 파서입니다. 이 클래스는 자전거 도로의 유형, 표면 상태, 고속도로, 트랙 유형 등을 고려하여 자전거 경로의 속도를 설정하고, 다양한 도로 상태에 맞는 속도를 적용하여 자전거 경로에 대한 평균 속도를 계산합니다.

* `BikeCommonPriorityParser.java` :  BikeCommonPriorityParser.java 클래스는 자전거 경로의 우선순위를 설정하는 파서입니다. 이 클래스는 자전거 도로에서의 우선순위를 정의하고, 도로 상태, 도로 표면 유형, 우선순위 등을 기반으로 자전거 경로의 우선순위를 계산하여 라우팅에 반영합니다.

* `BikeNetworkParserHelper.java` :  BikeNetworkParserHelper.java 클래스는 자전거 네트워크의 태그를 기반으로 지역, 지역 네트워크, 국가 네트워크, 국제 네트워크를 구분하여 `RouteNetwork`를 설정하는 기능을 제공합니다. `determine` 메서드는 주어진 태그 값에 따라 적합한 네트워크 유형을 반환합니다.

* `BikePriorityParser.java` :  BikePriorityParser.java 클래스는 자전거 도로의 우선순위를 처리하는 파서입니다. 이 클래스는 `BikeCommonPriorityParser`를 확장하여, 자전거 도로의 우선순위를 설정하고, 특정 도로 유형에 대해 우선순위를 부여하며, 자전거 경로에 맞는 속도 및 네트워크 정보를 설정합니다.

* `CarAccessParser.java` :  CarAccessParser.java 클래스는 자동차의 도로 접근 정보를 처리하는 파서입니다. 이 클래스는 도로의 접근 허용 여부를 정의하고, 자동차의 접근을 허용하는지 여부를 `BooleanEncodedValue`로 설정합니다. 또한, 회전 교차로와 같은 특정 도로 속성도 고려하여 접근 권한을 결정합니다.

* `CarAverageSpeedParser.java` :  CarAverageSpeedParser.java 클래스는 자동차 도로의 평균 속도를 파싱하는 클래스입니다. 이 클래스는 도로 표면 유형, 트랙 속도 및 기타 속도 제한을 처리하며, `defaultSpeedMap`과 `trackTypeSpeedMap`을 사용하여 각 도로에 대한 속도를 계산합니다. 또한, `badSurfaceSpeedMap`을 통해 표면이 나쁜 도로의 속도를 설정합니다.

* `CountryParser.java` :  CountryParser.java 클래스는 도로의 국가 정보를 처리하는 파서입니다. 이 클래스는 `Country` 태그를 읽어 도로가 속한 국가를 파싱하고, `EnumEncodedValue<Country>`를 사용해 도로의 국가 정보를 엣지에 설정합니다. 이를 통해 각 도로가 어떤 국가에 속하는지 관리할 수 있습니다.

* `DefaultMaxSpeedParser.java` :  DefaultMaxSpeedParser.java 클래스는 도로의 최대 속도를 설정하는 파서입니다. 이 클래스는 `maxspeed` 태그를 읽어 해당 도로의 속도를 설정하고, 국가 및 지역별 기본 속도 정보를 `LegalDefaultSpeeds` 라이브러리를 사용해 처리합니다. 속도 값이 없을 경우 국가 및 주(state)별 기본 속도를 적용하며, `ruralMaxSpeedEnc`와 `urbanMaxSpeedEnc`를 사용해 지역에 따른 속도 제한을 설정합니다.

* `FootAccessParser.java` :  FootAccessParser.java 클래스는 도보 도로의 접근 권한을 처리하는 파서입니다. 이 클래스는 도로의 접근 여부를 `BooleanEncodedValue`로 설정하며, 도보만 사용할 수 있는 도로, 예를 들어 `footway`나 `sidewalk` 등의 특정 도로 유형을 처리합니다.

* `FootAverageSpeedParser.java` :  FootAverageSpeedParser.java 클래스는 도보 경로의 평균 속도를 설정하는 파서입니다. 이 클래스는 도로 유형 및 도보 도로의 특성에 따라 속도를 설정하며, `sac_scale` 태그와 `highway` 태그에 기반하여 평균 속도를 결정합니다. 또한, 페리 속도를 처리하고, 도보 도로의 속도를 `setSpeed` 메서드를 통해 엣지에 설정합니다.

* `FootPriorityParser.java` :  FootPriorityParser.java 클래스는 도보 도로의 우선순위를 처리하는 파서입니다. 이 클래스는 도보 도로의 우선순위를 설정하고, `footway=use_sidepath` 태그를 기반으로 특정 도로 유형에 대해 우선순위를 재조정하여 도보 경로의 우선순위를 관리합니다.

* `MaxWeightExceptParser.java` :  MaxWeightExceptParser.java 클래스는 도로의 최대 중량 제한을 처리하는 파서입니다. 이 클래스는 특정 조건에 따라 최대 중량을 무시할 수 있도록 설정하며, 일부 조건에서는 `max_weight`를 무시하도록 설정하여 라우팅 경로 계산에 반영합니다.

* `ModeAccessParser.java` :  ModeAccessParser.java 클래스는 교통 수단에 따른 도로 접근을 관리하는 파서입니다. 이 클래스는 도로의 접근 가능 여부를 설정하고, 특정 도로에 대해 제한된 접근 규칙을 적용합니다. `restrictedValues`는 제한된 도로 유형을 정의하고, `vehicleForward`와 `vehicleBackward`는 도로 방향별 제한을 처리합니다. 또한, `barriers`는 장애물을 고려하여 접근을 제어합니다.

* `MountainBikeAccessParser.java` :  MountainBikeAccessParser.java 클래스는 산악 자전거 도로 접근을 처리하는 파서입니다. 이 클래스는 `sac_scale` 값에 따라 산악 자전거가 접근할 수 있는 도로를 정의하고, 특정 도로 유형과 장애물을 처리하는 기능을 제공합니다. `blockPrivate`와 `blockFords` 속성을 통해 특정 도로를 제한할 수 있습니다.

* `MountainBikeAverageSpeedParser.java` :  MountainBikeAverageSpeedParser.java 클래스는 산악 자전거의 평균 속도를 계산하는 파서입니다. 이 클래스는 도로의 표면 종류와 트랙 유형에 따른 속도를 설정하며, `grade1`부터 `grade5`까지 다양한 트랙 유형에 대한 속도를 정의하고, 다양한 표면에 대해 속도를 지정합니다. 또한, 산악 자전거 경로에 적합한 속도를 설정하여 라우팅에 반영합니다.

* `MountainBikePriorityParser.java` :  MountainBikePriorityParser.java 클래스는 산악 자전거 도로의 우선순위를 설정하는 파서입니다. 이 클래스는 도로 유형과 트랙 상태에 따라 산악 자전거 경로의 우선순위를 정의하고, `tracktype`과 `surface` 태그를 분석하여 우선순위 값을 설정합니다. `PREFER`, `SLIGHT_PREFER`, `VERY_NICE` 등의 우선순위 값을 도로 유형에 맞춰 설정합니다.

* `OSMBikeNetworkTagParser.java` :  OSMBikeNetworkTagParser.java 클래스는 OpenStreetMap(OSM)에서 자전거 네트워크 관련 태그를 파싱하는 클래스입니다. 이 클래스는 `route=bicycle`과 같은 자전거 관련 태그를 처리하여, 자전거 네트워크에 속하는 도로를 정의하고, 이를 GraphHopper에서 사용할 수 있도록 인코딩합니다.

* `OSMCrossingParser.java` :  OSMCrossingParser.java 클래스는 OpenStreetMap(OSM)에서 교차로 정보를 처리하는 파서입니다. 이 클래스는 `crossing`, `railway`, `level_crossing`과 같은 태그를 확인하고, 해당 도로에 대한 교차로 정보를 `Crossing` 열거형 값을 사용해 설정합니다. 교차로에 신호, 표식, 또는 장애물이 있는지 확인하고, 이에 맞는 정보를 엣지에 설정합니다.

* `OSMFootNetworkTagParser.java` :  OSMFootNetworkTagParser.java 클래스는 OpenStreetMap(OSM)에서 도보 네트워크 관련 태그를 파싱하는 클래스입니다. 이 클래스는 `route=hiking` 또는 `route=foot` 태그가 있는 도로와 관련된 네트워크 정보를 `RouteNetwork`로 처리하며, `network` 태그의 값을 기반으로 지역, 국가, 국제 네트워크 등을 설정하여 도로의 네트워크 범위를 정의합니다.

* `OSMFootwayParser.java` :  OSMFootwayParser.java 클래스는 OpenStreetMap(OSM)에서 도보 도로(`footway`) 태그를 처리하는 파서입니다. 이 클래스는 `footway` 태그의 값을 읽고, 이를 `Footway` 열거형 값으로 변환하여 도로 엣지에 해당 정보를 설정합니다. 이를 통해 도보 도로의 특성을 그래프에 반영할 수 있습니다.

* `OSMGetOffBikeParser.java` :  OSMGetOffBikeParser.java 클래스는 자전거 이용자가 도로에서 자전거를 내려야 하는 구간을 처리하는 파서입니다. 이 클래스는 `footway`, `pedestrian`, `platform` 등 특정 도로 유형에 대해 자전거 이용이 불가능한 구간을 설정하며, 도로가 `oneway`인 경우에도 반대 방향에서 자전거를 내려야 할지 여부를 판단하여 이를 그래프 엣지에 반영합니다.

* `OSMHazmatParser.java` :  OSMHazmatParser.java 클래스는 도로에서 위험물 운반 금지를 처리하는 파서입니다. 이 클래스는 `hazmat=no` 태그를 확인하여 해당 도로에서 위험물 운반을 금지하는 정보를 `Hazmat.NO`로 설정하고, 이를 그래프의 엣지에 반영합니다.

* `OSMHazmatTunnelParser.java` :  OSMHazmatTunnelParser.java 클래스는 OSM 데이터에서 위험물 운반 제한이 있는 터널을 처리하는 파서입니다. 이 클래스는 `hazmat:adr_tunnel_cat` 또는 `hazmat:tunnel_cat` 태그를 사용하여 터널에 대한 위험물 운반 제한 정보를 읽고, 이를 `HazmatTunnel` 열거형으로 설정하여 그래프 엣지에 반영합니다. `tunnel=yes` 태그가 있는 도로에 대해서도 해당 제한을 처리합니다.

* `OSMHazmatWaterParser.java` :  OSMHazmatWaterParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 물에 대한 위험물 운반 제한을 처리하는 파서입니다. `hazmat:water` 태그가 `no` 또는 `permissive`일 경우, 해당 도로에 대한 위험물 운반 제한을 설정하고, 이를 `HazmatWater` 열거형 값으로 인코딩하여 그래프에 반영합니다.

* `OSMHgvParser.java` :  OSMHgvParser.java 클래스는 OSM(OpenStreetMap)에서 고속도로(Heavy Goods Vehicle, HGV) 관련 태그를 처리하는 파서입니다. 이 클래스는 `hgv:conditional` 태그를 확인하여 해당 도로의 고속도로 제한을 설정하고, `Hgv` 열거형 값을 그래프의 엣지에 반영합니다. `conditionalWeightToTons` 메서드를 사용하여 조건부 중량 값을 처리하고, 고속도로에 대한 접근을 정의합니다.

* `OSMHikeRatingParser.java` :  OSMHikeRatingParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 하이킹 난이도를 처리하는 파서입니다. 이 클래스는 `sac_scale` 태그를 기반으로 하이킹 난이도를 설정하며, `hiking`부터 `difficult_alpine_hiking`까지 다양한 난이도 값을 정의하고, 이를 그래프의 엣지에 반영합니다.

* `OSMHorseRatingParser.java` :  OSMHorseRatingParser.java 클래스는 OpenStreetMap(OSM)에서 말타기 난이도를 처리하는 파서입니다. `horse_scale` 태그를 읽어 도로의 난이도를 정의하며, `common`, `demanding`, `difficult`, `critical`, `dangerous`, `impossible` 등의 난이도 값에 따라 등급을 설정합니다. 이 값을 `horseScale` 인코딩 값으로 설정하여 그래프 엣지에 반영합니다.

* `OSMLanesParser.java` :  OSMLanesParser.java 클래스는 OSM(OpenStreetMap)에서 도로의 차선 정보를 처리하는 파서입니다. 이 클래스는 `lanes` 태그를 읽어 도로에 포함된 차선의 수를 계산하고, 이를 그래프의 엣지에 설정합니다. `lanes` 값은 숫자나 구분 기호로 분리된 값을 처리하며, 최대 6차선까지 지원합니다.

* `OSMMaxAxleLoadParser.java` :  OSMMaxAxleLoadParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 최대 축 하중 정보를 파싱하는 클래스입니다. 이 클래스는 `maxaxleload` 태그를 읽어 도로의 최대 축 하중을 설정하고, 이를 `DecimalEncodedValue`로 인코딩하여 그래프의 엣지에 반영합니다.

* `OSMMaxHeightParser.java` :  OSMMaxHeightParser.java 클래스는 OSM(OpenStreetMap)에서 도로의 최대 높이를 처리하는 파서입니다. 이 클래스는 `maxheight` 또는 `maxheight:physical` 태그를 읽고, 이를 미터 단위로 변환하여 그래프 엣지에 설정합니다. `OSMValueExtractor`를 사용해 도로의 최대 높이를 추출하고, 이를 `DecimalEncodedValue`로 인코딩합니다.

* `OSMMaxLengthParser.java` :  OSMMaxLengthParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 도로의 최대 길이를 처리하는 파서입니다. 이 클래스는 `maxlength` 태그를 읽어 도로의 최대 길이를 계산하고, 이를 `DecimalEncodedValue`로 변환하여 그래프 엣지에 반영합니다.

* `OSMMaxSpeedParser.java` :  OSMMaxSpeedParser.java 클래스는 OSM(OpenStreetMap)에서 최대 속도 정보를 처리하는 파서입니다. 이 클래스는 `maxspeed`, `maxspeed:forward`, `maxspeed:backward` 태그를 읽어 도로의 최대 속도를 설정하고, 이를 그래프의 엣지에 반영합니다. 각 방향별 최대 속도를 고려하여 최적의 속도를 설정하고, 유효하지 않은 속도 값을 필터링합니다.

* `OSMMaxWeightParser.java` :  OSMMaxWeightParser.java 클래스는 OpenStreetMap(OSM) 데이터에서 도로의 최대 중량 정보를 처리하는 파서입니다. 이 클래스는 `maxweight`, `maxweightrating` 등의 태그를 읽어 도로의 최대 중량을 추출하고, 이를 `DecimalEncodedValue`로 그래프에 설정합니다. 또한, 특정 중량 조건에 대한 제한을 설정하고, HGV(Heavy Goods Vehicle) 운반 제한을 처리합니다.

* `OSMMaxWidthParser.java` :  OSMMaxWidthParser.java 클래스는 OSM(OpenStreetMap)에서 도로의 최대 폭을 처리하는 파서입니다. 이 클래스는 `maxwidth`, `maxwidth:physical`, `width`와 같은 태그를 읽고, 해당 도로의 최대 폭을 `DecimalEncodedValue`로 설정하여 그래프 엣지에 반영합니다. 이를 통해 도로의 물리적 폭 제한을 관리할 수 있습니다.

* `OSMMtbNetworkTagParser.java` :  OSMMtbNetworkTagParser.java 클래스는 OpenStreetMap(OSM)에서 산악 자전거 네트워크에 관련된 태그를 처리하는 파서입니다. 이 클래스는 `route=mtb` 태그가 있는 관계에서 네트워크 유형을 결정하고, 이를 `RouteNetwork`로 설정하여 그래프의 엣지에 반영합니다. 또한, `network` 태그를 통해 산악 자전거 네트워크의 범위를 결정합니다.

* `OSMMtbRatingParser.java` :  OSMMtbRatingParser.java 클래스는 OpenStreetMap(OSM)에서 산악 자전거 경로의 등급을 처리하는 파서입니다. 이 클래스는 `mtb:rating` 태그를 사용하여 산악 자전거 경로의 난이도를 평가하고, 이를 그래프에 반영하여 라우팅에 필요한 정보를 제공합니다.

* `OSMRoadAccessParser.java` :  OSMRoadAccessParser.java 클래스는 도로 접근 제한을 처리하는 파서입니다. 이 클래스는 `node_tags`를 확인하여 도로의 접근 제한을 설정하고, `gh:barrier_edge` 태그를 처리하여 도로에 대한 접근 권한을 정의합니다. 도로에 대한 접근 허용 여부는 `RoadAccess` 값을 사용하여 엣지에 반영됩니다.

* `OSMRoadClassLinkParser.java` :  OSMRoadClassLinkParser.java 클래스는 OSM(OpenStreetMap)에서 도로의 클래스 링크 정보를 처리하는 파서입니다. 이 클래스는 `highway` 태그가 `_link`로 끝나는 도로를 식별하고, 해당 도로를 링크 도로로 설정하여 `linkEnc` 값을 `false`로 설정합니다. 이를 통해 링크 도로를 그래프의 엣지에 반영합니다.

* `OSMRoadClassParser.java` :  OSMRoadClassParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 도로의 클래스를 파싱하는 클래스입니다. 이 클래스는 `highway` 태그를 기반으로 도로의 클래스를 `RoadClass` 열거형 값으로 설정하며, `_link`로 끝나는 도로는 링크 도로로 처리합니다. 이를 통해 도로의 종류와 특성을 그래프 엣지에 반영합니다.

* `OSMRoadEnvironmentParser.java` :  OSMRoadEnvironmentParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 도로 환경 정보를 처리하는 파서입니다. 이 클래스는 도로가 페리, 교량, 터널, 포드(ford), 일반 도로 등 다양한 환경에 속하는지를 확인하고, 이를 `RoadEnvironment` 열거형으로 설정하여 해당 도로의 환경을 그래프 엣지에 반영합니다.

* `OSMRoundaboutParser.java` :  OSMRoundaboutParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 회전교차로(`roundabout`) 정보를 처리하는 파서입니다. 이 클래스는 `junction=roundabout` 또는 `junction=circular` 태그를 확인하여 도로가 회전교차로인지 여부를 판단하고, 해당 정보를 `BooleanEncodedValue`로 설정하여 그래프 엣지에 반영합니다.

* `OSMSmoothnessParser.java` :  OSMSmoothnessParser.java 클래스는 도로의 표면 상태(Smoothness)를 파싱하는 클래스입니다. 이 클래스는 `smoothness` 태그를 기반으로 도로의 표면 상태를 평가하고, 이를 `SurfaceQuality` 열거형으로 변환하여 그래프 엣지에 반영합니다. 표면 상태가 좋을수록 더 높은 우선순위를 부여하고, 이를 라우팅에 반영하여 경로 계산에 영향을 미칩니다.

* `OSMSurfaceParser.java` :  OSMSurfaceParser.java 클래스는 OSM(OpenStreetMap)에서 도로의 표면 상태를 파싱하는 클래스입니다. `surface` 태그를 읽고, 이를 `Surface` 열거형 값으로 변환하여 그래프의 엣지에 설정합니다. 도로의 표면 상태에 따라 라우팅을 최적화할 수 있도록 도와줍니다.

* `OSMTemporalAccessParser.java` :  OSMTemporalAccessParser.java 클래스는 OSM(OpenStreetMap)에서 시간에 따른 도로 접근 제한을 처리하는 파서입니다. 이 클래스는 `temporal` 또는 `seasonal` 태그를 기반으로 도로의 접근 제한을 설정하며, 특정 날짜나 기간에 따른 도로 접근 권한을 결정합니다. 또한, `DateRangeParser`를 사용해 날짜 범위를 처리하고, 이를 `EdgeIntAccess`로 설정하여 그래프에 반영합니다.

* `OSMTollParser.java` :  OSMTollParser.java 클래스는 OSM(OpenStreetMap)에서 도로의 통행료 정보를 처리하는 파서입니다. `toll:hgv`, `toll:N2`, `toll:N3` 등의 태그를 확인하여 해당 도로의 통행료를 설정하고, 이를 `Toll` 열거형 값으로 그래프 엣지에 반영합니다. `CountryRule`을 사용하여 국가별 통행료 규제를 추가로 처리할 수 있습니다.

* `OSMTrackTypeParser.java` :  OSMTrackTypeParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 트랙 유형(`tracktype`) 정보를 처리하는 파서입니다. 이 클래스는 `tracktype` 태그를 확인하여, 해당 도로의 트랙 유형을 `TrackType` 열거형 값으로 변환하고, 이를 `EnumEncodedValue<TrackType>`에 설정하여 그래프 엣지에 반영합니다.

* `OSMWayIDParser.java` :  OSMWayIDParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 도로의 ID 정보를 처리하는 파서입니다. 이 클래스는 `way` 태그를 읽어 도로의 ID를 추출하고, 이를 그래프 엣지에 `WayID`로 저장하여, 경로 계산에 필요한 도로 식별 정보를 관리합니다.

* `OrientationCalculator.java` :  OrientationCalculator.java 클래스는 도로의 방향을 계산하는 클래스입니다. 이 클래스는 `ReaderWay`의 `point_list`에서 도로의 시작과 끝 지점을 가져와, `AngleCalc`를 사용하여 각 도로의 방향(방위각)을 계산하고 이를 `DecimalEncodedValue`로 저장합니다. 도로의 방향은 양방향에 대해 계산되며, 이를 그래프 엣지에 반영합니다.

* `RacingBikeAccessParser.java` :  RacingBikeAccessParser.java 클래스는 경주용 자전거 도로 접근을 처리하는 파서입니다. 이 클래스는 `tracktype`과 `surface`와 같은 태그를 분석하여 경주용 자전거가 접근 가능한 도로를 정의하고, 이를 그래프의 엣지에 반영하여 경주용 자전거 경로를 설정합니다.

* `RacingBikeAverageSpeedParser.java` :  RacingBikeAverageSpeedParser.java 클래스는 경주용 자전거의 평균 속도를 계산하는 파서입니다. 이 클래스는 도로의 트랙 유형과 표면에 따라 자전거 속도를 설정하며, `grade1`부터 `grade5`까지의 트랙 유형에 대해 각기 다른 속도를 지정하고, 다양한 표면 유형에 맞는 속도를 설정합니다. 이를 통해 경주용 자전거 경로에 대한 정확한 평균 속도를 계산하여 라우팅에 반영합니다.

* `RacingBikePriorityParser.java` :  RacingBikePriorityParser.java 클래스는 경주용 자전거의 경로 우선순위를 설정하는 파서입니다. 이 클래스는 도로 유형에 따라 우선순위를 설정하고, `path`, `road`, `secondary`, `tertiary`와 같은 도로 태그를 기반으로 우선순위를 지정합니다. 또한, `motorway`와 같은 고속도로를 회피하며, 특정 트랙 유형(예: `grade1`)에 대해서는 우선순위를 높이는 기능을 제공합니다.

* `RelationTagParser.java` :  RelationTagParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 관계(Relation) 태그를 처리하는 인터페이스입니다. 이 클래스는 `handleRelationTags` 메서드를 통해 `ReaderRelation` 객체에서 라우팅 플래그를 생성하고, 이를 `IntsRef`로 반환하여 그래프 엣지에 반영합니다. `ReaderRelation`을 엣지 플래그로 변환할 수 있도록 돕는 기능을 제공합니다.

* `RestrictionSetter.java` :  RestrictionSetter.java 클래스는 OSM(OpenStreetMap) 데이터에서 도로의 제한 사항을 처리하는 클래스입니다. 이 클래스는 회전 금지, 차선 변경 금지, 도로 속도 제한 등 다양한 제한을 설정하고 이를 경로 계산에 반영합니다. 또한, 특정 조건에 맞는 제한을 확인하여 라우팅 경로를 최적화할 수 있도록 돕습니다.

* `StateParser.java` :  StateParser.java 클래스는 국가 또는 주(state) 단위로 도로의 구분 정보를 처리하는 파서입니다. 이 클래스는 OSM(OpenStreetMap)에서 `state` 태그를 읽어 도로가 속한 국가 내의 주(state)를 구분하고, 이를 `State` 열거형 값으로 설정하여 그래프 엣지에 반영합니다.

* `TagParser.java` :  TagParser.java 클래스는 OSM(OpenStreetMap) 데이터에서 다양한 태그를 처리하는 기본 파서 클래스입니다. 이 클래스는 OSM의 여러 태그를 해석하여 라우팅에 필요한 정보를 추출하고, 이를 그래프 엣지에 설정합니다. `handleWayTags` 메서드는 각 태그에 대해 처리 작업을 정의하고, 이를 `EncodedValue`로 저장하여 후속 작업에 활용합니다.

## core/src/main/java/com/graphhopper/routing/util/tour

* `MultiPointTour.java` : MultiPointTour.java 클래스는 여러 지점을 포함하는 경로를 계산하고 관리하는 클래스입니다. 이 클래스는 여러 지점에 대한 경로를 생성하고, 각 지점 사이의 경로를 계산하여 효율적인 경로를 제공하는 데 사용됩니다. 주로 여러 목적지를 가진 여행 경로를 처리하는 데 활용됩니다.

* `SinglePointTour.java` : SinglePointTour.java 클래스는 단일 지점을 생성하는 경로 생성 전략을 구현한 클래스입니다. 이 클래스는 `TourStrategy`를 상속하여 하나의 지점만 생성하도록 설정하며, `getDistanceForIteration` 메서드를 통해 반복마다 약간 수정된 거리를 반환하고, `getHeadingForIteration` 메서드를 통해 랜덤한 방향을 반환합니다. 이를 통해 단일 지점을 중심으로 한 경로 생성을 제공합니다.

* `TourStrategy.java` : TourStrategy.java 클래스는 경로 생성 전략을 정의하는 추상 클래스입니다. 이 클래스는 주어진 거리 및 무작위 요소를 기반으로 경로의 지점을 생성하는 전략을 제공하며, getNumberOfGeneratedPoints, getDistanceForIteration, getHeadingForIteration 메서드를 통해 각 지점의 수, 거리 및 방향을 계산합니다. slightlyModifyDistance 메서드는 생성된 거리를 약간 수정하여 다양성을 추가합니다.

## core/src/main/java/com/graphhopper/routing/weighting

* `AbstractAdjustedWeighting.java` : AbstractAdjustedWeighting.java 클래스는 다른 `Weighting` 객체를 감싸는 추상 클래스입니다. 이 클래스는 기본 `Weighting` 객체(`superWeighting`)를 래핑하여, `calcEdgeWeight`, `calcEdgeMillis`, `calcTurnWeight` 등의 메서드를 호출하고 결과를 수정할 수 있는 기반을 제공합니다. 이 클래스는 상속하여 특정 가중치를 조정할 수 있도록 설계되었습니다.

* `AvoidEdgesWeighting.java` : AvoidEdgesWeighting.java 클래스는 특정 엣지들을 회피하도록 경로 가중치를 조정하는 클래스입니다. 이 클래스는 `avoidedEdges`에 지정된 엣지에 대해 가중치를 증가시키는 기능을 제공하며, 이를 통해 해당 엣지가 최단 경로에 포함될 가능성을 낮춥니다. `edgePenaltyFactor`를 설정하여 회피할 엣지에 대한 가중치 배율을 조정할 수 있습니다.

* `BalancedWeightApproximator.java` : BalancedWeightApproximator.java 클래스는 단방향 가중치 근사치를 양방향으로 조정하는 역할을 합니다. 이 클래스는 `WeightApproximator` 객체를 사용하여, A* 알고리즘에서의 정지 기준에 맞게 양방향 경로 추정치를 균형 잡힌 형태로 제공하며, `setFromTo` 메서드를 통해 출발지와 도착지를 설정하고, 가중치 추정치를 계산하여 경로 탐색에 활용할 수 있도록 합니다.

* `BeelineWeightApproximator.java` : BeelineWeightApproximator.java 클래스는 목표 노드까지의 예상 거리를 비선형 거리로 근사하는 가중치 계산기입니다. `DistanceCalc`를 사용해 시작 노드와 목표 노드 간의 거리를 계산하고, 이를 목표 노드까지의 최소 가중치와 곱하여 가중치를 추정합니다. 이 클래스는 경로 탐색에서 목표까지의 대략적인 거리를 반영하는 데 사용됩니다.

* `DefaultTurnCostProvider.java` : DefaultTurnCostProvider.java 클래스는 회전 시 발생하는 비용(turn cost)을 제공하는 클래스입니다. 이 클래스는 회전과 관련된 비용을 계산하고, 회전할 때 발생하는 비용을 `calcTurnCost` 메서드를 통해 경로 계산에 반영합니다. 이를 통해 회전이 포함된 경로에서의 추가 비용을 관리할 수 있습니다.

* `QueryGraphWeighting.java` : QueryGraphWeighting.java 클래스는 `QueryGraph`에서 최단 경로 계산 시 회전 비용(turn cost)을 포함하여 가중치를 정확히 계산하도록 해주는 래퍼 클래스입니다. 이 클래스는 `Weighting` 객체를 감싸서, `QueryGraph`에서 가상 노드와 가상 엣지를 포함한 경로 탐색 시 회전 비용을 적절히 처리할 수 있도록 합니다. 이를 통해 가상 노드와 관련된 경로 탐색에서도 회전 비용을 반영한 정확한 경로 계산이 가능합니다.

* `SpeedWeighting.java` : SpeedWeighting.java 클래스는 경로 탐색에서 각 엣지에 대한 속도 기반 가중치를 계산하는 클래스입니다. 이 클래스는 속도 제한 정보를 바탕으로 `calcEdgeWeight` 메서드를 사용하여 가중치를 계산하고, 속도에 따른 시간 및 비용을 추정하여 라우팅 알고리즘에 반영합니다. 이를 통해 속도에 따라 더 효율적인 경로를 찾을 수 있도록 돕습니다.

* `TurnCostProvider.java` : TurnCostProvider.java 클래스는 회전 비용을 계산하는 인터페이스입니다. 이 클래스는 `calcTurnWeight`와 `calcTurnMillis` 메서드를 정의하여, 특정 엣지에서 다른 엣지로의 회전 비용과 소요 시간을 계산합니다. `NO_TURN_COST_PROVIDER`는 회전 비용이 없는 기본 구현을 제공합니다. 이를 통해 회전 시 발생하는 추가 비용을 라우팅 알고리즘에 반영할 수 있습니다.

* `WeightApproximator.java` : WeightApproximator.java 클래스는 노드 간의 가중치 추정치를 계산하는 인터페이스입니다. 이 클래스는 `approximate` 메서드를 통해 현재 노드에서 목표 노드까지의 최소 가중치를 추정하며, `setTo` 메서드로 목표 노드를 설정합니다. 또한, `reverse` 메서드를 통해 반대 방향으로 추정할 수 있도록 객체를 복사하며, `getSlack` 메서드를 사용하여 여유 시간을 반환합니다.

* `Weighting.java` : Weighting.java 클래스는 그래프의 엣지에 대해 가중치를 계산하는 인터페이스입니다. 이 클래스는 여러 메서드를 통해 경로 탐색 중 가중치를 계산하고, 특히 `calcEdgeWeight`와 `calcEdgeMillis`는 엣지의 가중치와 이동 시간을 계산합니다. 또한, 회전 비용을 처리하는 `calcTurnWeight`와 `calcTurnMillis` 메서드를 제공하며, `hasTurnCosts` 메서드는 회전 비용이 있는지 확인합니다. 이 인터페이스를 통해 다양한 가중치 계산 방법을 구현할 수 있습니다.

## core/src/main/java/com/graphhopper/routing/weighting/custom

* `ClassHelper.java` : ClassHelper.java 클래스는 커스텀 가중치 클래스를 처리하는 인터페이스입니다. 이 인터페이스는 `getClassName` 메서드를 정의하여, 주어진 인코딩 값을 바탕으로 클래스 이름을 반환하는 기능을 제공합니다. 이를 통해 커스텀 가중치에서 클래스 이름을 동적으로 결정할 수 있도록 합니다.

* `ConditionalExpressionVisitor.java` : ConditionalExpressionVisitor.java 클래스는 커스텀 경로 계산 모델에서 조건식 처리에 사용되는 방문자 클래스입니다. 이 클래스는 `Visitor.AtomVisitor` 인터페이스를 구현하며, `if` 및 `else_if` 조건을 처리합니다. `allowedMethodParents`와 `allowedMethods`를 사용하여 허용된 메서드 및 변수만을 처리하도록 제한하고, `variableValidator`를 통해 변수 이름을 검증합니다. 이 클래스는 주어진 조건식을 평가하고, `ParseResult`에 해당 결과를 반영합니다.

* `CustomModelParser.java` : CustomModelParser.java 클래스는 GraphHopper에서 커스텀 경로 모델을 처리하는 파서입니다. 이 클래스는 조건부 표현식, 변수, 메서드 등을 기반으로 한 모델을 분석하여 경로 계산에 사용될 수 있는 규칙을 생성합니다. 이를 통해 커스텀 모델에 정의된 다양한 경로 제한 조건을 적용하여 최적의 경로를 계산할 수 있게 해줍니다.

* `CustomWeighting.java` : CustomWeighting.java 클래스는 기본 가중치 계산을 기반으로 엣지의 가중치를 사용자 정의 규칙에 맞게 조정하는 클래스입니다. 이 클래스는 `Weighting`을 구현하며, `calcEdgeWeight`, `calcEdgeMillis`와 같은 메서드를 사용하여 사용자 정의 모델에 맞춰 경로 탐색에 필요한 가중치를 계산합니다. 이를 통해 기본 속도 및 접근 제한 외에도 사용자 정의된 조건을 고려하여 경로 계산을 맞춤화할 수 있습니다.

* `CustomWeighting2.java` : CustomWeighting2.java 클래스는 커스텀 경로 가중치를 계산하는 클래스입니다. `Weighting` 인터페이스를 구현하며, 엣지의 속도와 우선순위에 따라 경로의 가중치를 계산합니다. `calcEdgeWeight` 메서드는 엣지의 우선순위에 따라 가중치를 계산하고, `calcEdgeMillis`는 속도를 기반으로 소요 시간을 계산합니다. 또한, 회전 비용을 처리하는 `TurnCostProvider`를 사용하여 경로 계산에 회전 비용을 반영합니다.

* `CustomWeightingHelper.java` : CustomWeightingHelper.java 클래스는 `CustomWeighting`에서 사용되는 헬퍼 클래스로, 커스텀 모델을 초기화하고 속도 및 우선순위를 계산하는 기능을 제공합니다. 이 클래스는 `getPriority`와 `getSpeed` 메서드를 통해 엣지에 대한 우선순위와 속도를 반환하며, `calcMaxSpeed` 메서드를 통해 최대 속도를 계산합니다. `init` 메서드는 `CustomModel`과 `EncodedValueLookup`을 초기화하여, 커스텀 가중치 계산에 필요한 데이터를 설정합니다.

* `FindMinMax.java` : FindMinMax.java 클래스는 라우팅에 사용되는 커스텀 모델의 유효성을 검사하는 클래스입니다. 이 클래스는 A* 알고리즘에서 최적성을 유지하기 위해 커스텀 모델이 기본 모델에 비해 엣지의 가중치를 감소시키지 않도록 확인합니다. checkLMConstraints 메서드는 거리 영향을 확인하고, priority와 speed와 같은 속성의 값들이 정의된 최소 및 최대 범위 내에 있는지 검사합니다.

* `NameValidator.java` : NameValidator.java 클래스는 주어진 이름이 유효한지 확인하는 인터페이스입니다. `isValid` 메서드는 문자열 이름을 입력받아 유효성을 검사하고, 그 결과를 `boolean` 값으로 반환합니다. 이 인터페이스는 커스텀 모델에서 이름 검증을 처리하는 데 사용됩니다.

* `ParseResult.java` : ParseResult.java 클래스는 커스텀 경로 모델을 파싱하고 처리하는 클래스입니다. 이 클래스는 파싱 결과를 담을 `StringBuilder` 객체인 `converted`, 파싱이 성공했는지 여부를 나타내는 `ok`, 오류 메시지를 담을 `invalidMessage`를 포함합니다. 또한, 추측된 변수와 연산자를 추적하는 `guessedVariables`와 `operators`를 저장하는 `Set`을 사용하여 모델 파싱의 결과를 관리합니다.

* `ValueExpressionVisitor.java` : ValueExpressionVisitor.java 클래스는 커스텀 모델의 표현식을 처리하는 방문자 클래스입니다. 이 클래스는 조건부 표현식이나 변수 계산 등을 처리하며, 파싱된 표현식을 평가하는 데 사용됩니다. `visit` 메서드를 통해 각 유형의 표현식을 처리하고, 결과를 반환하며, 이를 기반으로 모델의 유효성을 검사하고 실행할 수 있도록 지원합니다.

## core/src/main/java/com/graphhopper/search

* `KVStorage.java` : KVStorage.java 클래스는 `Key-Value` 형태로 데이터를 저장하고 조회하는 기능을 제공하는 클래스입니다. 이 클래스는 `Key`와 `Value` 쌍을 저장하는 구조를 구현하고, 데이터를 빠르게 검색하고 업데이트할 수 있는 메서드를 제공합니다. 주로 데이터를 효율적으로 관리하고, 경로 계산 시 빠른 데이터 접근을 가능하게 합니다.

## core/src/main/java/com/graphhopper/storage

* `AbstractDataAccess.java` : AbstractDataAccess.java 파일은 GraphHopper 라우팅 엔진의 저장소 계층에 속하는 추상 클래스입니다. 이 클래스는 데이터 유형에 따라 다양한 저장 방식을 관리하며, 파일 저장과 메모리 접근을 처리하는 메서드를 제공합니다. 하위 클래스들이 특정 저장 전략을 구현할 수 있도록 인터페이스를 제공하여 효율적인 데이터 접근을 돕습니다.

* `BaseGraph.java` : BaseGraph.java 파일은 GraphHopper에서 라우팅을 위한 핵심 그래프 구조를 정의합니다. 이 파일은 노드와 엣지를 관리하며, 그래프 저장소와 노드, 엣지 및 그 속성들을 추가하고 조회하는 메서드를 제공합니다. 이 클래스는 라우팅 엔진이 맵 데이터를 구조화하고 다른 컴포넌트들과 상호작용하는 데 중요한 역할을 하며, 보다 특화된 그래프 구현을 위한 기초 역할을 합니다.

* `BaseGraphNodesAndEdges.java` : BaseGraphNodesAndEdges.java 파일은 GraphHopper에서 그래프의 노드와 엣지를 관리하는 역할을 합니다. 노드와 엣지 데이터를 저장하고 읽어오는 메커니즘을 제공하며, 그래프의 기본 구조를 유지하고 최적화된 데이터 접근을 지원합니다.

* `CHConfig.java` : CHConfig.java 파일은 GraphHopper에서 CH(Contraction Hierarchies) 라우팅 프로필의 속성을 정의하는 클래스입니다. 이 클래스는 노드 기반 또는 엣지 기반 라우팅 설정을 포함하며, CH 그래프 데이터의 저장 및 식별을 지원합니다. 프로필 이름 검증과 가중치(weighting) 설정 등을 처리합니다.

* `CHEdgeFilter.java` : CHEdgeFilter.java 파일은 GraphHopper에서 CH(Contraction Hierarchies)와 관련된 엣지 필터를 정의하는 클래스입니다. 이 클래스는 특정 조건에 따라 엣지의 포함 여부를 결정하며, 라우팅 알고리즘에서 필터링 로직을 처리하는 데 사용됩니다.

* `CHStorage.java` : CHStorage.java 파일은 GraphHopper에서 CH(Contraction Hierarchies) 데이터의 저장을 처리하는 클래스입니다. CH 그래프의 노드, 엣지, 계층 구조 등의 데이터를 관리하며, 효율적인 라우팅을 위한 데이터 최적화 및 접근 방식을 제공합니다.

* `CHStorageBuilder.java` : CHStorageBuilder.java 파일은 CH(Contraction Hierarchies) 저장소를 생성하고 구성하는 역할을 합니다. 노드의 계층(level)을 설정하고, 유효한 CH 단축 경로(shortcut)를 추가하며, 저장소 내의 데이터 정렬 및 유효성을 보장합니다. CHStorage 인스턴스와 밀접하게 연관되어 있습니다.

* `DAType.java` : DAType.java 파일은 GraphHopper에서 DataAccess 객체의 생성 방식을 정의하는 클래스입니다. RAM, RAM_INT, RAM_STORE, MMAP 등 다양한 데이터 접근 유형을 지원하며, 메모리와 디스크 기반 저장 방식의 설정을 제공합니다. 이를 통해 데이터 저장소를 최적화하고 필요한 유형에 맞게 조정할 수 있습니다.

* `DataAccess.java` : DataAccess.java 파일은 GraphHopper에서 데이터 접근 인터페이스를 정의합니다. 바이트 단위로 데이터를 설정하거나 가져오는 메서드들을 제공하며, 메모리 및 파일 기반 저장소의 읽기와 쓰기를 효율적으로 처리하도록 설계되었습니다. 데이터의 생명주기 관리를 지원합니다.

* `Directory.java` : Directory.java 파일은 GraphHopper에서 여러 DataAccess 객체를 관리하는 인터페이스입니다. 특정 위치에서 DataAccess 객체를 생성, 제거하거나 디렉터리의 자원을 정리하는 메서드를 제공합니다. 이를 통해 노드, 엣지, 위치-아이디 매핑 데이터에 대한 효율적인 관리가 가능합니다.

* `ExtendedNodeAccess.java` : ExtendedNodeAccess.java 파일은 기존 NodeAccess에 추가적인 노드 정보를 관리하는 기능을 더한 클래스입니다. 추가적인 노드 데이터를 PointList로 저장하며, 기존 노드와의 ID 구분 및 접근 로직을 처리합니다. 이를 통해 확장된 노드 데이터 관리가 가능합니다.

* `GHDirectory.java` : GHDirectory.java 파일은 GraphHopper에서 Directory 인터페이스의 구현체로, 데이터 접근 객체(DataAccess)를 생성하고 관리합니다. 메모리 또는 파일 시스템에 기반하여 저장소를 설정하며, 환경에 따른 다양한 설정을 지원합니다.

* `GHLock.java` : GHLock.java 파일은 GraphHopper에서 잠금 기능을 관리하는 클래스입니다. 저장소 작업 중 동시 접근을 방지하기 위해 사용되며, 락(lock)을 생성, 해제하거나 확인하는 메서드를 제공합니다. 안정적인 데이터 처리에 기여합니다.

* `GHNodeAccess.java` : GHNodeAccess.java 파일은 GraphHopper에서 노드 데이터를 관리하는 클래스입니다. 노드의 위치, 고도와 같은 속성을 저장하고 접근하는 기능을 제공하며, 그래프 내의 노드와 관련된 데이터를 처리하는 데 사용됩니다.

* `Graph.java` : Graph.java 파일은 GraphHopper에서 그래프 인터페이스를 정의하며, 노드와 엣지의 추가, 조회 및 관련 속성 관리를 위한 메서드를 제공합니다. 라우팅 엔진의 기본 그래프 구조를 설계하고 상호작용을 지원하는 핵심 클래스입니다.

* `IntsRef.java` : IntsRef.java 파일은 GraphHopper에서 정수 배열을 저장하고 조작하기 위한 불변 참조를 제공합니다. 배열의 길이, 오프셋, 내용을 효율적으로 관리하며, Lucene에서 영감을 받아 구현되었습니다. 주로 정수 기반 데이터 처리를 최적화하는 데 사용됩니다.

* `LockFactory.java` : LockFactory.java 파일은 GraphHopper에서 파일 잠금을 관리하기 위한 인터페이스를 정의합니다. 잠금 디렉터리를 설정하고, 읽기 및 쓰기 잠금을 생성하거나 강제로 제거하는 메서드를 제공합니다. 이를 통해 데이터 저장소의 동시성 문제를 방지합니다.

* `MMapDataAccess.java` : MMapDataAccess.java 파일은 GraphHopper에서 메모리 맵핑(Memory Mapping)을 사용하여 데이터를 효율적으로 저장하고 읽기/쓰기를 처리하는 클래스입니다. 파일 기반 저장소에서 고속 데이터 접근을 지원하며, 메모리와 파일 간의 데이터를 매핑해 성능을 최적화합니다.

* `MMapDirectory.java` : MMapDirectory.java 파일은 GraphHopper에서 메모리 맵핑(Memory Mapped) DataAccess 객체를 관리하는 클래스입니다. GHDirectory를 확장하며, 저장소 위치와 DAType.MMAP 설정을 통해 메모리 맵핑 기반 데이터 접근을 지원합니다.

* `NativeFSLockFactory.java` : NativeFSLockFactory.java 파일은 GraphHopper에서 네이티브 파일 시스템 기반의 잠금(lock) 메커니즘을 제공하는 클래스입니다. 파일을 이용해 읽기 및 쓰기 잠금을 구현하며, 동시 데이터 접근 문제를 방지하도록 설계되었습니다. Lucene의 잠금 구현에서 영감을 받았습니다.

* `NodeAccess.java` : NodeAccess.java 파일은 GraphHopper에서 그래프 노드의 속성에 접근하는 인터페이스를 정의합니다. 노드의 회전 비용 인덱스를 설정하고 가져오는 메서드를 제공하며, 멀티 스레드 환경에서 독립적으로 작동하도록 설계되었습니다. TurnCostStorage와의 상호작용을 지원합니다.

* `RAMDataAccess.java` : RAMDataAccess.java 파일은 GraphHopper에서 RAM 기반 데이터 접근을 구현하는 클래스입니다. 데이터를 메모리에 직접 저장 및 처리하여 빠른 읽기/쓰기 작업을 지원하며, 고성능 데이터 접근이 필요한 경우 사용됩니다.

* `RAMDirectory.java` : RAMDirectory.java 파일은 GraphHopper에서 메모리에 저장된 DataAccess 객체를 관리하는 클래스입니다. RAM 기반 저장소를 완전히 메모리에 유지하거나 필요 시 저장 및 로드할 수 있도록 설정을 제공합니다. RAMDataAccess 및 RAMIntDataAccess와 밀접하게 관련됩니다.

* `RAMIntDataAccess.java` : RAMIntDataAccess.java 파일은 GraphHopper에서 정수 배열 기반의 메모리 내 데이터 접근을 구현한 클래스입니다. 데이터를 메모리에 유지하며 필요한 경우 flush()를 통해 디스크에 저장할 수 있는 기능을 제공합니다. 빠르고 효율적인 정수 기반 데이터 처리를 지원합니다.

* `RoutingCHEdgeExplorer.java` : RoutingCHEdgeExplorer.java 파일은 GraphHopper에서 CH(Contraction Hierarchies) 라우팅을 위한 엣지 탐색 인터페이스를 정의합니다. 라우팅 과정에서 특정 엣지를 탐색하거나 필터링하여 최적 경로를 계산하는 데 사용됩니다.

* `RoutingCHEdgeIterator.java` : RoutingCHEdgeIterator.java 파일은 GraphHopper에서 CH(Contraction Hierarchies) 탐색을 위한 엣지 이터레이터 인터페이스를 정의합니다. 현재 엣지의 상태를 제공하는 RoutingCHEdgeIteratorState를 확장하며, `next()` 메서드를 통해 엣지 탐색을 진행할 수 있습니다.

* `RoutingCHEdgeIteratorImpl.java` : RoutingCHEdgeIteratorImpl.java 파일은 GraphHopper에서 CH(Contraction Hierarchies) 엣지 탐색의 구체적인 구현체로, 엣지 및 단축 경로를 탐색합니다. `outEdges`와 `inEdges` 메서드를 통해 출력 및 입력 엣지를 설정하며, CHStorage 및 BaseGraph와 상호작용합니다.

* `RoutingCHEdgeIteratorState.java` : RoutingCHEdgeIteratorState.java 파일은 GraphHopper에서 CH(Contraction Hierarchies) 엣지 상태를 정의하는 인터페이스입니다. CH 엣지 ID, 원래 엣지 ID, 연결 노드, 단축 경로의 스킵된 엣지 정보 및 가중치 등의 속성을 제공합니다. CH 라우팅에 필요한 엣지 데이터를 구조적으로 관리합니다.

* `RoutingCHEdgeIteratorStateImpl.java` : RoutingCHEdgeIteratorStateImpl.java 파일은 GraphHopper에서 CH(Contraction Hierarchies) 엣지 상태를 구현한 클래스입니다. CHStorage 및 BaseGraph와 상호작용하여 엣지의 기본 노드, 인접 노드, 단축 경로 정보를 관리하며, 가중치 계산과 경로 최적화를 지원합니다.

* `RoutingCHGraph.java` : RoutingCHGraph.java 파일은 GraphHopper에서 CH(Contraction Hierarchies) 기반 그래프를 정의하는 인터페이스입니다. 노드, 엣지, 단축 경로를 탐색하는 메서드를 제공하며, 그래프의 레벨과 턴 비용을 계산할 수 있습니다. BaseGraph 및 Weighting과 상호작용하여 라우팅에 필요한 데이터와 기능을 지원합니다.

* `RoutingCHGraphImpl.java` :  RoutingCHGraphImpl.java 파일은 GraphHopper에서 CH(Contraction Hierarchies) 기반 라우팅 그래프의 구체적인 구현체입니다. BaseGraph, CHStorage, Weighting과 상호작용하여 노드와 엣지 탐색, 단축 경로 관리, 그리고 턴 비용 처리를 포함한 CH 그래프 관련 기능을 제공합니다.

* `SimpleFSLockFactory.java` : SimpleFSLockFactory.java 파일은 GraphHopper에서 파일 기반 잠금을 생성하고 관리하는 클래스입니다. 디렉터리 내에 잠금 파일을 생성하며, 잠금 해제 및 강제 제거 기능도 제공합니다. Lucene의 잠금 구현을 기반으로 설계되었습니다.

* `Storable.java` : Storable.java 파일은 GraphHopper에서 저장 가능한(storable) 객체의 생명주기를 정의하는 인터페이스입니다. 객체 생성, 설정, 데이터 로드 및 생성, 사용 중 데이터 플러시, 그리고 종료 시 닫기(close)와 같은 일관된 메서드 구조를 제공합니다. Closeable 인터페이스를 확장합니다.

* `StorableProperties.java` : StorableProperties.java 파일은 GraphHopper에서 메모리에 저장된 속성 데이터를 파일로 플러시(저장)하거나 기존 파일에서 속성을 로드하는 역할을 합니다. 데이터 접근(DataAccess) 객체와 상호작용하며, 속성 데이터를 Key-Value 형태로 관리합니다. Thread-safe로 설계되어 동시 작업을 지원합니다.

* `TurnCostStorage.java` : TurnCostStorage.java 파일은 GraphHopper에서 회전 비용(turn cost)을 관리하는 클래스입니다. 특정 노드와 엣지에 대한 회전 비용을 저장하고 조회하는 기능을 제공하며, 효율적인 라우팅을 위해 데이터를 압축된 형태로 관리합니다.

* `VLongStorage.java` : VLongStorage.java 파일은 GraphHopper에서 가변 길이(long 타입)의 데이터를 저장하고 관리하는 클래스입니다. 메모리 효율성을 극대화하기 위해 가변 길이 인코딩을 사용하며, 데이터 접근과 저장 공간 최적화를 지원합니다.

## core/src/main/java/com/graphhopper/storage/index

* `InMemConstructionIndex.java` : InMemConstructionIndex.java 파일은 GraphHopper에서 메모리 내 인덱스 구조를 관리하는 클래스입니다. Leaf와 Tree 구조를 사용해 데이터를 저장하고 탐색하며, SpatialKeyAlgo를 통해 공간 데이터를 인코딩 및 관리합니다. 빠른 위치 기반 검색을 지원합니다.

* `IndexStructureInfo.java` : IndexStructureInfo.java 파일은 GraphHopper에서 공간 인덱스의 구조 정보를 정의하는 클래스입니다. 바운딩 박스(BBox)와 분할 수준 등을 기반으로 인덱스 구조를 생성하며, SpatialKeyAlgo와 PixelGridTraversal을 활용해 효율적인 위치 검색을 지원합니다.

* `LineIntIndex.java` : LineIntIndex.java 파일은 GraphHopper에서 선형 데이터의 정수 인덱스를 관리하는 클래스입니다. 공간 분할 및 범위 기반 검색을 지원하며, 빠르고 효율적인 데이터 조회를 위해 설계되었습니다. PixelGridTraversal 등과 함께 작동합니다.

* `LocationIndex.java` : LocationIndex.java 파일은 GraphHopper에서 위도 및 경도와 같은 실제 위치 데이터를 메모리 내 그래프의 내부 ID 또는 인덱스에 매핑하는 인터페이스입니다. `findClosest` 메서드를 통해 특정 위치에서 가장 가까운 노드와 엣지를 탐색하며, EdgeFilter를 사용해 결과를 필터링할 수 있습니다. 다중 스레드 환경에서 안전하게 작동하도록 설계되었습니다.

* `LocationIndexTree.java` : LocationIndexTree.java 파일은 GraphHopper에서 공간 데이터를 효율적으로 검색하기 위한 트리 기반 위치 인덱스를 구현합니다. BBox 및 QuadTree를 활용하여 검색 속도를 높이며, 노드 및 엣지의 근접성을 빠르게 계산하는 데 사용됩니다.

* `PixelGridTraversal.java` : PixelGridTraversal.java 파일은 GraphHopper에서 공간 데이터의 그리드 기반 탐색을 지원하는 클래스입니다. 좌표를 픽셀 단위로 변환하고 경로를 계산하며, 효율적인 범위 탐색 및 데이터 정렬을 수행합니다.

* `Snap.java` : Snap.java 파일은 GraphHopper에서 `LocationIndex` 검색 결과를 표현하는 클래스입니다. 특정 위치에서 가장 가까운 도로, 노드, 그리고 "스냅된" 좌표를 저장하며, 거리 계산과 관련 데이터를 제공하여 경로 탐색을 지원합니다.

## core/src/main/java/com/graphhopper/util

* `AngleCalc.java` : AngleCalc.java 파일은 GraphHopper에서 세 점을 사용하여 회전 각도를 계산하는 클래스입니다. 고속 atan2 메서드를 통해 효율적인 계산을 지원하며, 자주 사용되는 각도 계산에 대한 상수를 포함합니다.

* `ArrayUtil.java` : ArrayUtil.java 파일은 GraphHopper에서 배열 관련 유틸리티 기능을 제공하는 클래스입니다. 정수 배열을 생성, 정렬, 변환하는 메서드들과 배열 초기화 및 임의 생성 등의 유용한 도구를 포함하고 있습니다.

* `BitUtil.java` : BitUtil.java 파일은 GraphHopper에서 비트 조작을 위한 유틸리티 클래스입니다. 정수 데이터를 비트 수준에서 변환하거나 조작하는 다양한 메서드를 제공하며, 효율적인 메모리 사용과 데이터 처리를 지원합니다.

* `BodyAndStatus.java` : BodyAndStatus.java 파일은 GraphHopper에서 HTTP 응답 데이터를 캡슐화하는 클래스입니다. JSON 형식의 본문(`body`)과 상태 코드(`status`)를 저장하며, 이를 반환하는 간단한 게터 메서드를 제공합니다.

* `BreadthFirstSearch.java` : BreadthFirstSearch.java 파일은 GraphHopper에서 그래프 탐색을 구현한 클래스입니다. 너비 우선 탐색(BFS) 알고리즘을 기반으로 하며, 주어진 조건에 따라 노드를 순차적으로 탐색합니다.

* `Constants.java` : Constants.java 파일은 GraphHopper에서 공통적으로 사용되는 상수값들을 정의합니다. 프로젝트 전반에 걸쳐 반복적으로 사용되는 지리적 참조, 설정값 등을 중앙에서 관리하여 코드의 일관성과 가독성을 높입니다.

* `DepthFirstSearch.java` : DepthFirstSearch.java 파일은 GraphHopper에서 깊이 우선 탐색(DFS)을 구현한 클래스입니다. 그래프의 노드를 깊이 우선으로 순회하며 특정 조건에 따라 탐색 로직을 사용자 정의할 수 있도록 설계되었습니다.

* `DistanceCalc.java` : DistanceCalc.java 파일은 GraphHopper에서 두 지점 간의 거리를 계산하는 인터페이스입니다. Haversine 공식을 사용해 지구 상의 거리와 3D 거리 계산을 수행하며, 서브클래스를 통해 정밀도 조정이 가능합니다.

* `DistanceCalc3D.java` : DistanceCalc3D.java 파일은 GraphHopper에서 3D 거리 계산을 구현한 클래스입니다. 유클리드 기하학과 Haversine 공식을 결합하여 지구상의 두 지점 간 3D 거리를 계산하며, 고도 차이와 평면 거리를 모두 고려합니다.

* `DistanceCalcEarth.java` : DistanceCalcEarth.java 파일은 GraphHopper에서 지구 표면의 두 점 사이의 거리를 계산하는 데 사용되는 클래스입니다. Haversine 공식을 기반으로 하며, 지리적 좌표의 정확한 거리 계산을 지원합니다.

* `DistanceCalcEuclidean.java` : DistanceCalcEuclidean.java 파일은 GraphHopper에서 유클리드 거리 계산을 구현한 클래스입니다. 두 점 사이의 직선 거리를 계산하며, 단순하고 효율적인 방식으로 거리 계산이 필요한 경우에 사용됩니다.

* `DistancePlaneProjection.java` : DistancePlaneProjection.java 파일은 GraphHopper에서 지구를 평면으로 투영하여 두 지점 간 거리를 계산하는 클래스입니다. Mercator 투영법과 수학적 접근을 활용하며, 작은 거리 계산에 적합한 효율적인 알고리즘을 제공합니다.

* `Downloader.java` : Downloader.java 파일은 GraphHopper에서 HTTP 요청을 통해 데이터를 다운로드하고 처리하는 클래스입니다. 압축된 데이터를 해제하거나 파일로 저장하는 기능을 제공하며, 사용자 지정 HTTP 헤더와 타임아웃 설정을 지원합니다.

* `EdgeExplorer.java` : EdgeExplorer.java 파일은 GraphHopper에서 엣지 탐색을 위한 인터페이스를 정의합니다. 특정 노드를 기준으로 인접 엣지를 탐색할 수 있는 `setBaseNode` 메서드를 제공하며, 다중 스레드 환경에서는 각 스레드마다 별도의 인스턴스를 사용해야 합니다.

* `EdgeIterator.java` : EdgeIterator.java 파일은 GraphHopper에서 특정 노드에 연결된 모든 엣지를 반복(iterate)하며 탐색하는 인터페이스를 정의합니다. `next()` 메서드를 통해 다음 엣지로 이동하며, 노드와 엣지의 상태를 효율적으로 관리합니다. `NO_EDGE` 및 `ANY_EDGE`와 같은 특별 값을 사용해 특정 조건을 처리합니다.

* `EdgeIteratorState.java` : EdgeIteratorState.java 파일은 GraphHopper에서 엣지의 상태를 나타내는 인터페이스입니다. 노드, 거리, 가중치, 플래그 등의 속성을 설정하거나 가져오는 메서드를 제공하며, 경로 탐색과 관련된 정보를 관리합니다.

* `FetchMode.java` : FetchMode.java 파일은 GraphHopper에서 `EdgeIteratorState#fetchWayGeometry` 호출 시 어떤 노드가 `PointList`에 포함되어야 하는지를 지정하는 열거형(enum)입니다. `TOWER_ONLY`, `PILLAR_ONLY`, `ALL` 등 다양한 모드를 정의하여 데이터 추출 방식을 조정할 수 있습니다.

* `GHUtility.java` : GHUtility.java 파일은 GraphHopper에서 다양한 그래프 관련 유틸리티 메서드를 제공하는 클래스입니다. 공통 노드 검색, 방향 전환 비용 계산, 경로 최적화와 같은 여러 편의 기능을 포함하고 있으며, 그래프 데이터 구조를 효율적으로 관리하는 데 사용됩니다.

* `GitInfo.java` : GitInfo.java 파일은 GraphHopper에서 Git 저장소 관련 정보를 관리하는 클래스입니다. 현재 커밋 해시, 브랜치 이름 등의 메타데이터를 추적하며, 애플리케이션의 빌드 상태와 관련된 정보를 제공합니다.

* `Instructions.java` : Instructions.java 파일은 GraphHopper에서 내비게이션 기기 또는 애플리케이션이 특정 위치에서 가장 가까운 지점을 찾거나 다음 경로 지시를 제공하도록 돕는 유틸리티 클래스입니다. 지점 간 거리 계산 및 내비게이션 경로의 지시를 효율적으로 관리합니다.

* `MiniPerfTest.java` : MiniPerfTest.java 파일은 GraphHopper에서 성능 테스트를 수행하는 유틸리티 클래스입니다. 작업(Task)의 실행 시간을 측정하고 최소, 최대, 평균 실행 시간을 계산하며, JVM 최적화를 방지하기 위한 더미 결과 값을 포함합니다.

* `PathMerger.java` : PathMerger.java 파일은 GraphHopper에서 여러 경로를 하나로 병합하는 클래스입니다. 경로의 지점, 속성, 거리 등을 관리하며, 결과 경로를 `ResponsePath`로 반환하여 라우팅 결과를 처리하는 데 사용됩니다.

* `PathSimplification.java` : PathSimplification.java 파일은 GraphHopper에서 경로 데이터를 단순화하는 클래스입니다. Ramer-Douglas-Peucker 알고리즘을 사용해 불필요한 포인트를 제거하며, 메모리 사용량을 줄이고 경로 데이터를 더 효율적으로 처리할 수 있도록 합니다.

* `RamerDouglasPeucker.java` : RamerDouglasPeucker.java 파일은 GraphHopper에서 Ramer-Douglas-Peucker 알고리즘을 사용해 경로 데이터를 단순화하는 클래스입니다. 불필요한 포인트를 제거해 경로를 최적화하고 메모리 사용량을 줄이는 데 사용됩니다.

* `SimpleIntDeque.java` : SimpleIntDeque.java 파일은 GraphHopper에서 정수를 저장하고 관리하기 위한 단순한 큐(Deque) 자료 구조를 구현합니다. 큐의 삽입과 제거 작업을 효율적으로 수행하며, 메모리 최적화를 위해 설계되었습니다.

* `StopWatch.java` : StopWatch.java 파일은 GraphHopper에서 간단한 성능 측정을 위한 클래스로, 작업의 시작과 종료 시간을 기록하여 소요 시간을 초 단위 또는 밀리초 단위로 반환합니다. 작업 이름 설정 및 진행 중인 시간 계산 기능도 제공합니다.

* `TranslationMap.java` : TranslationMap.java 파일은 GraphHopper에서 다국어 지원을 위한 번역 데이터를 관리하는 클래스입니다. 번역된 문자열을 메모리에 저장하고 필요에 따라 특정 언어로 데이터를 가져오는 기능을 제공합니다. Translation 객체를 사용하여 지역화된 메시지를 처리합니다.

* `Unzipper.java` : Unzipper.java 파일은 GraphHopper에서 ZIP 파일을 해제하는 기능을 제공하는 클래스입니다. 지정된 디렉토리로 압축을 해제하고, 파일 경로를 검증하여 보안성을 유지하며, 진행 상태를 LongConsumer로 전달할 수 있는 기능도 포함하고 있습니다.

* `XFirstSearch.java` : XFirstSearch.java 파일은 GraphHopper에서 BFS(너비 우선 탐색)와 DFS(깊이 우선 탐색)의 공통 기능을 정의하는 추상 클래스입니다. 그래프 탐색을 위한 비트셋(BitSet)을 생성하고, 노드와 엣지를 처리하는 메서드를 제공합니다.

## core/src/main/java/com/graphhopper/util/details

* `AbstractPathDetailsBuilder.java` : AbstractPathDetailsBuilder.java 파일은 GraphHopper에서 경로의 세부 정보를 생성하는 추상 클래스입니다. 경로의 특정 구간에 대한 값과 간격을 기반으로 `PathDetail` 객체를 구성하며, 시작 및 종료 구간을 설정하고 세부 정보를 빌드하는 메서드를 제공합니다.

* `AverageSpeedDetails.java` : AverageSpeedDetails.java 파일은 GraphHopper에서 경로의 평균 속도를 계산하고 세부 정보를 제공하는 클래스입니다. 특정 경로 구간의 속도를 계산하여 `PathDetail` 객체에 추가하며, 계산 시 매우 짧은 엣지로 인한 무한대 속도를 방지하기 위해 정밀도를 조정할 수 있습니다.

* `BooleanDetails.java` : BooleanDetails.java 파일은 GraphHopper에서 경로 세부 정보로 불리언 값을 처리하는 클래스입니다. `AbstractPathDetailsBuilder`를 확장하며, 경로의 특정 구간에서 불리언 속성이 변경되는지 확인하고 그 값을 관리합니다.

* `ConstantDetailsBuilder.java` : ConstantDetailsBuilder.java 파일은 GraphHopper에서 경로의 특정 구간에 대해 일정한 값의 세부 정보를 생성하는 클래스입니다. 값이 변하지 않는 속성을 처리하며, 매우 짧은 경로에서도 일관된 세부 정보를 제공하도록 설계되었습니다.

* `DecimalDetails.java` : DecimalDetails.java 파일은 GraphHopper에서 경로의 소수점 기반 세부 정보를 관리하는 클래스입니다. `DecimalEncodedValue`를 사용해 경로 구간의 소수점 값을 처리하며, 무한대 값을 처리하거나 세부 정보를 특정 정밀도로 조정할 수 있는 기능을 제공합니다.

* `DistanceDetails.java` : DistanceDetails.java 파일은 GraphHopper에서 경로의 거리 정보를 생성하는 클래스입니다. `AbstractPathDetailsBuilder`를 확장하여 경로의 각 엣지에 대한 거리를 계산하고, 이를 `PathDetail` 객체로 반환합니다.

* `EdgeIdDetails.java` : EdgeIdDetails.java 파일은 GraphHopper에서 경로의 각 구간에 대한 엣지 ID를 계산하는 클래스입니다. `AbstractPathDetailsBuilder`를 확장하며, `EdgeIteratorState`를 기반으로 엣지 ID를 추출하여 `PathDetail` 객체에 추가합니다. VirtualEdge의 경우 원래 엣지 키를 사용합니다.

* `EdgeKeyDetails.java` : EdgeKeyDetails.java 파일은 GraphHopper에서 경로의 엣지 키를 처리하는 클래스입니다. `EdgeIteratorState`에서 엣지 키를 추출하고, 이를 `PathDetail` 객체에 포함시켜 경로의 특정 엣지 정보를 관리합니다.

* `EnumDetails.java` : EnumDetails.java 파일은 GraphHopper에서 `EnumEncodedValue`를 기반으로 경로 세부 정보를 관리하는 클래스입니다. 특정 엣지에서 값이 변경될 때마다 이를 추적하고, 해당 값을 `PathDetail` 객체로 저장하며, 값의 변경 여부를 확인하여 경로를 효율적으로 처리합니다.

* `IntDetails.java` : IntDetails.java 파일은 GraphHopper에서 경로의 구간별로 정수 값을 관리하는 클래스입니다. `IntEncodedValue`를 사용하여 경로의 세부 정보를 저장하며, 경로의 각 구간에 대해 정수형 데이터를 처리하고 이를 `PathDetail` 객체로 반환합니다.

* `IntersectionDetails.java` : IntersectionDetails.java 파일은 GraphHopper에서 경로 상의 교차로 세부 정보를 처리하는 클래스입니다. 경로의 각 교차로에서 변화하는 엣지를 추적하여, 각 엣지에 대한 방향과 진입 가능 여부를 관리합니다. 이 클래스는 Maplibre Navigation SDK의 포맷을 기반으로 교차로 데이터를 계산하고 반환하는 역할을 합니다.

* `IntersectionValues.java` : IntersectionValues.java 파일은 GraphHopper에서 교차로의 세부 정보를 관리하는 클래스입니다. 각 교차로에 대한 방향(`bearing`), 진입 여부(`entry`), 진입 노드(`in`), 출구 노드(`out`)를 처리하며, `PathDetail`을 생성하는 메서드도 제공합니다. 이 클래스는 교차로 관련 데이터를 효율적으로 생성하고 변환할 수 있게 돕습니다.

* `KVStringDetails.java` : KVStringDetails.java 파일은 GraphHopper에서 경로의 구간에 대해 키-값 쌍으로 문자열 데이터를 관리하는 클래스입니다. `EdgeIteratorState`에서 `getValue` 메서드를 사용하여 값을 추출하고, 값이 변경될 때마다 이를 `PathDetail` 객체로 저장합니다.

* `PathDetailsBuilder.java` : PathDetailsBuilder.java 파일은 GraphHopper에서 경로 세부 정보를 계산하고 관리하는 인터페이스입니다. 경로의 구간마다 값을 계산하며, 각 구간이 이전 엣지와 다른지 확인하는 메서드를 제공합니다. 또한, 경로 세부 정보를 구축하고, 구간을 시작하고 종료하는 기능을 지원합니다.

* `PathDetailsBuilderFactory.java` : PathDetailsBuilderFactory.java 파일은 GraphHopper에서 요청된 경로 세부 사항을 생성하는 팩토리 클래스입니다. 주어진 경로와 경로 세부 사항 이름 목록을 기반으로 여러 `PathDetailsBuilder` 인스턴스를 생성합니다. 이를 통해 경로의 시간, 거리, 가중치, 도로명, 평균 속도 등 다양한 세부 정보를 관리하고 반환할 수 있습니다.

* `PathDetailsFromEdges.java` : PathDetailsFromEdges.java 파일은 GraphHopper에서 경로 세부 정보를 엣지 기반으로 계산하는 클래스입니다. `PathDetailsBuilder`를 사용하여 경로의 각 구간에 대한 세부 정보를 계산하고, 이를 리스트 형태로 반환합니다. 이 클래스는 각 엣지를 처리하여 경로의 상세 정보를 생성하는 역할을 합니다.

* `StringDetails.java` : StringDetails.java 파일은 GraphHopper에서 경로의 문자열 세부 정보를 처리하는 클래스입니다. `StringEncodedValue`를 사용하여 경로의 구간에 대한 문자열 값을 추적하고, 이전 엣지와 다른 값을 비교하여 경로의 변화를 감지합니다. `PathDetail` 객체로 문자열 정보를 제공하며, 효율적인 경로 관리가 가능합니다.

* `TimeDetails.java` : TimeDetails.java 파일은 GraphHopper에서 경로의 시간 세부 정보를 처리하는 클래스입니다. 주어진 경로의 각 엣지에 대해 소요 시간을 계산하고, `GHUtility`를 사용하여 회전 시간 및 이동 시간을 계산합니다. 경로 세부 사항을 `PathDetail` 객체로 반환하며, 각 구간에 대한 시간을 추적합니다.

* `WeightDetails.java` : WeightDetails.java 파일은 GraphHopper에서 경로의 각 구간에 대한 가중치를 계산하는 클래스입니다. `Weighting` 객체를 사용하여 각 엣지의 가중치를 계산하고, 이를 `PathDetail`로 반환합니다. `isEdgeDifferentToLastEdge` 메서드를 통해 이전 엣지와 다른 경우에만 계산을 수행하여 성능을 최적화합니다.

## core/src/main/java/com/graphhopper/util/shapes

* `BBox.java` : BBox.java 파일은 GraphHopper에서 지리적 경계 상자를 나타내는 클래스입니다. 주어진 경계 내에서 위치 데이터를 포함하고, 해당 경계를 검사하거나 다른 경계와 비교하는 기능을 제공합니다. 이 클래스는 주로 공간 데이터의 효율적인 검색 및 필터링을 위해 사용됩니다.

* `Circle.java` : Circle.java 파일은 GraphHopper에서 원형 영역을 나타내는 클래스입니다. 주어진 중심 좌표와 반지름을 사용하여 원의 경계를 정의하며, 해당 원 안에 점이 포함되는지 확인하는 `contains` 메서드를 제공합니다. `DistanceCalc`를 사용하여 거리 계산을 수행하고, 원의 경계 상자를 계산하는 기능도 포함됩니다.

* `Polygon.java` : Polygon.java 파일은 GraphHopper에서 다각형 영역을 정의하는 클래스입니다. 주어진 좌표들을 사용하여 다각형을 만들고, 이 다각형을 포함하는 `PreparedGeometry` 객체를 생성합니다. `BBox` 객체를 사용하여 다각형의 경계 상자를 추출하며, `Shape` 인터페이스를 구현하여 다양한 공간 계산 기능을 제공합니다.

* `Shape.java` : Shape.java 파일은 GraphHopper에서 다양한 형태(원, 다각형, 직사각형 등)의 지리적 형태를 처리하는 인터페이스입니다. `intersects` 메서드는 주어진 점 목록과 형태가 교차하는지 확인하며, `contains`는 특정 위도와 경도가 형태 내부에 포함되는지 확인합니다. 또한, `getBounds` 메서드는 형태의 최소 경계 상자를 반환합니다.

## core/src/main/resources/com/graphhopper

여기에는 .java 파일이 없음.

## core/src/test/java/com/graphhopper

* `GraphHopperProfileTest.java` : GraphHopperProfileTest.java 클래스는 `GraphHopper` 프로파일의 직렬화 및 역직렬화 테스트를 수행하는 단위 테스트 클래스입니다. 이 클래스는 JSON 형식으로 직렬화된 프로파일 정보를 `ObjectMapper`를 사용하여 Java 객체로 역직렬화하고, 그 값들이 예상한 값과 일치하는지 검증합니다. 또한, 프로파일 이름 중복 오류를 체크하는 테스트도 포함되어 있습니다. 이를 통해 `Profile` 객체의 직렬화 및 역직렬화 과정에서 발생할 수 있는 오류를 확인합니다.

* `GraphHopperTest.java` : GraphHopperTest.java 클래스는 GraphHopper의 핵심 기능을 테스트하는 단위 테스트 클래스입니다. 이 테스트는 다양한 경로 탐색 기능, 설정, 그리고 데이터 처리가 예상대로 동작하는지 확인하기 위해 여러 단위 테스트를 수행합니다. 테스트는 경로 계산, 프로파일 설정, 지도 데이터 처리 등 GraphHopper의 핵심 로직에 대한 검증을 제공합니다.

## core/src/test/java/com/graphhopper/apache/commons/collections

* `IntFloatBinaryHeapTest.java` : IntFloatBinaryHeapTest.java 클래스는 `IntFloatBinaryHeap`의 기능을 테스트하는 단위 테스트 클래스입니다. 이 테스트 클래스는 `BinaryHeapTestInterface`를 구현하고, `IntFloatBinaryHeap`의 크기, 삽입, 삭제, 업데이트 등의 동작을 검증합니다. `growIfNeeded` 메서드에서는 힙 크기가 동적으로 증가하는지 테스트하며, 우선순위 큐의 동작을 검증합니다.

## core/src/test/java/com/graphhopper/apache/commons/lang3

* `StringUtilsTest.java` : StringUtilsTest.java 클래스는 `StringUtils` 클래스의 다양한 문자열 처리 메서드를 테스트하는 단위 테스트 클래스입니다. 이 클래스는 문자열을 다루는 유틸리티 메서드들에 대해 예상된 동작을 검증하고, 문자열 처리와 관련된 버그를 방지하기 위해 다양한 테스트 케이스를 제공합니다.

## core/src/test/java/com/graphhopper/coll

* `AbstractMyBitSetTest.java` : AbstractMyBitSetTest.java 클래스는 `GHBitSet` 클래스의 기능을 테스트하는 추상 테스트 클래스입니다. `GHBitSet` 객체를 생성하여 비트 집합에 다양한 값을 추가하고, 복사, 문자열 변환, 인덱스 조회 등의 동작을 검증합니다. 주요 테스트는 `copy` 메서드를 통해 비트 집합을 복사하는 기능, `toString` 메서드를 통한 출력 형식 검증, `next` 메서드를 통해 비트 집합에서 지정된 인덱스 이후의 비트 값을 찾는 기능 등을 포함합니다.

* `BinaryHeapTestInterface.java` : BinaryHeapTestInterface.java 클래스는 `BinaryHeap` 자료구조에 대한 테스트 인터페이스입니다. 이 클래스는 `push`, `poll`, `peek`, `update`, `clear` 등의 메서드를 포함하여, 이들 메서드가 올바르게 동작하는지 검증하는 테스트를 제공합니다. `testSize`, `testClear`와 같은 기본 테스트 메서드를 통해 크기 확인 및 힙 초기화, 값 업데이트 등을 수행하고, 우선순위 큐에서의 동작을 검증합니다.

* `BitSetImplTest.java` : BitSetImplTest.java 클래스는 `GHBitSetImpl`의 기능을 테스트하는 단위 테스트 클래스입니다. `AbstractMyBitSetTest`를 상속받아, `createBitSet` 메서드를 오버라이드하여 `GHBitSetImpl` 객체를 생성하고, 해당 객체에 대한 테스트를 수행합니다. 이를 통해 `GHBitSetImpl`의 동작을 검증하고, 비트 집합 기능의 정확성을 확인합니다.

* `GHLongLongBTreeTest.java` : GHLongLongBTreeTest.java 클래스는 `GHLongLongBTree`의 동작을 검증하는 단위 테스트 클래스입니다. 이 클래스는 `GHLongLongBTree`가 정확히 동작하는지, 예를 들어, 데이터를 삽입하거나 검색할 때 올바른 결과를 반환하는지 확인합니다. 또한, 예외 처리를 확인하는 테스트도 포함되어 있어, 잘못된 데이터 입력에 대해 적절히 예외를 발생시키는지 검증합니다.

* `GHSortedCollectionTest.java` : GHSortedCollectionTest.java 클래스는 `GHSortedCollection`의 기능을 테스트하는 단위 테스트 클래스입니다. 이 클래스는 `insert`, `poll`, `update` 메서드의 동작을 검증하며, 삽입된 값의 우선순위와 크기, 업데이트된 값이 올바르게 반영되는지 확인합니다. 각 테스트는 컬렉션에 값을 삽입하고, 우선순위를 기반으로 값을 확인하는 방식으로 `GHSortedCollection`의 정확한 동작을 확인합니다.

* `GHTBitSetTest.java` : GHTBitSetTest.java 클래스는 `GHTBitSet` 클래스의 기능을 테스트하는 단위 테스트 클래스입니다. `createBitSet` 메서드는 `GHBitSet` 객체를 생성하고, `testToString` 메서드는 `GHBitSet`의 문자열 출력이 올바르게 이루어지는지 검증합니다. 이 클래스는 `AbstractMyBitSetTest`를 상속하여 비트 집합 관련 테스트를 수행합니다.

* `MinHeapWithUpdateTest.java` : MinHeapWithUpdateTest.java 클래스는 `MinHeapWithUpdate` 클래스의 기능을 테스트하는 단위 테스트 클래스입니다. 이 클래스는 `BinaryHeapTestInterface`를 구현하여 힙 자료구조의 삽입, 삭제, 업데이트 등의 동작을 검증합니다. `push`, `update`, `poll`, `peekId`, `peekVal` 등의 메서드에 대해 테스트를 수행하며, 크기 및 범위 검사도 포함되어 있습니다. 또한, 예외 처리 테스트도 수행하여 힙이 잘못된 값에 대해 적절히 반응하는지 확인합니다.

## core/src/test/java/com/graphhopper/geohash

* `SpatialKeyAlgoTest.java` : SpatialKeyAlgoTest.java 클래스는 `SpatialKeyAlgo` 알고리즘의 기능을 테스트하는 단위 테스트 클래스입니다. 이 클래스는 공간 키 알고리즘을 사용하여 주어진 공간 데이터에 대한 처리 방식이 정확한지 검증합니다. `SpatialKeyAlgo` 알고리즘의 동작을 다양한 입력에 대해 테스트하여, 공간 데이터의 키 값 추출 및 관리가 올바르게 이루어지는지 확인합니다.

## core/src/test/java/com/graphhopper/isochrone/algorithm

* `QuadEdgeSubdivisionTest.java` : QuadEdgeSubdivisionTest.java 클래스는 `QuadEdgeSubdivision` 클래스의 동작을 테스트하는 단위 테스트 클래스입니다. 이 클래스는 `IncrementalDelaunayTriangulator`를 사용하여 Delaunay 삼각분할을 수행하고, `QuadEdgeSubdivision` 객체에 점을 삽입하면서 그 결과를 검증합니다. `assertEquals`를 사용해 삽입된 점의 수가 예상된 값과 일치하는지 확인합니다. 이 테스트는 삼각형 분할과 관련된 기능이 올바르게 동작하는지 확인하는 데 사용됩니다.

* `ShortestPathTreeTest.java` : ShortestPathTreeTest.java 클래스는 `ShortestPathTree` 알고리즘의 동작을 테스트하는 단위 테스트 클래스입니다. 이 클래스는 주어진 출발점에서 최단 경로 트리를 구축하고, 트리 내의 각 노드가 올바르게 연결되고 계산되는지 확인하는 테스트를 포함합니다. `ShortestPathTree`의 성능과 정확성을 검증하여 경로 계산이 올바르게 수행되는지 확인하는 데 사용됩니다.

## core/src/test/java/com/graphhopper/reader/dem

* `BridgeElevationInterpolatorTest.java` : BridgeElevationInterpolatorTest.java는 다리 관련 고도 데이터를 처리하는 EdgeElevationInterpolator에 대한 테스트 클래스입니다. 이 테스트 클래스는 다리 구조물에 있는 노드에 대해 고도 데이터가 올바르게 보간(interpolate)되는지 확인하는 역할을 합니다.

* `CGIARProviderTest.java` : 파일 `CGIARProviderTest.java`는 `CGIARProvider` 클래스의 기능을 테스트하는 단위 테스트 클래스입니다. 이 테스트는 특정 고도 데이터를 처리하는 `down` 메서드와 파일 이름 생성 기능을 검증하는 테스트를 포함하고 있으며, 고도 값에 대한 예상 동작을 확인하는 테스트 케이스들을 다룹니다.

* `EdgeElevationInterpolatorTest.java` : EdgeElevationInterpolatorTest.java는 EdgeElevationInterpolator 클래스에 대한 테스트를 작성한 코드입니다. 이 테스트는 도로 환경을 기반으로 고도 보간을 수행하는 기능을 검증하며, 특히 다리 구조물에서의 고도 보간이 제대로 동작하는지 확인합니다. setUp() 메서드에서는 필요한 테스트 환경을 설정하고, gatherOuterAndInnerNodeIdsOfStructure() 메서드를 통해 주어진 엣지에 대한 노드 ID를 수집합니다.

* `EdgeElevationSmoothingMovingAverageTest.java` : EdgeElevationSmoothingMovingAverageTest.java는 `EdgeElevationSmoothingMovingAverage` 클래스의 고도 보정 기능을 테스트하는 코드입니다. 이 테스트는 이동 평균을 사용하여 고도를 매끄럽게 조정하는 메서드를 검증합니다. 주요 테스트는 두 점 간 고도가 변하지 않는지, 고도가 일정한 경우 매끄럽게 유지되는지, 그리고 드문 점들에 대해 고도가 적절히 조정되는지를 확인합니다.

* `EdgeElevationSmoothingRamerTest.java` : EdgeElevationSmoothingRamerTest.java는 `EdgeElevationSmoothingRamer` 클래스의 고도 보정 기능을 테스트하는 코드입니다. 이 테스트는 `Ramer` 알고리즘을 사용하여 고도를 매끄럽게 조정하는 메서드를 검증합니다. 주요 테스트는 주어진 고도 데이터를 기준으로 매끄럽게 보정하고, 특정 조건에 따라 고도가 올바르게 수정되는지 확인합니다.

* `EdgeSamplingTest.java` : EdgeSamplingTest.java는 `EdgeSampling` 클래스의 샘플링 기능을 테스트하는 코드입니다. 이 테스트는 고도 데이터를 샘플링하여 도로 그래프에 적용되는 방식이 올바르게 작동하는지 검증합니다. 특히, 고도 샘플을 추출하고 이를 기반으로 정확한 고도 계산이 이루어지는지 확인하는 테스트를 포함하고 있습니다.

* `ElevationInterpolatorTest.java` : ElevationInterpolatorTest.java는 `ElevationInterpolator` 클래스의 고도 보간 기능을 테스트하는 코드입니다. 이 테스트는 두 점을 기반으로 고도를 계산하는 메서드가 예상대로 동작하는지 검증합니다. 여러 가지 테스트 케이스를 통해 다양한 좌표 쌍에 대해 고도를 정확히 계산하는지 확인하며, 보간 계산이 지정된 정확도 `PRECISION` 내에서 올바르게 수행되는지 테스트합니다.

* `GMTEDProviderTest.java` : GMTEDProviderTest.java는 `GMTEDProvider` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 특정 위도 및 경도에 대해 고도 데이터를 올바르게 제공하는지 검증하며, 다양한 경계 조건에 대해 `getMinLatForTile()` 및 `getMinLonForTile()` 메서드의 동작을 확인합니다. 주요 테스트는 주어진 좌표에 대한 최소 위도와 경도를 정확하게 반환하는지를 검증합니다.

* `HGTProviderTest.java` : HGTProviderTest.java는 `HGTProvider` 클래스의 `getEle` 메서드를 테스트하는 코드입니다. 이 테스트는 주어진 위도와 경도에 대해 고도 값을 정확하게 반환하는지 확인합니다. 테스트 케이스는 특정 좌표에서 예상되는 고도 값을 검증하며, 테스트는 비활성화(`@Disabled`) 상태로 되어 있습니다.

* `HeightTileTest.java` : HeightTileTest.java는 `HeightTile` 클래스의 고도 값을 반환하는 기능을 테스트하는 코드입니다. 이 테스트는 `getHeight()` 메서드가 특정 좌표에 대해 예상한 고도를 정확하게 반환하는지 검증합니다. 테스트는 `RAMDirectory`를 사용하여 데이터를 설정하고, 주어진 좌표에서 고도 값을 확인합니다.

* `MultiSourceElevationProviderTest.java` : MultiSourceElevationProviderTest.java는 `MultiSourceElevationProvider` 클래스의 고도 제공 기능을 테스트하는 코드입니다. 이 테스트는 여러 고도 제공자를 결합하여, 주어진 좌표에 대해 정확한 고도를 반환하는지 확인합니다. `testGetEleMocked` 테스트는 두 제공자의 고도 값을 확인하고, `testGetEle`는 실제 고도 값을 비교하여 정확성을 검증합니다. `@Disabled`가 적용된 테스트는 메모리 제한을 변경해야 실행할 수 있습니다.

* `SRTMGL1ProviderTest.java` : SRTMGL1ProviderTest.java는 `SRTMGL1Provider` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 고도 타일의 최소 위도 및 경도 값, 다운로드 URL 생성 기능을 검증합니다. 주요 테스트는 주어진 위도와 경도에 대해 올바른 최소 위도와 경도를 반환하는지, 그리고 예상되는 다운로드 URL을 생성하는지 확인합니다.

* `SRTMProviderTest.java` : SRTMProviderTest.java는 `SRTMProvider` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 주어진 위도와 경도에 대해 고도 파일 이름을 정확하게 생성하는지 확인합니다. 다양한 좌표에 대해 예상되는 파일 이름을 반환하는지 검증하는 테스트를 포함하고 있으며, `getFileName` 메서드의 동작이 정확한지 테스트합니다.

* `SkadiProviderTest.java` : SkadiProviderTest.java는 `SkadiProvider` 클래스의 `getDownloadURL` 및 `getFileName` 메서드를 테스트하는 코드입니다. 이 테스트는 주어진 위도와 경도에 대해 정확한 다운로드 URL과 파일 이름을 생성하는지 검증합니다. 각 테스트 케이스는 특정 좌표에 대한 예상되는 다운로드 URL과 파일 이름을 반환하는지 확인합니다.

* `TunnelElevationInterpolatorTest.java` : TunnelElevationInterpolatorTest.java는 `TunnelElevationInterpolator` 클래스의 고도 보간 기능을 테스트하는 코드입니다. 이 테스트는 터널과 관련된 도로 환경에서 고도 데이터를 정확히 보간하는지 확인합니다. `setUp()` 메서드는 테스트 환경을 설정하고, 고도 보간의 정확성을 검증하는 여러 테스트 케이스를 실행합니다.

## core/src/test/java/com/graphhopper/reader/osm

* `ExtractMembersTest.java` : ExtractMembersTest.java는 `OSMRestrictionConverter` 클래스의 `extractMembers` 메서드를 테스트하는 코드입니다. 이 테스트는 OSM (OpenStreetMap) 제약 조건을 처리할 때, `via` 노드가 있는 경우와 없는 경우를 검증합니다. 주요 테스트는 `via` 노드가 있는지 확인하고, 없는 경우 예외가 발생하는지, 여러 `via` 노드가 있는 경우에도 올바르게 처리되는지 확인합니다.

* `GraphHopperOSMTest.java` : GraphHopperOSMTest.java는 `GraphHopperOSM` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 OpenStreetMap(OSM) 데이터를 처리하여 경로를 계산하는 메서드를 검증합니다. 주요 테스트는 지도 데이터를 로딩하고, 로드된 데이터를 바탕으로 경로 계산이 제대로 이루어지는지 확인하며, 다양한 조건에서 경로 계산 성능을 테스트합니다.

* `OSMReaderTest.java` : OSMReaderTest.java는 `OSMReader` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 OpenStreetMap(OSM) 데이터를 읽고, 그 데이터를 바탕으로 그래프를 구축하는 과정에서 발생할 수 있는 오류를 검증합니다. 주요 테스트는 OSM 파일에서 데이터를 정확하게 읽고, 이를 기반으로 그래프를 생성할 수 있는지, 다양한 조건에서 데이터를 올바르게 처리하는지 확인합니다.

* `OSMReaderUtilityTest.java` : OSMReaderUtilityTest.java는 `OSMReaderUtility` 클래스의 `parseDuration` 메서드를 테스트하는 코드입니다. 이 테스트는 다양한 시간 형식을 올바르게 파싱하는지 확인하며, 잘못된 형식에 대해서는 예외를 발생시키는지 검증합니다. 주요 테스트는 `PT` 및 `P` 형식의 기간 문자열을 처리하고, 예상된 값을 반환하는지, 잘못된 형식의 입력에 대해서는 `IllegalArgumentException`을 발생시키는지를 확인합니다.

* `ReaderElementTest.java` : ReaderElementTest.java는 `ReaderElement` 클래스의 `getElement` 메서드를 테스트하는 코드입니다. 이 테스트는 OSM 데이터를 읽어 들이고, 각 요소들이 올바르게 파싱되고 반환되는지 확인합니다. 주요 테스트는 `id`와 `tags`를 포함한 OSM 요소들을 검증하며, 예상한 값과 비교하여 처리의 정확성을 점검합니다.

* `RestrictionTagParserTest.java` : RestrictionTagParserTest.java는 `RestrictionTagParser` 클래스의 `parseRestrictionTags` 메서드를 테스트하는 코드입니다. 이 테스트는 OSM 데이터에서 제약 조건 태그를 파싱하는 기능을 검증합니다. 주요 테스트는 `restriction` 태그와 관련된 데이터를 파싱할 때 발생할 수 있는 예외와 제약 조건의 정확성을 점검하며, 다양한 차량 유형에 대해 제약 조건을 처리하는 방식도 확인합니다.

* `WayToEdgeConverterTest.java` : WayToEdgeConverterTest.java는 `WayToEdgeConverter` 클래스의 `convertForViaWays` 메서드를 테스트하는 코드입니다. 이 테스트는 OSM에서 `via` 노드를 포함한 경로를 처리하는 기능을 검증합니다. 주요 테스트는 경로에 포함된 `via` 노드를 올바르게 변환하고, 이를 기반으로 생성된 엣지와 노드가 예상대로 맞는지 확인합니다. 추가적으로 여러 엣지가 포함된 경로를 처리하는 테스트도 포함됩니다.

* `WayToEdgesMapTest.java` : WayToEdgesMapTest.java는 `WayToEdgesMap` 클래스의 여러 기능을 테스트하는 코드입니다. 이 테스트는 OSM 데이터를 처리할 때, `reserve` 메서드를 사용하여 경로를 예약하고, 예약된 경로에만 엣지를 추가하는지 확인합니다. 또한, 경로가 순서대로 추가되는지, 중복된 경로가 추가되지 않도록 하는지 확인하는 테스트가 포함되어 있습니다. `putIfReserved` 메서드의 동작을 검증하고, 경로 예약 및 엣지 추가의 정확성을 점검합니다.

## core/src/test/java/com/graphhopper/reader/osm/conditional

* `DateRangeParserTest.java` : DateRangeParserTest.java는 `DateRangeParser` 클래스의 날짜 범위 파싱 기능을 테스트하는 코드입니다. 이 테스트는 다양한 날짜 형식에 대한 범위 문자열을 정확하게 처리할 수 있는지 검증합니다. 주요 테스트는 날짜 범위가 올바르게 파싱되어, 지정된 시작일과 종료일을 정확하게 반환하는지 확인하는 것입니다.

## core/src/test/java/com/graphhopper/routing

* `AStarBidirectionTest.java` : `AStarBidirectionTest.java`는 `A*` 알고리즘을 이용한 양방향 경로 탐색에 대한 테스트 코드입니다. 이 테스트는 경로 탐색 과정에서 불가능한 경로를 처리하는 방법을 검증합니다. 특히, 경로가 잘못된 탐색 트리를 포함할 수 있지만 예외를 발생시키지 않고 이를 처리하는 방식에 대해 검증합니다. `DecimalEncodedValue`를 사용하여 속도 값을 인코딩하고, 여러 가지 거리 계산을 통해 경로의 정확성을 점검합니다.

* `AlternativeRouteCHTest.java` : `AlternativeRouteCHTest.java`는 `AlternativeRoute` 기능을 테스트하는 코드로, Contraction Hierarchies(CH) 알고리즘을 활용하여 다양한 경로를 찾는 기능을 검증합니다. 이 테스트는 여러 경로 선택지에서 최적 경로를 제외한 대체 경로들을 찾아 비교하고, 각 경로가 주어진 조건에 맞게 작동하는지 확인합니다. 또한, `DecimalEncodedValue`를 사용하여 속도 인코딩 및 경로 계산을 테스트합니다.

* `AlternativeRouteEdgeCHTest.java` : `AlternativeRouteEdgeCHTest.java`는 `AlternativeRoute` 기능을 테스트하는 코드로, Contraction Hierarchies (CH) 알고리즘을 사용하여 여러 경로를 찾는 테스트입니다. 이 테스트는 경로 탐색에서 `turnCostEnc`와 `speedEnc`와 같은 인코딩 값을 사용하여, 다양한 경로 조건을 바탕으로 대체 경로를 정확히 찾을 수 있는지 검증합니다. 그래프의 각 엣지와 회전 비용 등을 확인하며, 최적의 경로를 제외한 대체 경로들이 제대로 계산되는지 점검합니다.

* `AlternativeRouteTest.java` : `AlternativeRouteTest.java`는 `AlternativeRoute` 기능을 테스트하는 코드로, 주어진 경로에서 최적 경로 외에 대체 경로들을 정확히 계산하는지 검증합니다. 테스트는 `SpeedWeighting`과 같은 다양한 가중치를 사용하여 경로를 비교하고, `DecimalEncodedValue`를 활용한 속도 인코딩이 올바르게 처리되는지 확인합니다. 주어진 조건 하에 여러 경로를 탐색하고, 각 경로의 정확성과 효율성을 검증하는 테스트가 포함되어 있습니다.

* `CHQueryWithTurnCostsTest.java` : `CHQueryWithTurnCostsTest.java`는 Contraction Hierarchies (CH) 알고리즘에서 회전 비용(turn cost)을 처리하는 기능을 테스트하는 코드입니다. 이 테스트는 경로 탐색 시 회전 비용을 정확하게 반영하는지 확인하며, 다양한 경로에서 turn costs가 올바르게 처리되는지 검증합니다. 또한, CH 그래프에서 빠른 경로 계산을 유지하면서 회전 비용을 포함한 경로가 정확히 계산되는지를 확인합니다.

* `CustomizableConditionalRestrictionsTest.java` : `CustomizableConditionalRestrictionsTest.java`는 조건부 접근 제한을 테스트하는 코드입니다. 이 테스트는 특정 날짜 범위 내에서 접근 가능한 경로를 찾아내고, 조건에 맞는 제한이 경로 계산에 반영되는지 검증합니다. `GHRequest`를 사용하여 주어진 경로에 대해 조건부 모델을 적용하고, `CustomModel`을 활용해 제한 조건을 테스트합니다. 이 테스트는 `foot_temporal_access`와 같은 사용자 정의 인코딩을 통해 경로가 조건에 맞게 계산되는지 확인합니다.

* `DefaultBidirPathExtractorTest.java` : `DefaultBidirPathExtractorTest.java`는 `DefaultBidirPathExtractor` 클래스의 경로 추출 기능을 테스트하는 코드입니다. 이 테스트는 양방향 경로 탐색에서, 주어진 그래프와 조건에 따라 경로를 정확하게 추출하는지 검증합니다. `DecimalEncodedValue`를 사용하여 속도와 회전 비용을 인코딩하고, `SpeedWeighting`을 통해 경로 계산을 진행합니다. 테스트는 양방향 경로 추출에서 예상한 경로와 거리 계산이 정확히 이루어지는지 확인합니다.

* `DijkstraBidirectionCHTest.java` : `DijkstraBidirectionCHTest.java`는 `Dijkstra` 알고리즘을 사용한 양방향 경로 탐색을 테스트하는 코드입니다. 이 테스트는 Contraction Hierarchies (CH) 알고리즘을 적용하여, 주어진 그래프에서 최단 경로를 양방향으로 빠르게 찾을 수 있는지 검증합니다. 또한, `DecimalEncodedValue`와 같은 인코딩 값을 사용하여 속도 및 경로 계산을 테스트하며, 양방향 경로 탐색의 정확성과 효율성을 점검합니다.

* `DijkstraOneToManyTest.java` : `DijkstraOneToManyTest.java`는 `DijkstraOneToMany` 알고리즘을 테스트하는 코드입니다. 이 테스트는 여러 출발점에서 다수의 목적지로 경로를 계산하는 기능을 검증합니다. `DecimalEncodedValue`를 사용하여 속도 인코딩을 처리하며, `SpeedWeighting`을 통해 경로의 가중치를 설정하고, 경로가 정확하게 계산되는지 확인합니다. 주어진 출발점과 목적지들에 대해 경로 계산을 실행하고, 결과가 예상대로 나오는지 점검하는 여러 테스트 케이스가 포함되어 있습니다.

* `DirectedBidirectionalDijkstraTest.java` : `DirectedBidirectionalDijkstraTest.java`는 `Directed Bidirectional Dijkstra` 알고리즘을 테스트하는 코드입니다. 이 테스트는 양방향 경로 탐색에서 Dijkstra 알고리즘이 올바르게 동작하는지 검증합니다. 주어진 시작점과 목표 지점에 대해 양방향으로 경로를 계산하며, 다양한 테스트 케이스를 통해 알고리즘의 정확성과 효율성을 확인합니다.

* `DirectedRoutingTest.java` : `DirectedRoutingTest.java`는 `DirectedRouting` 기능을 테스트하는 코드입니다. 이 테스트는 방향성 경로 탐색 알고리즘을 검증하며, 경로 계산에서 방향성이 잘 반영되는지 확인합니다. 여러 출발지와 목적지에 대해 방향을 고려한 최단 경로를 찾고, 다양한 제한 사항과 조건을 처리하는 테스트 케이스가 포함되어 있습니다. 경로 탐색의 정확성과 성능을 점검하는 데 초점을 맞추고 있습니다.

* `DirectionResolverOnQueryGraphTest.java` : `DirectionResolverOnQueryGraphTest.java`는 `DirectionResolver`의 동작을 테스트하는 코드입니다. 이 테스트는 경로 탐색에서 주어진 `queryGraph`에 대해 방향을 정확히 해결하는 기능을 검증합니다. `FlagEncoder`와 같은 인코딩을 활용하여 경로에 대한 방향 정보를 추출하고, 올바르게 해석되는지 확인하는 테스트를 포함하고 있습니다. 주로 방향성에 따른 경로 계산의 정확성을 점검합니다.

* `DirectionResolverTest.java` : `DirectionResolverTest.java`는 `DirectionResolver` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 경로 탐색에서 주어진 방향을 정확하게 처리하는지 검증합니다. `FlagEncoder`와 같은 인코딩을 사용하여 경로의 방향성을 처리하고, 경로 계산이 제대로 수행되는지 점검합니다. 또한, 방향 정보를 통해 경로 탐색에서 발생할 수 있는 다양한 예외 상황에 대해 테스트를 진행합니다.

* `EdgeBasedRoutingAlgorithmTest.java` : `EdgeBasedRoutingAlgorithmTest.java`는 `EdgeBasedRoutingAlgorithm`의 경로 탐색 알고리즘을 테스트하는 코드입니다. 이 테스트는 엣지 기반 경로 탐색 방식을 사용하여 경로를 효율적으로 계산할 수 있는지 검증합니다. 다양한 경로 조건과 설정을 바탕으로 테스트를 진행하며, 경로 계산의 정확성과 성능을 점검합니다. `EdgeBasedRoutingAlgorithm`이 실제 경로 계산에서 어떻게 작동하는지 확인하는 다양한 테스트 케이스를 포함하고 있습니다.

* `HeadingResolverTest.java` : `HeadingResolverTest.java`는 `HeadingResolver` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 주어진 경로에서 헤딩 정보를 해결하는 기능을 검증합니다. `BooleanEncodedValue`와 `DecimalEncodedValue`를 사용하여 경로의 속도와 접근 가능성을 인코딩하고, `QueryGraph`를 활용하여 경로의 정확성을 점검합니다. 특히, 경로가 직선일 때 올바른 헤딩을 계산하는지 확인하며, 다양한 테스트 케이스를 통해 헤딩 해결 기능의 정확성을 검증합니다.

* `HeadingRoutingTest.java` : `HeadingRoutingTest.java`는 `HeadingRouting` 알고리즘의 기능을 테스트하는 코드입니다. 이 테스트는 경로 탐색에서 헤딩 정보가 제대로 반영되는지 확인합니다. 주어진 출발지와 목적지 사이에서 헤딩 값을 사용해 경로의 방향성을 처리하며, 다양한 테스트 케이스를 통해 헤딩 기반 경로 탐색의 정확성과 효율성을 검증합니다. 또한, 방향성 계산이 올바르게 이루어지는지 점검합니다.

* `PathTest.java` : `PathTest.java`는 경로 탐색 알고리즘의 여러 기능을 테스트하는 코드입니다. 이 테스트는 경로 계산에 사용되는 다양한 파라미터와 조건들을 확인하고, 경로의 정확성과 효율성을 검증합니다. 특히, `Map`을 대신하여 `List`를 사용하는 API 변경 사항을 테스트하며, 경로 탐색과 관련된 여러 테스트 케이스를 실행하여 올바른 결과가 반환되는지 점검합니다.

* `PriorityRoutingTest.java` : `PriorityRoutingTest.java`는 경로 탐색에서 우선순위 기반의 라우팅 기능을 테스트하는 코드입니다. 이 테스트는 `DecimalEncodedValue`를 사용하여 속도와 우선순위를 인코딩하고, `EncodingManager`를 통해 그래프를 생성한 후, 주어진 노드 간에 최적 경로를 계산하는 기능을 검증합니다. 테스트는 경로의 우선순위가 올바르게 적용되고, `PriorityCode`와 같은 요소들이 경로 계산에 적절히 반영되는지 점검합니다.

* `QueryRoutingCHGraphTest.java` : `QueryRoutingCHGraphTest.java`는 Contraction Hierarchies (CH) 그래프에서의 경로 탐색을 테스트하는 코드입니다. 이 테스트는 주어진 그래프와 조건에서 `QueryRouting` 알고리즘이 제대로 동작하는지 검증하며, 특히 `fastest` 가중치를 사용하는 경로 계산을 확인합니다. 여러 테스트 케이스를 통해 경로 탐색 성능과 정확성을 점검하고, 다양한 조건에서 경로가 정확히 계산되는지 확인합니다.

* `RandomCHRoutingTest.java` : `RandomCHRoutingTest.java`는 Contraction Hierarchies (CH) 알고리즘을 사용하여 임의의 경로 탐색을 테스트하는 코드입니다. 이 테스트는 다양한 경로 탐색 조건과 파라미터 설정을 통해, CH 알고리즘이 주어진 조건 하에서 올바르게 동작하는지 검증합니다. 또한, `TurnCost`와 `SpeedWeighting`을 활용하여 경로의 속도와 회전 비용을 반영하며, 주어진 경로에서 효율적으로 최단 경로를 계산할 수 있는지 확인합니다.

* `RandomizedRoutingTest.java` : `RandomizedRoutingTest.java`는 경로 탐색 알고리즘에서 랜덤화된 조건을 사용하여 경로를 계산하는 기능을 테스트하는 코드입니다. 이 테스트는 다양한 경로 탐색 조건을 무작위로 설정하고, 주어진 그래프에서 경로가 올바르게 계산되는지 확인합니다. 여러 가지 변수와 설정을 통해 알고리즘의 정확성과 성능을 평가하며, 다양한 조건 하에서 경로 탐색이 정확히 이루어지는지 검증합니다.

* `RoundTripRoutingTest.java` : `RoundTripRoutingTest.java`는 `RoundTripRouting` 기능을 테스트하는 코드입니다. 이 테스트는 출발지로 돌아오는 경로를 정확히 계산하는지 검증하며, 주어진 조건 하에서 경로 탐색이 올바르게 수행되는지 확인합니다. `SpeedWeighting`을 사용하여 속도를 고려하고, `DecimalEncodedValue`와 같은 인코딩을 활용해 경로 탐색이 정확히 이루어지는지 점검합니다. 경로의 정확성과 성능을 평가하는 여러 테스트 케이스가 포함되어 있습니다.

* `RoutingAlgorithmTest.java` : `RoutingAlgorithmTest.java`는 다양한 경로 탐색 알고리즘을 테스트하는 코드입니다. 이 테스트는 여러 라우팅 알고리즘이 주어진 조건 하에서 정확하게 경로를 계산하는지 확인합니다. `SpeedWeighting`, `TurnCost`, `DecimalEncodedValue`와 같은 인코딩을 사용하여 다양한 경로 계산 방식의 정확성을 점검하며, 주어진 그래프에서 경로 탐색의 효율성과 성능을 평가합니다.

* `RoutingAlgorithmWithOSMTest.java` : `RoutingAlgorithmWithOSMTest.java`는 OpenStreetMap(OSM) 데이터를 사용하여 라우팅 알고리즘을 테스트하는 코드입니다. 이 테스트는 OSM 데이터를 기반으로 경로를 계산하고, 여러 라우팅 알고리즘이 주어진 그래프에서 정확하게 동작하는지 확인합니다. 다양한 경로 계산 조건과 파라미터 설정을 통해 알고리즘의 정확성과 성능을 점검합니다. `TurnCost`와 `SpeedWeighting`을 적용하여 계산된 경로가 예상대로 동작하는지 테스트합니다.

* `RoutingCHGraphImplTest.java` : `RoutingCHGraphImplTest.java`는 Contraction Hierarchies (CH) 그래프에서 라우팅 기능을 테스트하는 코드입니다. 이 테스트는 `RoutingCHGraphImpl` 구현을 통해 경로 탐색이 정확하게 이루어지는지 확인합니다. 다양한 테스트 케이스를 통해 CH 알고리즘이 주어진 그래프에 대해 효율적으로 경로를 계산하는지 점검하며, 경로 탐색에서 발생할 수 있는 예외 사항이나 오류를 처리하는 능력을 테스트합니다.

## core/src/test/java/com/graphhopper/routing/ch

* `CHPreparationGraphTest.java` : `CHPreparationGraphTest.java`는 Contraction Hierarchies (CH) 그래프 준비 과정에 대한 기능을 테스트하는 코드입니다. 이 테스트는 `CHPreparationGraph` 클래스가 주어진 그래프에서 엣지를 추가하고, 축소된 그래프에서 경로 탐색을 위한 준비가 제대로 이루어지는지 검증합니다. 주된 테스트는 노드와 엣지의 연결 상태, 그래프 축소 준비 상태 및 축소된 그래프에서의 엣지 탐색 기능을 점검하는 것입니다. `prepareForContraction` 메서드를 사용하여 그래프를 준비하고, 축소된 그래프에서의 경로 추적을 확인하는 테스트가 포함됩니다.

* `CHPreparationHandlerTest.java` : `CHPreparationHandlerTest.java`는 `CHPreparationHandler` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 `setCHProfiles` 메서드를 사용하여 Contraction Hierarchies (CH) 그래프 준비 작업을 활성화하고, `isEnabled` 메서드로 해당 기능이 활성화되는지 검증합니다. 테스트는 CH 프로파일을 설정하여 준비 작업이 제대로 수행되는지, 활성화 여부를 확인합니다.

* `CHTurnCostTest.java` : `CHTurnCostTest.java`는 Contraction Hierarchies (CH) 알고리즘에서 회전 비용(turn cost)이 경로 탐색에 제대로 반영되는지 테스트하는 코드입니다. 이 테스트는 회전 비용이 포함된 경로 계산에서 CH 그래프가 어떻게 처리되는지 확인합니다. 다양한 테스트 케이스를 통해 경로 탐색 시 회전 비용이 제대로 적용되고, 경로의 정확성과 효율성을 점검합니다.

* `EdgeBasedNodeContractorTest.java` : `EdgeBasedNodeContractorTest.java`는 `EdgeBasedNodeContractor` 클래스를 테스트하는 코드입니다. 이 테스트는 Contraction Hierarchies (CH) 알고리즘에서 노드를 축소하는 과정이 제대로 수행되는지 검증합니다. 주어진 그래프에서 노드를 효율적으로 축소하고, 축소된 그래프에서 경로 탐색이 정상적으로 이루어지는지를 점검합니다. 여러 테스트 케이스를 통해 축소 작업이 올바르게 수행되는지, 최단 경로 계산이 제대로 이루어지는지 확인합니다.

* `EdgeBasedWitnessPathSearcherTest.java` : `EdgeBasedWitnessPathSearcherTest.java`는 `EdgeBasedWitnessPathSearcher` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 주어진 그래프에서 경로가 축소된 후 최단 경로를 찾기 위한 "witness path"를 검색하는 기능을 검증합니다. 테스트는 여러 경로 조건을 설정하고, 축소된 그래프에서 경로를 탐색하며, 주어진 제한 조건 내에서 경로가 예상대로 계산되는지 확인합니다. `prepareForContraction`과 `runSearch` 메서드를 사용하여 경로를 검색하고, 결과가 정확한지 확인하는 테스트가 포함되어 있습니다.

* `NodeBasedNodeContractorTest.java` : `NodeBasedNodeContractorTest.java`는 Contraction Hierarchies (CH) 알고리즘에서 노드를 축소하는 기능을 테스트하는 코드입니다. 이 테스트는 `NodeBasedNodeContractor` 클래스의 동작을 검증하며, 주어진 그래프에서 노드를 축소하고, 축소된 그래프에서 경로 탐색이 올바르게 수행되는지를 확인합니다. 축소된 그래프에서 경로가 효율적으로 계산되는지, 노드 축소가 예상대로 이루어지는지 점검하는 다양한 테스트 케이스가 포함되어 있습니다.

* `NodeBasedWitnessPathSearcherTest.java` : `NodeBasedWitnessPathSearcherTest.java`는 `NodeBasedWitnessPathSearcher` 클래스의 기능을 테스트하는 코드입니다. 이 테스트는 Contraction Hierarchies (CH) 알고리즘에서 "witness path"를 계산하는 기능을 검증합니다. 테스트는 특정 노드를 무시하고 경로를 찾는 방식과 경로의 길이를 비교하며, 주어진 조건에 맞는 경로가 올바르게 계산되는지를 점검합니다. `findUpperBound` 메서드를 사용하여 상한 값을 구하고, `getSettledNodes` 메서드로 처리된 노드의 수를 확인합니다.

* `OnFlyStatisticsCalculatorTest.java` : `OnFlyStatisticsCalculatorTest.java`는 `OnFlyStatisticsCalculator` 클래스의 통계 계산 기능을 테스트하는 코드입니다. 이 테스트는 데이터의 평균과 분산을 실시간으로 계산하는 기능을 검증합니다. 여러 값을 추가한 후, `getMean()` 메서드를 사용하여 평균 값을, `getVariance()` 메서드를 사용하여 분산 값을 계산하고, 계산된 값이 예상 범위 내에 있는지 확인합니다. `assertEquals`를 사용하여 계산된 값이 정확한지 검증하는 테스트가 포함되어 있습니다.

* `PrepareContractionHierarchiesTest.java` : `PrepareContractionHierarchiesTest.java`는 Contraction Hierarchies (CH) 알고리즘의 준비 작업을 테스트하는 코드입니다. 이 테스트는 그래프를 준비하고 축소하는 과정에서 발생할 수 있는 문제를 검증합니다. 주요 테스트는 `prepare` 및 `contract` 메서드를 사용하여 그래프의 축소 작업이 올바르게 수행되는지 확인하며, 축소된 그래프에서 최단 경로 탐색이 정확히 이루어지는지 점검합니다.

## core/src/test/java/com/graphhopper/routing/ev

* `BooleanEncodedValueTest.java` : 


